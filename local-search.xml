<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>关于大一的一些总结</title>
    <link href="/2025/05/29/%E5%85%B3%E4%BA%8E%E5%A4%A7%E4%B8%80%E7%9A%84%E4%B8%80%E4%BA%9B%E6%80%BB%E7%BB%93/"/>
    <url>/2025/05/29/%E5%85%B3%E4%BA%8E%E5%A4%A7%E4%B8%80%E7%9A%84%E4%B8%80%E4%BA%9B%E6%80%BB%E7%BB%93/</url>
    
    <content type="html"><![CDATA[<h1 id="关于大一的一些总结"><a href="#关于大一的一些总结" class="headerlink" title="关于大一的一些总结"></a>关于大一的一些总结</h1><p>一转眼进入大学快一年了，最近看了知乎上不少大佬的总结，感想颇多，在期末复习空闲之余，也来写一写，算是对大一的总结吧。</p><h2 id="9-11月"><a href="#9-11月" class="headerlink" title="9-11月"></a>9-11月</h2><p>一进入大学，和预想中的一样如同脱笼之鸟，自由自在。在初开学那几个月，也不知道自己要做什么，按部就班的上课下课，浑浑噩噩，回到宿舍就是玩，当时还报名了大学生数学竞赛，想着把高数学完，不过11月考完之后也是结果惨淡（笑</p><h2 id="11-1月"><a href="#11-1月" class="headerlink" title="11-1月"></a>11-1月</h2><p>从11月开始，逐渐萌生了“我能在空闲时间做点什么”的想法，一开始，看着网上的教程搭了个github.io的博客，一股脑放了几篇课上做的笔记进去。后面上了新生研讨课，对深度学习有一些兴趣，花了两周时间在b站上看完了吴恩达的机器学习视频（效率太低了），后来再看到yolov8的演示视频，就想着自己也做一个（基本出于玩的心理吧，想做一个aimbot出来（，在鏖战几个晚上，从下载python，配置虚拟环境，更新显卡驱动，一步步也是做了个半成品出来。后面到了十二月就开始复习期末考了（（（</p><h2 id="寒假"><a href="#寒假" class="headerlink" title="寒假"></a>寒假</h2><p>很遗憾，寒假可以说是基本啥也没做（），由于下学期要用到Qt做大作业，寒假下了Qt，用Qt仿照教程写了个贪吃蛇，俄罗斯方块。<br>寒假还了解到学院对于低年级生有一个关于科研的计划，挺有兴趣就参加了，在寒假的末尾过了一下《Deep Learning》前面的数学基础部分。</p><h2 id="2-5月"><a href="#2-5月" class="headerlink" title="2-5月"></a>2-5月</h2><p>看到关于深度学习入门有推荐看CS231N的，遂去看了CS231N，就认真写了assignment<br>1，后面几乎是对照网上答案写一行看一行（bushi 到了三月底，开始准备程设的大作业了，要求很宽泛，利用C++语言编写一个图形化程序，最开始就打算写一个简单的坦克大战，后面觉得太糊弄了，又去学了OpenGL，照着教程从着色器开始，写了一个肉鸽类小游戏。完成这些到了四月中，关于CV领域，看了几篇入门级的论文（R-CNN， Fast R-CNN，Faster R-CNN， Mask R-CNN）然后到了五一，赶大作业赶了五天，又推翻了原来的方案，用qt+cmake整了个烂活出来。接下来两周准备了一下学校的特色体测（总算及格了，然后得准备前面三个月学习内容的总结汇报，算是论文串讲（？。上周去考了市里的数学竞赛（被数分题击碎了，接下来可能就要开始复习期末了。</p><h2 id="接下来的一点想法"><a href="#接下来的一点想法" class="headerlink" title="接下来的一点想法"></a>接下来的一点想法</h2><p>坦白说大一并没有学到什么，不过大致知道了如何学习，逐渐想明白了自己要做什么。<br>因为前面一年基本没有沉下心来好好学一些东西，缺少了沉淀，没有持续学习新知识带来的愉悦感，决定暑假开始接触一下科研。也算写了一些代码了，我发现在做一个项目的时候，并不一定要求每方面的知识都要完全掌握，更多都是边学边做，遂打算多读一些CV领域的论文，看看能不能联系老师去实验室实习。为了坚持下来，我决定用输出倒逼输入，以后应该会慢慢开始在知乎分享一下学习感悟。</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>程设一些知识点</title>
    <link href="/2025/05/24/%E7%A8%8B%E8%AE%BE%E4%B8%80%E4%BA%9B%E7%9F%A5%E8%AF%86%E7%82%B9/"/>
    <url>/2025/05/24/%E7%A8%8B%E8%AE%BE%E4%B8%80%E4%BA%9B%E7%9F%A5%E8%AF%86%E7%82%B9/</url>
    
    <content type="html"><![CDATA[<h1 id="程设一些知识点"><a href="#程设一些知识点" class="headerlink" title="程设一些知识点"></a>程设一些知识点</h1><p>知识点</p><p>本文档主要整理了我在复习中遇到比较不熟悉的几个知识点，包括运算符重载、虚函数、模板以及智能指针的使用。</p><h2 id="1-运算符重载"><a href="#1-运算符重载" class="headerlink" title="1. 运算符重载"></a>1. 运算符重载</h2><p>运算符重载允许我们为自定义类型（如类）重新定义或“重载”已有的运算符行为。</p><h3 id="1-1-输入-输出运算符重载-以-complex-类为例"><a href="#1-1-输入-输出运算符重载-以-complex-类为例" class="headerlink" title="1.1. 输入&#x2F;输出运算符重载 (以 complex 类为例)"></a>1.1. 输入&#x2F;输出运算符重载 (以 <code>complex</code> 类为例)</h3><p>在 C++ 中，<code>&lt;&lt;</code> 和 <code>&gt;&gt;</code> 运算符通常分别用于输出和输入。我们可以为自定义类重载这些运算符，以便能像内置类型一样方便地进行输入输出操作。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;iostream&gt;</span> <span class="hljs-comment">// 需要包含 iostream 以使用 cin 和 cout</span></span><br><br><span class="hljs-comment">// 为了代码片段能独立编译，我们添加 using namespace std;</span><br><span class="hljs-comment">// 在大型项目中，更推荐显式使用 std::cin, std::cout 等</span><br><span class="hljs-keyword">using</span> <span class="hljs-keyword">namespace</span> std;<br><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">complex</span> &#123;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-type">double</span> r, i; <span class="hljs-comment">// 实部和虚部</span><br><br>    <span class="hljs-comment">// 构造函数 (为了示例完整性，添加一个默认构造函数和带参构造函数)</span><br>    <span class="hljs-built_in">complex</span>(<span class="hljs-type">double</span> r_val = <span class="hljs-number">0.0</span>, <span class="hljs-type">double</span> i_val = <span class="hljs-number">0.0</span>) : <span class="hljs-built_in">r</span>(r_val), <span class="hljs-built_in">i</span>(i_val) &#123;&#125;<br><br>    <span class="hljs-comment">// 拷贝构造函数 (用户提供的版本)</span><br>    <span class="hljs-comment">// 注意：通常如果类中没有动态分配的资源，编译器生成的默认拷贝构造函数就足够了。</span><br>    <span class="hljs-comment">// 这里的实现是空的，仅为展示。在实际应用中，如果需要自定义，则应正确拷贝成员。</span><br>    <span class="hljs-built_in">complex</span>(<span class="hljs-type">const</span> complex &amp;c) &#123;<br>        r = c.r;<br>        i = c.i;<br>        <span class="hljs-comment">// cout &lt;&lt; &quot;拷贝构造函数被调用&quot; &lt;&lt; endl; // 用于观察</span><br>    &#125;<br><br>    <span class="hljs-comment">// 友元函数重载 &gt;&gt; (输入运算符)</span><br>    <span class="hljs-comment">// istream&amp; 表示输入流对象的引用，使其可以链式操作 (cin &gt;&gt; a &gt;&gt; b)</span><br>    <span class="hljs-comment">// complex&amp; com 表示要读取数据的 complex 对象的引用</span><br>    <span class="hljs-keyword">friend</span> istream &amp;<span class="hljs-keyword">operator</span>&gt;&gt;(istream &amp;in, complex &amp;com) &#123;<br>        <span class="hljs-comment">// 提示用户输入</span><br>        <span class="hljs-comment">// cout &lt;&lt; &quot;请输入复数的实部和虚部 (以空格分隔): &quot;; // 实际应用中可加提示</span><br>        in &gt;&gt; com.r &gt;&gt; com.i; <span class="hljs-comment">// 从输入流中读取实部和虚部</span><br>        <span class="hljs-keyword">return</span> in; <span class="hljs-comment">// 返回输入流对象，支持链式输入</span><br>    &#125;<br><br>    <span class="hljs-comment">// 友元函数重载 &lt;&lt; (输出运算符)</span><br>    <span class="hljs-comment">// ostream&amp; 表示输出流对象的引用</span><br>    <span class="hljs-comment">// const complex&amp; com 表示要输出的 complex 对象的常量引用 (因为输出操作不应修改对象)</span><br>    <span class="hljs-keyword">friend</span> ostream &amp;<span class="hljs-keyword">operator</span>&lt;&lt;(ostream &amp;out, <span class="hljs-type">const</span> complex &amp;com) &#123;<br>        out &lt;&lt; com.r &lt;&lt; <span class="hljs-string">&quot; + &quot;</span> &lt;&lt; com.i &lt;&lt; <span class="hljs-string">&quot;i&quot;</span>; <span class="hljs-comment">// 按 &quot;实部 + 虚部i&quot; 格式输出</span><br>        <span class="hljs-comment">// 注意：原代码是 cout &lt;&lt; com.r &lt;&lt; &#x27; &#x27; &lt;&lt; com.i; 为了更像复数，这里做了修改</span><br>        <span class="hljs-keyword">return</span> out; <span class="hljs-comment">// 返回输出流对象，支持链式输出</span><br>    &#125;<br>&#125;;<br><br><span class="hljs-comment">// 示例用法：</span><br><span class="hljs-comment">// int main() &#123;</span><br><span class="hljs-comment">//     complex c1;</span><br><span class="hljs-comment">//     cout &lt;&lt; &quot;请输入一个复数 (实部 虚部): &quot;;</span><br><span class="hljs-comment">//     cin &gt;&gt; c1; // 调用 operator&gt;&gt;(cin, c1)</span><br><span class="hljs-comment">//     cout &lt;&lt; &quot;输入的复数是: &quot; &lt;&lt; c1 &lt;&lt; endl; // 调用 operator&lt;&lt;(cout, c1)</span><br><span class="hljs-comment">//</span><br><span class="hljs-comment">//     complex c2 = c1; // 调用拷贝构造函数</span><br><span class="hljs-comment">//     cout &lt;&lt; &quot;拷贝后的复数是: &quot; &lt;&lt; c2 &lt;&lt; endl;</span><br><span class="hljs-comment">//     return 0;</span><br><span class="hljs-comment">// &#125;</span><br></code></pre></td></tr></table></figure><p><strong>解释:</strong></p><ul><li><code>friend</code> 关键字：允许非成员函数访问类的 <code>private</code> 和 <code>protected</code> 成员。对于 <code>&lt;&lt;</code> 和 <code>&gt;&gt;</code> 重载，通常将它们声明为友元函数，因为它们的左操作数是流对象（<code>istream</code> 或 <code>ostream</code>），而不是类对象。</li><li><code>istream &amp;operator&gt;&gt;(istream &amp;in, complex &amp;com)</code>：<ul><li>参数 <code>istream &amp;in</code>: 输入流的引用 (例如 <code>cin</code>)。</li><li>参数 <code>complex &amp;com</code>: 要被赋值的 <code>complex</code> 对象的引用。</li><li>返回值 <code>istream &amp;</code>: 返回输入流的引用，以支持链式输入 (如 <code>cin &gt;&gt; c1 &gt;&gt; c2;</code>)。</li></ul></li><li><code>ostream &amp;operator&lt;&lt;(ostream &amp;out, const complex &amp;com)</code>：<ul><li>参数 <code>ostream &amp;out</code>: 输出流的引用 (例如 <code>cout</code>)。</li><li>参数 <code>const complex &amp;com</code>: 要输出的 <code>complex</code> 对象的常量引用（输出操作不应修改对象）。</li><li>返回值 <code>ostream &amp;</code>: 返回输出流的引用，以支持链式输出 (如 <code>cout &lt;&lt; c1 &lt;&lt; c2;</code>)。</li></ul></li></ul><h3 id="1-2-前置与后置自增-自减运算符重载"><a href="#1-2-前置与后置自增-自减运算符重载" class="headerlink" title="1.2. 前置与后置自增&#x2F;自减运算符重载"></a>1.2. 前置与后置自增&#x2F;自减运算符重载</h3><p>自增 (<code>++</code>) 和自减 (<code>--</code>) 运算符有前置和后置两种形式。它们的重载方式略有不同。</p><h4 id="1-2-1-成员函数方式"><a href="#1-2-1-成员函数方式" class="headerlink" title="1.2.1. 成员函数方式"></a>1.2.1. 成员函数方式</h4><ul><li><p><strong>前置运算符 (<code>++obj</code> 或 <code>--obj</code>)</strong>:</p><ul><li>声明: <code>T&amp; operator++();</code> 或 <code>T&amp; operator--();</code></li><li>返回类型通常是被操作对象的引用 (<code>T&amp;</code>)。</li><li>实现时，先修改对象的值，然后返回修改后的对象。</li></ul></li><li><p><strong>后置运算符 (<code>obj++</code> 或 <code>obj--</code>)</strong>:</p><ul><li>声明: <code>T operator++(int);</code> 或 <code>T operator--(int);</code></li><li>参数列表中的 <code>int</code> 是一个哑元（dummy argument），仅用于区分前置和后置版本，调用时不需要传递实参。</li><li>返回类型通常是被操作对象的值 (<code>T</code>)，而不是引用。这是因为后置操作符需要返回对象在自增&#x2F;自减前的状态。</li><li>实现时，先保存对象的当前状态（创建一个临时副本），然后修改原始对象的值，最后返回保存的临时副本。</li></ul></li></ul><h4 id="1-2-2-全局-友元-函数方式"><a href="#1-2-2-全局-友元-函数方式" class="headerlink" title="1.2.2. 全局 (友元) 函数方式"></a>1.2.2. 全局 (友元) 函数方式</h4><ul><li><p><strong>前置运算符 (<code>++obj</code> 或 <code>--obj</code>)</strong>:</p><ul><li>声明: <code>T1&amp; operator++(T2&amp; obj);</code> 或 <code>T1&amp; operator--(T2&amp; obj);</code> (其中 <code>T1</code> 通常是 <code>T2</code> 或其引用)</li><li>第一个参数是被操作对象的引用。</li></ul></li><li><p><strong>后置运算符 (<code>obj++</code> 或 <code>obj--</code>)</strong>:</p><ul><li>声明: <code>T1 operator++(T2&amp; obj, int);</code> 或 <code>T1 operator--(T2&amp; obj, int);</code> (其中 <code>T1</code> 通常是 <code>T2</code>)</li><li>第一个参数是被操作对象的引用，第二个参数是哑元 <code>int</code>。</li></ul></li></ul><h4 id="1-2-3-Counter-类示例-全局函数方式"><a href="#1-2-3-Counter-类示例-全局函数方式" class="headerlink" title="1.2.3. Counter 类示例 (全局函数方式)"></a>1.2.3. <code>Counter</code> 类示例 (全局函数方式)</h4><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-comment">// 假设 Counter 类的定义</span><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Counter</span> &#123;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-type">int</span> value;<br><br>    <span class="hljs-built_in">Counter</span>(<span class="hljs-type">int</span> v = <span class="hljs-number">0</span>) : <span class="hljs-built_in">value</span>(v) &#123;&#125;<br><br>    <span class="hljs-comment">// 声明友元函数以便它们可以访问 Counter 的私有成员 (如果 value 是 private)</span><br>    <span class="hljs-comment">// 如果 value 是 public，则友元声明不是严格必需的，但通常是良好实践，</span><br>    <span class="hljs-comment">// 因为运算符重载通常与类的接口紧密相关。</span><br>    <span class="hljs-keyword">friend</span> Counter&amp; <span class="hljs-keyword">operator</span>++(Counter&amp; c);     <span class="hljs-comment">// 前置++</span><br>    <span class="hljs-keyword">friend</span> Counter <span class="hljs-keyword">operator</span>++(Counter&amp; c, <span class="hljs-type">int</span>); <span class="hljs-comment">// 后置++</span><br><br>    <span class="hljs-comment">// 为了方便演示，添加一个输出方法</span><br>    <span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">print</span><span class="hljs-params">()</span> <span class="hljs-type">const</span> </span>&#123;<br>        cout &lt;&lt; <span class="hljs-string">&quot;Counter value: &quot;</span> &lt;&lt; value &lt;&lt; endl;<br>    &#125;<br>&#125;;<br><br><span class="hljs-comment">// 全局函数实现前置 ++</span><br>Counter&amp; <span class="hljs-keyword">operator</span>++(Counter&amp; c) &#123;<br>    ++c.value; <span class="hljs-comment">// 修改对象的值</span><br>    <span class="hljs-keyword">return</span> c;  <span class="hljs-comment">// 返回修改后的对象</span><br>&#125;<br><br><span class="hljs-comment">// 全局函数实现后置 ++</span><br>Counter <span class="hljs-keyword">operator</span>++(Counter&amp; c, <span class="hljs-type">int</span>) &#123;<br>    Counter temp = c; <span class="hljs-comment">// 保存原始值的副本</span><br>    ++c.value;        <span class="hljs-comment">// 修改原始对象的值 (也可以调用前置版本: ++c;)</span><br>    <span class="hljs-keyword">return</span> temp;      <span class="hljs-comment">// 返回原始值的副本</span><br>&#125;<br><br><span class="hljs-comment">// 示例用法：</span><br><span class="hljs-comment">// int main() &#123;</span><br><span class="hljs-comment">//     Counter c1(5);</span><br><span class="hljs-comment">//     cout &lt;&lt; &quot;初始 &quot;; c1.print();</span><br><span class="hljs-comment">//</span><br><span class="hljs-comment">//     Counter c2 = ++c1; // 前置自增</span><br><span class="hljs-comment">//     cout &lt;&lt; &quot;++c1 后: &quot; &lt;&lt; endl;</span><br><span class="hljs-comment">//     cout &lt;&lt; &quot;c1: &quot;; c1.print();</span><br><span class="hljs-comment">//     cout &lt;&lt; &quot;c2: &quot;; c2.print();</span><br><span class="hljs-comment">//</span><br><span class="hljs-comment">//     Counter c3(10);</span><br><span class="hljs-comment">//     cout &lt;&lt; &quot;初始 &quot;; c3.print();</span><br><span class="hljs-comment">//     Counter c4 = c3++; // 后置自增</span><br><span class="hljs-comment">//     cout &lt;&lt; &quot;c3++ 后: &quot; &lt;&lt; endl;</span><br><span class="hljs-comment">//     cout &lt;&lt; &quot;c3: &quot;; c3.print();</span><br><span class="hljs-comment">//     cout &lt;&lt; &quot;c4: &quot;; c4.print();</span><br><span class="hljs-comment">//</span><br><span class="hljs-comment">//     return 0;</span><br><span class="hljs-comment">// &#125;</span><br></code></pre></td></tr></table></figure><p><strong>要点:</strong></p><ul><li>后置版本通过一个未命名的 <code>int</code> 参数与前置版本区分。</li><li>前置版本通常返回对象的引用 (<code>T&amp;</code>)，而后置版本通常按值返回 (<code>T</code>)，因为它返回的是操作前的对象状态。</li></ul><h2 id="2-虚函数-Virtual-Functions"><a href="#2-虚函数-Virtual-Functions" class="headerlink" title="2. 虚函数 (Virtual Functions)"></a>2. 虚函数 (Virtual Functions)</h2><p>虚函数是实现 C++ 运行时多态性的关键机制。当通过基类指针或引用调用派生类中的虚函数时，会根据指针或引用实际指向的对象类型来决定调用哪个版本的函数。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Base</span> &#123;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-comment">// 虚析构函数</span><br>    <span class="hljs-comment">// 如果一个类要作为基类，并且可能会通过基类指针删除派生类对象，</span><br>    <span class="hljs-comment">// 那么它的析构函数应该声明为 virtual。</span><br>    <span class="hljs-comment">// 这样可以确保在删除派生类对象时，先调用派生类的析构函数，然后再调用基类的析构函数。</span><br>    <span class="hljs-comment">// = default; 表示使用编译器生成的默认实现。</span><br>    <span class="hljs-keyword">virtual</span> ~<span class="hljs-built_in">Base</span>() = <span class="hljs-keyword">default</span>;<br><br>    <span class="hljs-comment">// 纯虚函数 (Pure Virtual Function)</span><br>    <span class="hljs-comment">// 纯虚函数在基类中没有实现，它强制派生类必须提供该函数的具体实现。</span><br>    <span class="hljs-comment">// 包含纯虚函数的类是抽象类，不能被实例化。</span><br>    <span class="hljs-comment">// const 表示该成员函数不会修改类的成员变量。</span><br>    <span class="hljs-comment">// = 0; 表明这是一个纯虚函数。</span><br>    <span class="hljs-function"><span class="hljs-keyword">virtual</span> <span class="hljs-type">int</span> <span class="hljs-title">function</span><span class="hljs-params">(<span class="hljs-type">int</span> x)</span> <span class="hljs-type">const</span> </span>= <span class="hljs-number">0</span>;<br><br>    <span class="hljs-comment">// 普通虚函数 (示例)</span><br>    <span class="hljs-function"><span class="hljs-keyword">virtual</span> <span class="hljs-type">void</span> <span class="hljs-title">printType</span><span class="hljs-params">()</span> </span>&#123;<br>        cout &lt;&lt; <span class="hljs-string">&quot;This is Base class&quot;</span> &lt;&lt; endl;<br>    &#125;<br>&#125;;<br><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Derived</span> : <span class="hljs-keyword">public</span> Base &#123;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-comment">// 覆盖 (override) 基类的虚析构函数</span><br>    <span class="hljs-comment">// ~Derived() override &#123; cout &lt;&lt; &quot;Derived destructor called&quot; &lt;&lt; endl; &#125; // C++11 override 关键字</span><br>    ~<span class="hljs-built_in">Derived</span>() &#123; <span class="hljs-comment">/* cout &lt;&lt; &quot;Derived destructor called&quot; &lt;&lt; endl; */</span> &#125; <span class="hljs-comment">// 也可以不加 override</span><br><br>    <span class="hljs-comment">// 实现基类中的纯虚函数</span><br>    <span class="hljs-comment">// C++11 中推荐使用 override 关键字明确表示这是对基类虚函数的覆盖</span><br>    <span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">function</span><span class="hljs-params">(<span class="hljs-type">int</span> x)</span> <span class="hljs-type">const</span> <span class="hljs-keyword">override</span> </span>&#123;<br>        <span class="hljs-keyword">return</span> x * x;<br>    &#125;<br><br>    <span class="hljs-comment">// 覆盖基类的普通虚函数</span><br>    <span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">printType</span><span class="hljs-params">()</span> <span class="hljs-keyword">override</span> </span>&#123;<br>        cout &lt;&lt; <span class="hljs-string">&quot;This is Derived class&quot;</span> &lt;&lt; endl;<br>    &#125;<br>&#125;;<br><br><span class="hljs-comment">// 示例用法：</span><br><span class="hljs-comment">// int main() &#123;</span><br><span class="hljs-comment">//     // Base b; // 错误！Base 是抽象类，不能实例化</span><br><span class="hljs-comment">//</span><br><span class="hljs-comment">//     Derived d;</span><br><span class="hljs-comment">//     cout &lt;&lt; &quot;d.function(5): &quot; &lt;&lt; d.function(5) &lt;&lt; endl; // 输出 25</span><br><span class="hljs-comment">//</span><br><span class="hljs-comment">//     Base* ptr_b = &amp;d; // 基类指针指向派生类对象</span><br><span class="hljs-comment">//     cout &lt;&lt; &quot;ptr_b-&gt;function(10): &quot; &lt;&lt; ptr_b-&gt;function(10) &lt;&lt; endl; // 输出 100 (调用 Derived::function)</span><br><span class="hljs-comment">//     ptr_b-&gt;printType(); // 输出 &quot;This is Derived class&quot; (调用 Derived::printType)</span><br><span class="hljs-comment">//</span><br><span class="hljs-comment">//     // 如果 Base 的析构函数不是 virtual，通过基类指针 delete 派生类对象可能导致资源泄漏</span><br><span class="hljs-comment">//     // Base* ptr_heap = new Derived();</span><br><span class="hljs-comment">//     // delete ptr_heap; // 正确调用 Derived 和 Base 的析构函数</span><br><span class="hljs-comment">//</span><br><span class="hljs-comment">//     return 0;</span><br><span class="hljs-comment">// &#125;</span><br></code></pre></td></tr></table></figure><p><strong>关键点:</strong></p><ul><li><code>virtual ~Base() = default;</code>: 虚析构函数。如果类可能被继承，并且可能通过基类指针删除派生类对象，则析构函数应声明为 <code>virtual</code>。这确保了派生类的析构函数会被正确调用，防止资源泄漏。<code>= default</code> 表示使用编译器提供的默认实现。</li><li><code>virtual int function(int x) const = 0;</code>: 纯虚函数。<ul><li><code>virtual</code>: 表明它是一个虚函数。</li><li><code>const</code>: 表明该函数不会修改类的成员变量（对于 <code>const</code> 对象或通过 <code>const</code> 引用&#x2F;指针调用）。</li><li><code>= 0</code>: 将其声明为纯虚函数。纯虚函数在声明它的类中没有定义（实现）。</li><li>包含一个或多个纯虚函数的类称为抽象类。抽象类不能被实例化（不能创建其对象）。</li><li>派生类必须实现（覆盖）基类中的所有纯虚函数，否则派生类也将成为抽象类。</li></ul></li></ul><h2 id="3-类模板-Class-Templates"><a href="#3-类模板-Class-Templates" class="headerlink" title="3. 类模板 (Class Templates)"></a>3. 类模板 (Class Templates)</h2><p>类模板允许我们定义一个通用的类蓝图，其中的数据类型或某些值可以作为参数在实例化时指定。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;stdexcept&gt;</span> <span class="hljs-comment">// 为了使用 std::out_of_range</span></span><br><br><span class="hljs-keyword">template</span> &lt;<span class="hljs-keyword">typename</span> T, <span class="hljs-type">int</span> i&gt; <span class="hljs-comment">// T 是类型参数，i 是非类型参数 (整数常量)</span><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">TestClass</span> &#123;<br><span class="hljs-keyword">public</span>:<br>    T buffer[i]; <span class="hljs-comment">// T 类型的数组，其大小由非类型参数 i 决定</span><br><br>    <span class="hljs-comment">// 构造函数 (示例)</span><br>    <span class="hljs-built_in">TestClass</span>() &#123;<br>        <span class="hljs-comment">// 可以在这里初始化 buffer，例如：</span><br>        <span class="hljs-comment">// for(int k = 0; k &lt; i; ++k) &#123;</span><br>        <span class="hljs-comment">//     buffer[k] = T(); // 使用 T 类型的默认构造函数初始化</span><br>        <span class="hljs-comment">// &#125;</span><br>    &#125;<br><br>    <span class="hljs-comment">// 成员函数声明</span><br>    <span class="hljs-function">T <span class="hljs-title">getData</span><span class="hljs-params">(<span class="hljs-type">int</span> j)</span></span>;<br><br>    <span class="hljs-comment">// 另一个成员函数示例</span><br>    <span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">setData</span><span class="hljs-params">(<span class="hljs-type">int</span> j, T value)</span> </span>&#123;<br>        <span class="hljs-keyword">if</span> (j &gt;= <span class="hljs-number">0</span> &amp;&amp; j &lt; i) &#123;<br>            buffer[j] = value;<br>        &#125; <span class="hljs-keyword">else</span> &#123;<br>            <span class="hljs-comment">// 处理越界，例如抛出异常</span><br>            <span class="hljs-keyword">throw</span> std::<span class="hljs-built_in">out_of_range</span>(<span class="hljs-string">&quot;Index out of bounds in setData&quot;</span>);<br>        &#125;<br>    &#125;<br><br>    <span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">getSize</span><span class="hljs-params">()</span> <span class="hljs-type">const</span> </span>&#123;<br>        <span class="hljs-keyword">return</span> i;<br>    &#125;<br>&#125;;<br><br><span class="hljs-comment">// 类模板的成员函数定义在类外时，需要再次声明模板参数列表</span><br><span class="hljs-keyword">template</span> &lt;<span class="hljs-keyword">typename</span> T, <span class="hljs-type">int</span> i&gt;<br>T TestClass&lt;T, i&gt;::<span class="hljs-built_in">getData</span>(<span class="hljs-type">int</span> j) &#123;<br>    <span class="hljs-keyword">if</span> (j &gt;= <span class="hljs-number">0</span> &amp;&amp; j &lt; i) &#123;<br>        <span class="hljs-keyword">return</span> *(buffer + j); <span class="hljs-comment">// 等价于 buffer[j]</span><br>    &#125; <span class="hljs-keyword">else</span> &#123;<br>        <span class="hljs-comment">// 处理越界，例如抛出异常或返回默认值</span><br>        <span class="hljs-keyword">throw</span> std::<span class="hljs-built_in">out_of_range</span>(<span class="hljs-string">&quot;Index out of bounds in getData&quot;</span>);<br>        <span class="hljs-comment">// 或者 return T(); // 返回 T 类型的默认构造值</span><br>    &#125;<br>&#125;<br><br><span class="hljs-comment">// 示例用法：</span><br><span class="hljs-comment">// int main() &#123;</span><br><span class="hljs-comment">//     TestClass&lt;int, 10&gt; intTest; // T 为 int, i 为 10</span><br><span class="hljs-comment">//     intTest.setData(0, 100);</span><br><span class="hljs-comment">//     intTest.setData(5, 500);</span><br><span class="hljs-comment">//</span><br><span class="hljs-comment">//     cout &lt;&lt; &quot;intTest.getData(0): &quot; &lt;&lt; intTest.getData(0) &lt;&lt; endl; // 输出 100</span><br><span class="hljs-comment">//     cout &lt;&lt; &quot;intTest.getData(5): &quot; &lt;&lt; intTest.getData(5) &lt;&lt; endl; // 输出 500</span><br><span class="hljs-comment">//     cout &lt;&lt; &quot;Size of intTest buffer: &quot; &lt;&lt; intTest.getSize() &lt;&lt; endl; // 输出 10</span><br><span class="hljs-comment">//</span><br><span class="hljs-comment">//     TestClass&lt;double, 5&gt; doubleTest; // T 为 double, i 为 5</span><br><span class="hljs-comment">//     doubleTest.setData(2, 3.14);</span><br><span class="hljs-comment">//     cout &lt;&lt; &quot;doubleTest.getData(2): &quot; &lt;&lt; doubleTest.getData(2) &lt;&lt; endl; // 输出 3.14</span><br><span class="hljs-comment">//</span><br><span class="hljs-comment">//     // TestClass&lt;int, 0&gt; zeroSizeTest; // 通常 i 必须大于 0，否则 buffer[0] 是非法的</span><br><span class="hljs-comment">//</span><br><span class="hljs-comment">//     try &#123;</span><br><span class="hljs-comment">//         cout &lt;&lt; intTest.getData(10) &lt;&lt; endl; // 会抛出异常</span><br><span class="hljs-comment">//     &#125; catch (const std::out_of_range&amp; e) &#123;</span><br><span class="hljs-comment">//         cerr &lt;&lt; &quot;Exception: &quot; &lt;&lt; e.what() &lt;&lt; endl;</span><br><span class="hljs-comment">//     &#125;</span><br><span class="hljs-comment">//</span><br><span class="hljs-comment">//     return 0;</span><br><span class="hljs-comment">// &#125;</span><br></code></pre></td></tr></table></figure><p><strong>解释:</strong></p><ul><li><code>template &lt;typename T, int i&gt;</code>: 模板声明。<ul><li><code>typename T</code>: <code>T</code> 是一个类型参数，代表任何数据类型（如 <code>int</code>, <code>double</code>, <code>std::string</code>, 自定义类等）。<code>class</code> 也可以用来代替 <code>typename</code>。</li><li><code>int i</code>: <code>i</code> 是一个非类型模板参数。它必须是一个编译时常量（如整型、指针、引用，或枚举值）。在这里，它决定了 <code>buffer</code> 数组的大小。</li></ul></li><li><code>T buffer[i];</code>: 类成员，一个类型为 <code>T</code>，大小为 <code>i</code> 的数组。</li><li><code>T TestClass&lt;T,i&gt;::getData(int j)</code>: 当在类模板外部定义成员函数时，必须重复模板参数列表，并使用 <code>TestClass&lt;T,i&gt;</code> 来限定函数所属的类。</li></ul><h2 id="4-函数模板与智能指针"><a href="#4-函数模板与智能指针" class="headerlink" title="4. 函数模板与智能指针"></a>4. 函数模板与智能指针</h2><p>函数模板允许我们编写一个通用的函数，它可以处理不同类型的数据。智能指针是管理动态分配内存的类，有助于防止内存泄漏。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;memory&gt;</span> <span class="hljs-comment">// 为了使用 std::unique_ptr</span></span><br><br><span class="hljs-comment">// 假设 GtLever 函数的声明 (具体实现未知，仅为示例)</span><br><span class="hljs-comment">// 它接收一个 T 类型的数组，数组长度 n，以及一个 T 类型的 lever 值</span><br><span class="hljs-keyword">template</span> &lt;<span class="hljs-keyword">class</span> <span class="hljs-title class_">T</span>&gt;<br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">GtLever</span><span class="hljs-params">(<span class="hljs-type">const</span> T* arr, <span class="hljs-type">int</span> n, T lever)</span> </span>&#123;<br>    cout &lt;&lt; <span class="hljs-string">&quot;GtLever called with lever: &quot;</span> &lt;&lt; lever &lt;&lt; <span class="hljs-string">&quot; and array: &quot;</span>;<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; n; ++i) &#123;<br>        cout &lt;&lt; arr[i] &lt;&lt; (i == n - <span class="hljs-number">1</span> ? <span class="hljs-string">&quot;&quot;</span> : <span class="hljs-string">&quot;, &quot;</span>);<br>    &#125;<br>    cout &lt;&lt; endl;<br>    <span class="hljs-comment">// 实际的 GtLever 逻辑会在这里</span><br>&#125;<br><br><br><span class="hljs-comment">// 处理输入和调用 GtLever 的函数模板</span><br><span class="hljs-keyword">template</span> &lt;<span class="hljs-keyword">class</span> <span class="hljs-title class_">T</span>&gt; <span class="hljs-comment">// T 是类型参数</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">processInput</span><span class="hljs-params">(<span class="hljs-type">int</span> length, <span class="hljs-type">int</span> n_for_gtlever)</span> </span>&#123; <span class="hljs-comment">// 修改了第二个参数名以示区分</span><br>    <span class="hljs-comment">// 使用智能指针 std::unique_ptr 管理动态分配的数组内存</span><br>    <span class="hljs-comment">// std::unique_ptr&lt;T[]&gt; arr(new T[length]);</span><br>    <span class="hljs-comment">// C++14 及以后版本推荐使用 std::make_unique</span><br>    std::unique_ptr&lt;T[]&gt; arr = std::<span class="hljs-built_in">make_unique</span>&lt;T[]&gt;(length);<br><br>    cout &lt;&lt; <span class="hljs-string">&quot;Enter &quot;</span> &lt;&lt; length &lt;&lt; <span class="hljs-string">&quot; elements of type &quot;</span> &lt;&lt; <span class="hljs-built_in">typeid</span>(T).<span class="hljs-built_in">name</span>() &lt;&lt; <span class="hljs-string">&quot;:&quot;</span> &lt;&lt; endl;<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; length; i++) &#123;<br>        cin &gt;&gt; arr[i];<br>    &#125;<br><br>    T lever;<br>    cout &lt;&lt; <span class="hljs-string">&quot;Enter the lever value of type &quot;</span> &lt;&lt; <span class="hljs-built_in">typeid</span>(T).<span class="hljs-built_in">name</span>() &lt;&lt; <span class="hljs-string">&quot;:&quot;</span> &lt;&lt; endl;<br>    cin &gt;&gt; lever;<br><br>    <span class="hljs-comment">// 调用 GtLever 函数</span><br>    <span class="hljs-comment">// arr.get() 返回指向 unique_ptr所管理数组的原始指针</span><br>    <span class="hljs-built_in">GtLever</span>(arr.<span class="hljs-built_in">get</span>(), n_for_gtlever, lever); <span class="hljs-comment">// 假设 GtLever 需要的长度是 n_for_gtlever</span><br>                                             <span class="hljs-comment">// 如果 GtLever 处理整个 arr，则应传递 length</span><br>&#125;<br><br><span class="hljs-comment">// 示例用法 (构造方法调用)：</span><br><span class="hljs-comment">// int main() &#123;</span><br><span class="hljs-comment">//     int length, n;</span><br><span class="hljs-comment">//</span><br><span class="hljs-comment">//     cout &lt;&lt; &quot;Processing char input:&quot; &lt;&lt; endl;</span><br><span class="hljs-comment">//     cout &lt;&lt; &quot;Enter array length: &quot;;</span><br><span class="hljs-comment">//     cin &gt;&gt; length;</span><br><span class="hljs-comment">//     cout &lt;&lt; &quot;Enter n for GtLever: &quot;;</span><br><span class="hljs-comment">//     cin &gt;&gt; n;</span><br><span class="hljs-comment">//     processInput&lt;char&gt;(length, n); // 实例化函数模板，T 为 char</span><br><span class="hljs-comment">//</span><br><span class="hljs-comment">//     cout &lt;&lt; &quot;\nProcessing int input:&quot; &lt;&lt; endl;</span><br><span class="hljs-comment">//     cout &lt;&lt; &quot;Enter array length: &quot;;</span><br><span class="hljs-comment">//     cin &gt;&gt; length;</span><br><span class="hljs-comment">//     cout &lt;&lt; &quot;Enter n for GtLever: &quot;;</span><br><span class="hljs-comment">//     cin &gt;&gt; n;</span><br><span class="hljs-comment">//     processInput&lt;int&gt;(length, n); // 实例化函数模板，T 为 int</span><br><span class="hljs-comment">//</span><br><span class="hljs-comment">//     return 0;</span><br><span class="hljs-comment">// &#125;</span><br></code></pre></td></tr></table></figure><p><strong>解释:</strong></p><ul><li><code>template &lt;class T&gt;</code>: <code>processInput</code> 是一个函数模板，<code>T</code> 是其类型参数。</li><li><code>std::unique_ptr&lt;T[]&gt; arr(new T[length]);</code> (或 <code>std::make_unique&lt;T[]&gt;(length);</code>):<ul><li><code>std::unique_ptr</code> 是一种智能指针，它拥有其指向的对象。当 <code>unique_ptr</code> 对象本身被销毁时（例如离开作用域），它会自动释放所管理的内存。这有助于防止内存泄漏。</li><li><code>T[]</code> 表示 <code>unique_ptr</code> 管理的是一个 <code>T</code> 类型的数组。</li><li><code>new T[length]</code> 动态分配一个包含 <code>length</code> 个 <code>T</code> 类型元素的数组。</li><li><code>std::make_unique&lt;T[]&gt;(length)</code> (C++14+) 是创建 <code>unique_ptr</code> 的更安全、更简洁的方式。</li></ul></li><li><code>arr.get()</code>: <code>unique_ptr</code> 的 <code>get()</code> 成员函数返回一个指向其所管理内存的原始指针。这在需要将管理的内存传递给不接受 <code>unique_ptr</code> 的旧式 C API 或函数时很有用。</li><li><code>processInput&lt;char&gt;(length, n);</code>: 这是函数模板的显式实例化。编译器会根据指定的类型参数 <code>char</code> 生成一个特定版本的 <code>processInput</code> 函数。</li></ul><h2 id="5-构造函数中使用智能指针初始化成员"><a href="#5-构造函数中使用智能指针初始化成员" class="headerlink" title="5. 构造函数中使用智能指针初始化成员"></a>5. 构造函数中使用智能指针初始化成员</h2><p>在类的构造函数中初始化 <code>std::unique_ptr</code> 成员是常见的做法，以确保资源在对象创建时被正确获取，并在对象销毁时自动释放。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-comment">// 假设 T 是一个已定义的类型或模板参数</span><br><span class="hljs-comment">// template &lt;typename T&gt; // 如果 C1 本身是模板类</span><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">C1</span> &#123;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-type">int</span> num;<br>    std::unique_ptr&lt;<span class="hljs-type">int</span>[]&gt; a; <span class="hljs-comment">// 使用 int[] 作为示例，原代码是 T[]</span><br><br>    <span class="hljs-comment">// 构造函数</span><br>    <span class="hljs-comment">// 使用成员初始化列表来初始化 num 和智能指针 a</span><br>    <span class="hljs-comment">// std::make_unique&lt;T[]&gt;(n) (C++14+) 是推荐的初始化方式</span><br>    <span class="hljs-built_in">C1</span>(<span class="hljs-type">int</span> n) : <span class="hljs-built_in">num</span>(n), <span class="hljs-built_in">a</span>(std::<span class="hljs-built_in">make_unique</span>&lt;<span class="hljs-type">int</span>[]&gt;(n)) &#123;<br>        cout &lt;&lt; <span class="hljs-string">&quot;C1 constructor called. num = &quot;</span> &lt;&lt; num &lt;&lt; <span class="hljs-string">&quot;. Array of size &quot;</span> &lt;&lt; n &lt;&lt; <span class="hljs-string">&quot; allocated.&quot;</span> &lt;&lt; endl;<br>        <span class="hljs-comment">// 可以在这里初始化数组 a 的内容</span><br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; n; ++i) &#123;<br>            a[i] = i * <span class="hljs-number">10</span>; <span class="hljs-comment">// 示例初始化</span><br>        &#125;<br>    &#125;<br><br>    <span class="hljs-comment">// 析构函数 (由 unique_ptr 自动管理内存，通常不需要显式 delete)</span><br>    ~<span class="hljs-built_in">C1</span>() &#123;<br>        cout &lt;&lt; <span class="hljs-string">&quot;C1 destructor called. Memory for array will be automatically deallocated by unique_ptr.&quot;</span> &lt;&lt; endl;<br>    &#125;<br><br>    <span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">printArray</span><span class="hljs-params">()</span> <span class="hljs-type">const</span> </span>&#123;<br>        <span class="hljs-keyword">if</span> (num &gt; <span class="hljs-number">0</span> &amp;&amp; a) &#123;<br>            cout &lt;&lt; <span class="hljs-string">&quot;Array contents: &quot;</span>;<br>            <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; num; ++i) &#123;<br>                cout &lt;&lt; a[i] &lt;&lt; (i == num - <span class="hljs-number">1</span> ? <span class="hljs-string">&quot;&quot;</span> : <span class="hljs-string">&quot;, &quot;</span>);<br>            &#125;<br>            cout &lt;&lt; endl;<br>        &#125; <span class="hljs-keyword">else</span> &#123;<br>            cout &lt;&lt; <span class="hljs-string">&quot;Array is empty or not allocated.&quot;</span> &lt;&lt; endl;<br>        &#125;<br>    &#125;<br>&#125;;<br><br><span class="hljs-comment">// 示例用法：</span><br><span class="hljs-comment">// int main() &#123;</span><br><span class="hljs-comment">//     C1 obj1(5);</span><br><span class="hljs-comment">//     obj1.printArray();</span><br><span class="hljs-comment">//</span><br><span class="hljs-comment">//     // 当 obj1 离开作用域时，其析构函数被调用，</span><br><span class="hljs-comment">//     // unique_ptr a 会自动释放其管理的动态数组内存。</span><br><span class="hljs-comment">//</span><br><span class="hljs-comment">//     return 0;</span><br><span class="hljs-comment">// &#125;</span><br></code></pre></td></tr></table></figure><p><strong>解释:</strong></p><ul><li><code>std::unique_ptr&lt;T[]&gt; a;</code>: 类 <code>C1</code> 有一个名为 <code>a</code> 的成员，它是一个指向 <code>T</code> 类型数组的 <code>unique_ptr</code>。</li><li><code>C1(int n) : num(n), a(std::make_unique&lt;T[]&gt;(n)) &#123;&#125;</code>:<ul><li>这是构造函数。</li><li><code>:</code> num(n),<code> a(std::make_unique&lt;T[]&gt;(n))</code> 是成员初始化列表。这是初始化类成员（尤其是 <code>const</code> 成员、引用成员和需要构造函数参数的成员对象）的首选方式。</li><li><code>a(std::make_unique&lt;T[]&gt;(n))</code> 初始化 <code>unique_ptr</code> 成员 <code>a</code>，使其管理一个新分配的大小为 <code>n</code> 的 <code>T</code> 类型数组。</li></ul></li><li>当 <code>C1</code> 类型的对象被销毁时，其成员 <code>a</code> (即 <code>unique_ptr</code>) 也会被销毁。<code>unique_ptr</code> 的析构函数会自动 <code>delete[]</code> 它所管理的数组，从而防止内存泄漏。</li></ul>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>C++程序设计复习笔记</title>
    <link href="/2025/05/24/CPP%E7%A8%8B%E5%BA%8F%E8%AE%BE%E8%AE%A1%E5%A4%8D%E4%B9%A0%E7%AC%94%E8%AE%B0/"/>
    <url>/2025/05/24/CPP%E7%A8%8B%E5%BA%8F%E8%AE%BE%E8%AE%A1%E5%A4%8D%E4%B9%A0%E7%AC%94%E8%AE%B0/</url>
    
    <content type="html"><![CDATA[<h1 id="C-程序设计复习笔记"><a href="#C-程序设计复习笔记" class="headerlink" title="C++程序设计复习笔记"></a>C++程序设计复习笔记</h1><h2 id="第六章-指针、引用与动态内存分配"><a href="#第六章-指针、引用与动态内存分配" class="headerlink" title="第六章 指针、引用与动态内存分配"></a>第六章 指针、引用与动态内存分配</h2><h3 id="一、指针类型与指针变量"><a href="#一、指针类型与指针变量" class="headerlink" title="一、指针类型与指针变量"></a>一、指针类型与指针变量</h3><h4 id="1-指针的基本概念"><a href="#1-指针的基本概念" class="headerlink" title="1. 指针的基本概念"></a>1. 指针的基本概念</h4><ul><li><strong>指针 (Pointer)</strong>：一种复合数据类型，存储的是另一个变量的内存地址。</li><li>通过指针，可以间接访问和修改其指向的内存地址中的数据。</li><li><strong>地址 (Address)</strong>：内存中每个字节的唯一编号。</li></ul><h4 id="2-指针变量"><a href="#2-指针变量" class="headerlink" title="2. 指针变量"></a>2. 指针变量</h4><ul><li><strong>指针变量的说明 (Declaration)</strong>：  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs cpp">&lt;数据类型&gt; *&lt;指针变量名&gt;;<br><span class="hljs-comment">// 例如:</span><br><span class="hljs-type">int</span> *p; <span class="hljs-comment">// p 是一个指向整型变量的指针</span><br><span class="hljs-type">char</span> *name; <span class="hljs-comment">// name 是一个指向字符型变量的指针</span><br></code></pre></td></tr></table></figure><ul><li><code>*</code> 表明该变量是一个指针。</li><li>数据类型指明了指针所指向的变量的类型。</li><li>未初始化的指针处于<strong>悬挂状态 (Dangling Pointer)</strong>，其指向不确定，使用它可能导致程序崩溃。</li></ul></li><li><strong>指针变量的值 (Value)</strong>：<ul><li>指针变量存储的是其所指向的内存单元的<strong>首地址</strong>。</li><li>指针变量自身也占用内存空间（通常为4或8字节，取决于系统架构）。</li></ul></li><li><strong>指针可以指向的成分</strong>：变量、常量、数组、函数、类对象等。</li></ul><h4 id="3-指针类型的初始化与赋值"><a href="#3-指针类型的初始化与赋值" class="headerlink" title="3. 指针类型的初始化与赋值"></a>3. 指针类型的初始化与赋值</h4><ul><li><strong>初始化 (Initialization)</strong>：<ul><li><strong>初始化为内存地址</strong>：使用取地址运算符 <code>&amp;</code> 获取变量地址。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">int</span> a = <span class="hljs-number">18</span>;<br><span class="hljs-type">int</span> *p = &amp;a; <span class="hljs-comment">// 指针p初始化为变量a的地址</span><br></code></pre></td></tr></table></figure></li><li><strong>初始化为 <code>nullptr</code> (C++11及以后)</strong>：表示指针不指向任何有效的内存地址。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">int</span> *p = <span class="hljs-literal">nullptr</span>;<br></code></pre></td></tr></table></figure></li><li><strong>初始化为 <code>0</code> 或 <code>NULL</code> (C风格)</strong>：与 <code>nullptr</code> 类似，表示空指针。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">int</span> *p1 = <span class="hljs-number">0</span>;<br><span class="hljs-type">int</span> *p2 = <span class="hljs-literal">NULL</span>; <span class="hljs-comment">// NULL通常是宏定义 (void*)0</span><br></code></pre></td></tr></table></figure></li></ul></li><li><strong>赋值 (Assignment)</strong>：<ul><li>赋值运算符 <code>=</code> 左边是指针变量，右边是地址表达式。</li><li>地址存放数据的类型必须与指针说明的类型一致。</li><li><strong>不可任意赋一个内存地址常量</strong>，只能赋已分配内存的变量地址或 <code>nullptr</code>。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">int</span> i = <span class="hljs-number">3</span>;<br><span class="hljs-type">int</span> *pi;<br>pi = &amp;i; <span class="hljs-comment">// 正确</span><br><span class="hljs-comment">// pi = 1000; // 错误，不能直接赋地址常量</span><br></code></pre></td></tr></table></figure></li></ul></li></ul><h4 id="4-指针运算"><a href="#4-指针运算" class="headerlink" title="4. 指针运算"></a>4. 指针运算</h4><ul><li><strong>取内容运算符 <code>*</code> (Dereference Operator)</strong>：<ul><li>表达式：<code>*&lt;指针变量名&gt;</code></li><li>作用：访问指针所指向的内存地址中存储的数据。</li><li><code>*p</code> 可以看作一个变量，可以出现在表达式的左边（赋值）或右边（取值）。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">int</span> a = <span class="hljs-number">18</span>, *p;<br>p = &amp;a;<br>cout &lt;&lt; *p; <span class="hljs-comment">// 输出18 (a的值)</span><br>*p = <span class="hljs-number">20</span>;    <span class="hljs-comment">// 等价于 a = 20;</span><br></code></pre></td></tr></table></figure></li></ul></li><li><strong>取地址运算符 <code>&amp;</code> (Address-of Operator)</strong>：<ul><li>表达式：<code>&amp;&lt;可寻址数据名&gt;</code></li><li>作用：获取变量、数组元素、类对象等的内存地址。</li></ul></li><li><strong>指针的算术运算</strong>：主要用于指向数组元素时。<ul><li><code>p + i</code>：指向指针 <code>p</code> 当前所指元素之后第 <code>i</code> 个元素的地址。</li><li><code>p - i</code>：指向指针 <code>p</code> 当前所指元素之前第 <code>i</code> 个元素的地址。</li><li><code>p++</code> 或 <code>++p</code>：指向下一个元素。</li><li><code>p--</code> 或 <code>--p</code>：指向上一个元素。</li><li><code>p2 - p1</code>：若 <code>p1</code> 和 <code>p2</code> 指向同一数组中的元素，结果是它们之间元素的个数。</li><li><strong>注意</strong>：指针加减的单位是其所指向数据类型的大小。例如，<code>int *p; p+1;</code> 地址会增加 <code>sizeof(int)</code> 个字节。</li></ul></li><li><strong>指针的关系运算</strong>：<ul><li>可以比较两个同类型指针是否相等 (<code>==</code>, <code>!=</code>) 或比较它们指向的地址先后 (<code>&lt;</code>, <code>&gt;</code>, <code>&lt;=</code>, <code>&gt;=</code>)。</li><li>常用于判断指针是否为空 (<code>p == nullptr</code>) 或遍历数组。</li></ul></li></ul><h3 id="二、指针与常量"><a href="#二、指针与常量" class="headerlink" title="二、指针与常量"></a>二、指针与常量</h3><h4 id="1-指向常量的指针-Pointer-to-Constant"><a href="#1-指向常量的指针-Pointer-to-Constant" class="headerlink" title="1. 指向常量的指针 (Pointer to Constant)"></a>1. 指向常量的指针 (Pointer to Constant)</h4><ul><li>指针指向的内容不可通过该指针修改，但指针本身可以改变指向。</li><li>声明格式：<code>const &lt;数据类型&gt; *&lt;指针变量名&gt;;</code> 或 <code>&lt;数据类型&gt; const *&lt;指针变量名&gt;;</code>  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">const</span> <span class="hljs-type">int</span> a = <span class="hljs-number">10</span>;<br><span class="hljs-type">const</span> <span class="hljs-type">int</span> *p1 = &amp;a; <span class="hljs-comment">// p1 指向常量a</span><br><span class="hljs-comment">// *p1 = 20; // 错误：不能通过p1修改a的值</span><br><br><span class="hljs-type">int</span> b = <span class="hljs-number">20</span>;<br><span class="hljs-type">const</span> <span class="hljs-type">int</span> *p2 = &amp;b; <span class="hljs-comment">// p2 指向变量b，但视其内容为常量</span><br><span class="hljs-comment">// *p2 = 30; // 错误：不能通过p2修改b的值</span><br>b = <span class="hljs-number">30</span>;     <span class="hljs-comment">// 正确：b本身可以修改</span><br><span class="hljs-type">int</span> c = <span class="hljs-number">40</span>;<br>p2 = &amp;c;    <span class="hljs-comment">// 正确：p2可以指向其他地址</span><br></code></pre></td></tr></table></figure></li><li>常用于函数参数，防止函数内部修改实参。</li></ul><h4 id="2-常量指针-Constant-Pointer"><a href="#2-常量指针-Constant-Pointer" class="headerlink" title="2. 常量指针 (Constant Pointer)"></a>2. 常量指针 (Constant Pointer)</h4><ul><li>指针本身是常量，其指向的地址不可改变，但其指向地址的内容可以通过该指针修改（如果指向的不是常量）。</li><li>声明格式：<code>&lt;数据类型&gt; * const &lt;指针变量名&gt;;</code></li><li><strong>必须在声明时初始化。</strong>  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">int</span> a = <span class="hljs-number">10</span>;<br><span class="hljs-type">int</span> b = <span class="hljs-number">20</span>;<br><span class="hljs-type">int</span> * <span class="hljs-type">const</span> p = &amp;a; <span class="hljs-comment">// p是一个常量指针，初始化后不能再指向其他地址</span><br>*p = <span class="hljs-number">15</span>; <span class="hljs-comment">// 正确：可以修改a的值</span><br><span class="hljs-comment">// p = &amp;b; // 错误：p不能指向其他地址</span><br></code></pre></td></tr></table></figure></li></ul><h4 id="3-指向常量的常量指针-Constant-Pointer-to-Constant"><a href="#3-指向常量的常量指针-Constant-Pointer-to-Constant" class="headerlink" title="3. 指向常量的常量指针 (Constant Pointer to Constant)"></a>3. 指向常量的常量指针 (Constant Pointer to Constant)</h4><ul><li>指针本身和其指向的内容都不可改变。</li><li>声明格式：<code>const &lt;数据类型&gt; * const &lt;指针变量名&gt;;</code></li><li><strong>必须在声明时初始化。</strong>  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">const</span> <span class="hljs-type">int</span> a = <span class="hljs-number">10</span>;<br><span class="hljs-type">const</span> <span class="hljs-type">int</span> * <span class="hljs-type">const</span> p = &amp;a;<br><span class="hljs-comment">// *p = 15; // 错误</span><br><span class="hljs-comment">// int b = 20;</span><br><span class="hljs-comment">// p = &amp;b; // 错误</span><br></code></pre></td></tr></table></figure></li></ul><h3 id="三、指针与数组"><a href="#三、指针与数组" class="headerlink" title="三、指针与数组"></a>三、指针与数组</h3><h4 id="1-指向一维数组元素的指针"><a href="#1-指向一维数组元素的指针" class="headerlink" title="1. 指向一维数组元素的指针"></a>1. 指向一维数组元素的指针</h4><ul><li><strong>数组名即指针常量</strong>：数组名代表数组首元素的地址。<code>a</code> 等价于 <code>&amp;a[0]</code>。</li><li>可以用指针访问数组元素：  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">int</span> arr[<span class="hljs-number">5</span>] = &#123;<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>, <span class="hljs-number">4</span>, <span class="hljs-number">5</span>&#125;;<br><span class="hljs-type">int</span> *p = arr; <span class="hljs-comment">// p 指向 arr[0]</span><br><br><span class="hljs-comment">// 访问方式等价：</span><br><span class="hljs-comment">// arr[i]  &lt;==&gt; *(arr + i) &lt;==&gt; *(p + i) &lt;==&gt; p[i]</span><br><span class="hljs-comment">// &amp;arr[i] &lt;==&gt; arr + i    &lt;==&gt; p + i</span><br></code></pre></td></tr></table></figure></li><li>指针变量可以进行自增、自减等运算来移动指向，数组名作为常量指针不行。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs cpp">p++; <span class="hljs-comment">// p 指向 arr[1]</span><br><span class="hljs-comment">// arr++; // 错误</span><br></code></pre></td></tr></table></figure></li></ul><h4 id="2-指向二维数组元素的指针"><a href="#2-指向二维数组元素的指针" class="headerlink" title="2. 指向二维数组元素的指针"></a>2. 指向二维数组元素的指针</h4><ul><li>二维数组 <code>A[m][n]</code> 在内存中是按行连续存储的。</li><li><strong><code>A</code></strong>：二维数组名，代表首行 <code>A[0]</code> 的地址，是一个指向包含 <code>n</code> 个元素的一维数组的指针。类型为 <code>(*)[n]</code>。</li><li><strong><code>A[i]</code></strong>：第 <code>i</code> 行的数组名，代表该行首元素 <code>A[i][0]</code> 的地址。类型为 <code>*</code>。</li><li><strong><code>&amp;A[i][j]</code></strong>：第 <code>i</code> 行第 <code>j</code> 列元素的地址。</li><li><strong>访问方式</strong>：<ul><li><code>A[i][j]</code></li><li><code>*(A[i] + j)</code></li><li><code>*(*(A + i) + j)</code></li></ul></li><li><strong>用指针访问二维数组元素</strong>：<ul><li><strong>行指针 (指向一维数组的指针)</strong>：  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">int</span> A[<span class="hljs-number">3</span>][<span class="hljs-number">4</span>];<br><span class="hljs-built_in">int</span> (*p)[<span class="hljs-number">4</span>]; <span class="hljs-comment">// p是一个指针，指向包含4个int元素的一维数组</span><br>p = A;       <span class="hljs-comment">// p指向A[0]</span><br><span class="hljs-comment">// p+i 指向 A[i]</span><br><span class="hljs-comment">// *(p+i) 是 A[i] 的首元素地址，即 &amp;A[i][0]</span><br><span class="hljs-comment">// *(*(p+i)+j) 是 A[i][j] 的值</span><br><span class="hljs-comment">// (*(p+i))[j] 也是 A[i][j] 的值</span><br></code></pre></td></tr></table></figure></li><li><strong>元素指针 (指向单个元素的指针)</strong>：  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">int</span> A[<span class="hljs-number">3</span>][<span class="hljs-number">4</span>];<br><span class="hljs-type">int</span> *p;<br>p = A[<span class="hljs-number">0</span>]; <span class="hljs-comment">// p 指向 A[0][0] (或 p = &amp;A[0][0];)</span><br><span class="hljs-comment">// 此时p可以像一维数组指针一样遍历整个二维数组</span><br><span class="hljs-comment">// p + (i * 列数 + j) 指向 A[i][j] 的地址</span><br></code></pre></td></tr></table></figure></li></ul></li></ul><h4 id="3-指针数组-Array-of-Pointers"><a href="#3-指针数组-Array-of-Pointers" class="headerlink" title="3. 指针数组 (Array of Pointers)"></a>3. 指针数组 (Array of Pointers)</h4><ul><li>数组的每个元素都是指针。</li><li>声明格式：<code>&lt;数据类型&gt; *&lt;数组名&gt;[&lt;元素个数&gt;];</code>  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">int</span> a=<span class="hljs-number">1</span>, b=<span class="hljs-number">2</span>, c=<span class="hljs-number">3</span>;<br><span class="hljs-type">int</span> *ptrArr[<span class="hljs-number">3</span>] = &#123;&amp;a, &amp;b, &amp;c&#125;; <span class="hljs-comment">// ptrArr是一个包含3个int*类型元素的数组</span><br><span class="hljs-comment">// ptrArr[0] 指向 a, ptrArr[1] 指向 b, ptrArr[2] 指向 c</span><br></code></pre></td></tr></table></figure></li><li>常用于存储多个字符串：  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">char</span> *names[] = &#123;<span class="hljs-string">&quot;Alice&quot;</span>, <span class="hljs-string">&quot;Bob&quot;</span>, <span class="hljs-string">&quot;Charlie&quot;</span>&#125;;<br><span class="hljs-comment">// names[0] 指向 &quot;Alice&quot; 的首字符 &#x27;A&#x27;</span><br></code></pre></td></tr></table></figure></li></ul><h4 id="4-多重指针-Pointer-to-Pointer"><a href="#4-多重指针-Pointer-to-Pointer" class="headerlink" title="4. 多重指针 (Pointer to Pointer)"></a>4. 多重指针 (Pointer to Pointer)</h4><ul><li>指针变量本身也存储在内存中，也有地址。指向指针变量地址的指针称为多重指针。</li><li><strong>二重指针</strong>：指向一个指针变量的指针。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">int</span> x = <span class="hljs-number">123</span>;<br><span class="hljs-type">int</span> *p = &amp;x;    <span class="hljs-comment">// p 指向 x</span><br><span class="hljs-type">int</span> **q = &amp;p;   <span class="hljs-comment">// q 指向 p</span><br><span class="hljs-comment">// *q  等价于 p (即x的地址)</span><br><span class="hljs-comment">// **q 等价于 *p (即x的值，123)</span><br></code></pre></td></tr></table></figure></li><li>常用于动态分配二维数组或处理指针数组。</li></ul><h4 id="5-字符指针与字符串"><a href="#5-字符指针与字符串" class="headerlink" title="5. 字符指针与字符串"></a>5. 字符指针与字符串</h4><ul><li>字符串可以用字符数组表示，也可以用字符指针指向字符串字面值常量。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">char</span> strArray[] = <span class="hljs-string">&quot;hello&quot;</span>; <span class="hljs-comment">// 字符数组，&quot;hello&quot;存储在数组中，可修改</span><br><span class="hljs-type">char</span> *strPtr = <span class="hljs-string">&quot;world&quot;</span>;   <span class="hljs-comment">// 字符指针，指向字符串常量&quot;world&quot;</span><br>                          <span class="hljs-comment">// 字符串常量通常存储在只读区，不可通过strPtr修改</span><br></code></pre></td></tr></table></figure></li><li><code>cout &lt;&lt; strPtr;</code> 会输出整个字符串，直到遇到 <code>\0</code>。</li><li>字符指针数组常用于管理多个字符串。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">char</span> *days[] = &#123;<span class="hljs-string">&quot;Sunday&quot;</span>, <span class="hljs-string">&quot;Monday&quot;</span>, ...&#125;;<br></code></pre></td></tr></table></figure></li><li><strong>字符串处理函数</strong> (需包含 <code>&lt;cstring&gt;</code> 或 <code>&lt;string.h&gt;</code>)：<ul><li><code>strlen(const char *s)</code>：返回字符串长度（不包括 <code>\0</code>）。</li><li><code>strcpy(char *dest, const char *src)</code>：复制字符串（有安全风险，建议用 <code>strcpy_s</code>）。</li><li><code>strcat(char *dest, const char *src)</code>：连接字符串（有安全风险，建议用 <code>strcat_s</code>）。</li><li><code>strcmp(const char *s1, const char *s2)</code>：比较字符串。</li></ul></li></ul><h3 id="四、动态内存分配"><a href="#四、动态内存分配" class="headerlink" title="四、动态内存分配"></a>四、动态内存分配</h3><h4 id="1-C-内存管理"><a href="#1-C-内存管理" class="headerlink" title="1. C++内存管理"></a>1. C++内存管理</h4><ul><li><strong>静态存储区 (Static&#x2F;Global Storage)</strong>：存放全局变量、静态变量。程序运行期间一直存在。</li><li><strong>栈区 (Stack)</strong>：存放函数参数、局部变量。函数调用时分配，调用结束时自动释放。</li><li><strong>堆区 (Heap)</strong>：用于动态内存分配。程序员手动通过 <code>new</code> 分配，通过 <code>delete</code> 释放。</li></ul><h4 id="2-动态内存分配运算符"><a href="#2-动态内存分配运算符" class="headerlink" title="2. 动态内存分配运算符"></a>2. 动态内存分配运算符</h4><ul><li><strong><code>new</code> 运算符</strong>：在堆区分配内存。<ul><li>分配单个变量：<code>&lt;指针变量&gt; = new &lt;数据类型&gt;;</code></li><li>分配单个变量并初始化：<code>&lt;指针变量&gt; = new &lt;数据类型&gt;(&lt;初值&gt;);</code> 或 <code>&lt;指针变量&gt; = new &lt;数据类型&gt;&#123;&lt;初值&gt;&#125;;</code> (C++11)</li><li>分配一维数组：<code>&lt;指针变量&gt; = new &lt;数据类型&gt;[&lt;元素个数&gt;];</code></li><li>如果分配失败（内存不足），<code>new</code> 默认会抛出 <code>std::bad_alloc</code> 异常。</li></ul></li><li><strong><code>delete</code> 运算符</strong>：释放 <code>new</code> 分配的内存。<ul><li>释放单个变量：<code>delete &lt;指针变量&gt;;</code></li><li>释放一维数组：<code>delete [] &lt;指针变量&gt;;</code> (注意 <code>[]</code> 不可省略)</li><li><strong>重要</strong>：<ul><li><code>delete</code> 只能用于 <code>new</code> 分配的内存。</li><li>同一块内存不能 <code>delete</code> 多次。</li><li><code>delete</code> 后，指针变量本身的值不会改变（仍指向原地址，成为悬挂指针），最好将其设为 <code>nullptr</code>。</li><li><code>new</code> 和 <code>delete</code> (以及 <code>new[]</code> 和 <code>delete[]</code>)必须配对使用，否则会导致<strong>内存泄漏 (Memory Leak)</strong> 或 <strong>未定义行为</strong>。</li></ul></li></ul></li></ul><h4 id="3-动态变量"><a href="#3-动态变量" class="headerlink" title="3. 动态变量"></a>3. 动态变量</h4><ul><li>通过 <code>new &lt;数据类型&gt;</code> 创建的无名变量，通过指针访问。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">int</span> *p = <span class="hljs-keyword">new</span> <span class="hljs-built_in">int</span>(<span class="hljs-number">5</span>); <span class="hljs-comment">// 分配一个int空间，初始化为5</span><br>cout &lt;&lt; *p;          <span class="hljs-comment">// 输出5</span><br><span class="hljs-keyword">delete</span> p;            <span class="hljs-comment">// 释放内存</span><br>p = <span class="hljs-literal">nullptr</span>;<br></code></pre></td></tr></table></figure></li></ul><h4 id="4-动态数组"><a href="#4-动态数组" class="headerlink" title="4. 动态数组"></a>4. 动态数组</h4><ul><li><strong>一维动态数组</strong>：  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">int</span> n;<br>cin &gt;&gt; n;<br><span class="hljs-type">int</span> *arr = <span class="hljs-keyword">new</span> <span class="hljs-type">int</span>[n]; <span class="hljs-comment">// 数组大小可以是变量</span><br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; n; ++i) &#123;<br>    arr[i] = i * <span class="hljs-number">10</span>;<br>&#125;<br><span class="hljs-comment">// ...使用arr...</span><br><span class="hljs-keyword">delete</span> [] arr;<br>arr = <span class="hljs-literal">nullptr</span>;<br></code></pre></td></tr></table></figure></li><li><strong>二维动态数组</strong>：通常通过指针数组实现。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">int</span> rows, cols;<br>cin &gt;&gt; rows &gt;&gt; cols;<br><br><span class="hljs-comment">// 分配行指针数组</span><br><span class="hljs-type">int</span> **matrix = <span class="hljs-keyword">new</span> <span class="hljs-type">int</span>*[rows];<br><span class="hljs-comment">// 为每一行分配列空间</span><br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; rows; ++i) &#123;<br>    matrix[i] = <span class="hljs-keyword">new</span> <span class="hljs-type">int</span>[cols];<br>&#125;<br><br><span class="hljs-comment">// 初始化和使用 matrix[i][j]</span><br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; rows; ++i) &#123;<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = <span class="hljs-number">0</span>; j &lt; cols; ++j) &#123;<br>        matrix[i][j] = i * cols + j;<br>    &#125;<br>&#125;<br><br><span class="hljs-comment">// 释放内存 (与分配顺序相反)</span><br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; rows; ++i) &#123;<br>    <span class="hljs-keyword">delete</span> [] matrix[i]; <span class="hljs-comment">// 先释放每一行</span><br>&#125;<br><span class="hljs-keyword">delete</span> [] matrix; <span class="hljs-comment">// 再释放行指针数组</span><br>matrix = <span class="hljs-literal">nullptr</span>;<br></code></pre></td></tr></table></figure></li><li><strong>内存泄漏 (Memory Leak)</strong>：如果动态分配的内存不再需要但没有被 <code>delete</code>，这块内存就无法再被程序使用，造成浪费。</li></ul><h3 id="五、引用类型"><a href="#五、引用类型" class="headerlink" title="五、引用类型"></a>五、引用类型</h3><h4 id="1-引用的含义"><a href="#1-引用的含义" class="headerlink" title="1. 引用的含义"></a>1. 引用的含义</h4><ul><li><strong>引用 (Reference)</strong>：是已存在变量的一个<strong>别名 (Alias)</strong>。</li><li>引用不是新定义一个变量，它不占用独立的内存空间（或者说它与它所引用的变量共享同一块内存空间）。</li><li>对引用的操作就是对其所引用的变量的操作。</li></ul><h4 id="2-引用变量"><a href="#2-引用变量" class="headerlink" title="2. 引用变量"></a>2. 引用变量</h4><ul><li><strong>引用变量的说明与初始化</strong>：<ul><li>格式：<code>&lt;数据类型&gt; &amp;&lt;引用名&gt; = &lt;已存在变量名&gt;;</code></li><li><strong>引用在声明时必须初始化</strong>，并且一旦初始化，就不能再引用其他变量。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">int</span> a = <span class="hljs-number">10</span>;<br><span class="hljs-type">int</span> &amp;ref_a = a; <span class="hljs-comment">// ref_a 是 a 的引用 (别名)</span><br><br>cout &lt;&lt; a;      <span class="hljs-comment">// 输出 10</span><br>cout &lt;&lt; ref_a;  <span class="hljs-comment">// 输出 10</span><br><br>ref_a = <span class="hljs-number">20</span>;     <span class="hljs-comment">// 修改 ref_a 就是修改 a</span><br>cout &lt;&lt; a;      <span class="hljs-comment">// 输出 20</span><br></code></pre></td></tr></table></figure></li><li><code>&amp;</code> 在这里不是取地址运算符，而是引用声明符。</li></ul></li></ul><h4 id="3-引用与指针的区别"><a href="#3-引用与指针的区别" class="headerlink" title="3. 引用与指针的区别"></a>3. 引用与指针的区别</h4><table><thead><tr><th align="left">特性</th><th align="left">指针 (Pointer)</th><th align="left">引用 (Reference)</th></tr></thead><tbody><tr><td align="left"><strong>本质</strong></td><td align="left">存储变量地址的变量</td><td align="left">已存在变量的别名</td></tr><tr><td align="left"><strong>内存空间</strong></td><td align="left">占用独立的内存空间</td><td align="left">不占用独立内存空间 (与原变量共享)</td></tr><tr><td align="left"><strong>初始化</strong></td><td align="left">可以不初始化 (但危险)，可以为空 (<code>nullptr</code>)</td><td align="left">必须在声明时初始化，不能为空</td></tr><tr><td align="left"><strong>可变性</strong></td><td align="left">可以改变指向 (指向不同变量)</td><td align="left">一旦初始化，不能再引用其他变量</td></tr><tr><td align="left"><strong>操作</strong></td><td align="left">通过 <code>*</code> 间接访问，<code>p</code> 是地址，<code>*p</code> 是内容</td><td align="left">直接使用，<code>ref</code> 就是内容</td></tr><tr><td align="left"><strong>多级</strong></td><td align="left">可以有多级指针 (<code>int **p</code>)</td><td align="left">不能有引用的引用 (<code>int &amp;&amp;r</code> 非C++11右值引用含义)</td></tr><tr><td align="left"><strong>数组</strong></td><td align="left">可以有指针数组 (<code>int *arr[]</code>)</td><td align="left">不能有引用数组 (<code>int &amp;arr[]</code> 非法)</td></tr><tr><td align="left"><strong>空值</strong></td><td align="left">可以为 <code>nullptr</code></td><td align="left">必须引用一个存在的实体</td></tr></tbody></table><h4 id="4-引用的安全性"><a href="#4-引用的安全性" class="headerlink" title="4. 引用的安全性"></a>4. 引用的安全性</h4><ul><li>引用比指针更安全，因为它必须初始化，且不能为空，也不能随意改变引用的对象。</li><li>常用于函数参数（传递大对象时避免拷贝开销，且能修改实参）和函数返回值。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-comment">// 通过引用修改参数</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">swap</span><span class="hljs-params">(<span class="hljs-type">int</span> &amp;x, <span class="hljs-type">int</span> &amp;y)</span> </span>&#123;<br>    <span class="hljs-type">int</span> temp = x;<br>    x = y;<br>    y = temp;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">main</span><span class="hljs-params">()</span> </span>&#123;<br>    <span class="hljs-type">int</span> m = <span class="hljs-number">5</span>, n = <span class="hljs-number">10</span>;<br>    <span class="hljs-built_in">swap</span>(m, n); <span class="hljs-comment">// m变为10, n变为5</span><br>    <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>&#125;<br></code></pre></td></tr></table></figure></li><li><strong>数组的引用</strong>：  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">int</span> arr[<span class="hljs-number">5</span>];<br><span class="hljs-built_in">int</span> (&amp;ref_arr)[<span class="hljs-number">5</span>] = arr; <span class="hljs-comment">// ref_arr 是数组 arr 的引用</span><br>ref_arr[<span class="hljs-number">0</span>] = <span class="hljs-number">10</span>; <span class="hljs-comment">// 等价于 arr[0] = 10;</span><br></code></pre></td></tr></table></figure></li></ul><h2 id="第七章-类和对象"><a href="#第七章-类和对象" class="headerlink" title="第七章 类和对象"></a>第七章 类和对象</h2><h3 id="一、面向对象程序设计思想"><a href="#一、面向对象程序设计思想" class="headerlink" title="一、面向对象程序设计思想"></a>一、面向对象程序设计思想</h3><h4 id="1-结构化程序设计与面向对象程序设计"><a href="#1-结构化程序设计与面向对象程序设计" class="headerlink" title="1. 结构化程序设计与面向对象程序设计"></a>1. 结构化程序设计与面向对象程序设计</h4><ul><li><strong>结构化程序设计</strong>：以过程&#x2F;函数为中心，自顶向下，逐步求精。</li><li><strong>面向对象程序设计 (OOP)</strong>：以对象为中心，将数据和操作数据的函数封装在一起。主要特征：封装、继承、多态。</li></ul><h4 id="2-类和对象的基本概念"><a href="#2-类和对象的基本概念" class="headerlink" title="2. 类和对象的基本概念"></a>2. 类和对象的基本概念</h4><ul><li><strong>类 (Class)</strong>：<ul><li>对具有相同属性（数据）和行为（操作）的对象的抽象描述。</li><li>一种用户自定义的数据类型，是创建对象的模板。</li><li>定义类时不分配内存。</li></ul></li><li><strong>对象 (Object&#x2F;Instance)</strong>：<ul><li>类的具体实例。</li><li>程序运行时，对象在内存中占据实际空间。</li><li>每个对象都有其自身的状态（由数据成员的值决定）和行为（由成员函数实现）。</li></ul></li></ul><h4 id="3-面向对象程序设计的特点"><a href="#3-面向对象程序设计的特点" class="headerlink" title="3. 面向对象程序设计的特点"></a>3. 面向对象程序设计的特点</h4><ul><li><strong>封装 (Encapsulation)</strong>：将数据（属性）和操作这些数据的函数（方法）捆绑在一起，形成一个独立的单元（类）。对外部隐藏对象的内部实现细节，只提供公共接口进行交互。</li><li><strong>继承 (Inheritance)</strong>：允许创建一个新类（派生类&#x2F;子类），从一个或多个已存在的类（基类&#x2F;父类）继承属性和行为。实现代码重用和层次化组织。</li><li><strong>多态 (Polymorphism)</strong>：同一操作作用于不同的对象，可以有不同的解释，产生不同的执行结果。主要通过虚函数和重载实现。</li></ul><h4 id="4-面向对象程序设计的过程"><a href="#4-面向对象程序设计的过程" class="headerlink" title="4. 面向对象程序设计的过程"></a>4. 面向对象程序设计的过程</h4><ol><li><strong>识别对象和类</strong>：分析问题域，找出关键实体及其共同特征。</li><li><strong>定义类的属性和行为</strong>：确定类的数据成员和成员函数。</li><li><strong>定义类之间的关系</strong>：如继承、组合等。</li><li><strong>创建和使用对象</strong>：通过类的实例解决问题。</li></ol><h3 id="二、类的定义与对象的说明"><a href="#二、类的定义与对象的说明" class="headerlink" title="二、类的定义与对象的说明"></a>二、类的定义与对象的说明</h3><h4 id="1-类的定义"><a href="#1-类的定义" class="headerlink" title="1. 类的定义"></a>1. 类的定义</h4><ul><li>使用 <code>class</code> 关键字。</li><li>一般格式：  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> &lt;类名&gt; &#123;<br><span class="hljs-keyword">private</span>:<br>    <span class="hljs-comment">// 私有成员 (数据和函数)</span><br>    <span class="hljs-comment">// 只能被本类的成员函数和友元访问</span><br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-comment">// 公有成员 (数据和函数)</span><br>    <span class="hljs-comment">// 可以被类外部访问，是类的接口</span><br><span class="hljs-keyword">protected</span>:<br>    <span class="hljs-comment">// 保护成员 (数据和函数)</span><br>    <span class="hljs-comment">// 可以被本类的成员函数、友元以及派生类的成员函数访问</span><br><span class="hljs-comment">// friend &lt;友元声明&gt;; // 友元不是成员，但可以访问私有和保护成员</span><br>&#125;; <span class="hljs-comment">// 注意类定义末尾的分号</span><br></code></pre></td></tr></table></figure></li><li><strong>访问限定符 (Access Specifiers)</strong>：<code>private</code>, <code>public</code>, <code>protected</code>。<ul><li>若不写访问限定符，类中成员默认为 <code>private</code> (结构体 <code>struct</code> 中默认为 <code>public</code>)。</li><li>访问限定符可以出现多次，其作用域到下一个访问限定符或类定义结束。</li></ul></li></ul><h4 id="2-成员变量与成员函数"><a href="#2-成员变量与成员函数" class="headerlink" title="2. 成员变量与成员函数"></a>2. 成员变量与成员函数</h4><ul><li><strong>成员变量 (Data Members &#x2F; Attributes)</strong>：描述类的属性，可以是任何数据类型（包括其他类的对象）。<ul><li>可以在声明时赋默认值 (C++11)。</li></ul></li><li><strong>成员函数 (Member Functions &#x2F; Methods)</strong>：描述类的行为，操作类的数据成员。<ul><li><strong>类内定义</strong>：函数体直接写在类定义中，通常自动成为内联函数 (inline)。</li><li><strong>类外定义</strong>：在类定义中只写函数原型，在类外部使用作用域解析运算符 <code>::</code> 定义函数体。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">MyClass</span> &#123;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">func1</span><span class="hljs-params">()</span></span>; <span class="hljs-comment">// 类内声明</span><br>    <span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">func2</span><span class="hljs-params">()</span> </span>&#123; <span class="hljs-comment">/* 类内定义 */</span> &#125;<br>&#125;;<br><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">MyClass::func1</span><span class="hljs-params">()</span> </span>&#123; <span class="hljs-comment">// 类外定义</span><br>    <span class="hljs-comment">// ...</span><br>&#125;<br></code></pre></td></tr></table></figure></li></ul></li></ul><h4 id="3-类对象的说明-创建对象"><a href="#3-类对象的说明-创建对象" class="headerlink" title="3. 类对象的说明 (创建对象)"></a>3. 类对象的说明 (创建对象)</h4><ul><li><strong>普通对象</strong>：<code>&lt;类名&gt; &lt;对象名&gt;;</code> 或 <code>&lt;类名&gt; &lt;对象名&gt;(&lt;构造函数实参&gt;);</code></li><li><strong>对象数组</strong>：<code>&lt;类名&gt; &lt;数组名&gt;[&lt;大小&gt;];</code></li><li><strong>对象指针</strong>：<code>&lt;类名&gt; *&lt;指针名&gt;;</code><ul><li>通常与 <code>new</code> 结合使用动态创建对象：<code>指针名 = new &lt;类名&gt;(&lt;构造函数实参&gt;);</code></li><li>访问成员：<code>指针名-&gt;成员</code> 或 <code>(*指针名).成员</code></li></ul></li><li><strong>对象引用</strong>：<code>&lt;类名&gt; &amp;&lt;引用名&gt; = &lt;已存在对象&gt;;</code></li></ul><h4 id="4-this-指针"><a href="#4-this-指针" class="headerlink" title="4. this 指针"></a>4. <code>this</code> 指针</h4><ul><li>是一个隐含的指针，存在于类的非静态成员函数中。</li><li>指向调用该成员函数的<strong>当前对象</strong>。</li><li>值为当前对象的起始地址。</li><li>主要用途：<ul><li>区分同名的成员变量和局部变量&#x2F;参数：<code>this-&gt;成员变量</code>。</li><li>在成员函数中返回当前对象的引用或指针：<code>return *this;</code> 或 <code>return this;</code>。</li></ul>  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Box</span> &#123;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-type">int</span> H;<br>    <span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">setH</span><span class="hljs-params">(<span class="hljs-type">int</span> H)</span> </span>&#123;<br>        <span class="hljs-keyword">this</span>-&gt;H = H; <span class="hljs-comment">// this-&gt;H 是成员变量, H 是参数</span><br>    &#125;<br>&#125;;<br></code></pre></td></tr></table></figure></li></ul><h3 id="三、构造函数与析构函数"><a href="#三、构造函数与析构函数" class="headerlink" title="三、构造函数与析构函数"></a>三、构造函数与析构函数</h3><h4 id="1-对象的初始化"><a href="#1-对象的初始化" class="headerlink" title="1. 对象的初始化"></a>1. 对象的初始化</h4><ul><li>对象在使用前通常需要初始化其数据成员。</li><li>可以通过构造函数自动完成。</li></ul><h4 id="2-构造函数-Constructor"><a href="#2-构造函数-Constructor" class="headerlink" title="2. 构造函数 (Constructor)"></a>2. 构造函数 (Constructor)</h4><ul><li>一种特殊的成员函数，在创建对象时自动被调用，用于初始化对象。</li><li><strong>特点</strong>：<ul><li>函数名与类名完全相同。</li><li>没有返回类型 (连 <code>void</code> 也没有)。</li><li>可以重载 (定义多个参数列表不同的构造函数)。</li><li>可以有参数，可以有默认参数。</li></ul></li><li><strong>默认构造函数 (Default Constructor)</strong>：<ul><li>无参数的构造函数。</li><li>如果类中没有定义任何构造函数，编译器会自动生成一个不做任何事情的默认构造函数。</li><li>一旦定义了任何构造函数，编译器就不再自动生成默认构造函数。如果此时仍需要无参构造，必须显式定义或使用 <code>= default</code> (C++11)。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">MyClass</span> &#123;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-built_in">MyClass</span>() = <span class="hljs-keyword">default</span>; <span class="hljs-comment">// 显式声明使用编译器生成的默认构造函数</span><br>    <span class="hljs-comment">// MyClass() &#123;&#125;      // 自定义默认构造函数</span><br>&#125;;<br></code></pre></td></tr></table></figure></li></ul></li><li><strong>自定义构造函数</strong>：<ul><li><strong>初始化列表 (Member Initializer List)</strong>：推荐使用，在构造函数体执行前初始化成员。效率更高，特别是对于类类型成员和 <code>const</code> 或引用成员（它们必须在初始化列表初始化）。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Point</span> &#123;<br>    <span class="hljs-type">int</span> x, y;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-built_in">Point</span>(<span class="hljs-type">int</span> ix, <span class="hljs-type">int</span> iy) : <span class="hljs-built_in">x</span>(ix), <span class="hljs-built_in">y</span>(iy) &#123; <span class="hljs-comment">// 初始化列表</span><br>        <span class="hljs-comment">// 构造函数体</span><br>    &#125;<br>&#125;;<br></code></pre></td></tr></table></figure></li></ul></li><li><strong>委托构造函数 (Delegating Constructor) (C++11)</strong>：一个构造函数可以在其初始化列表中调用同一个类的另一个构造函数。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Box</span> &#123;<br>    <span class="hljs-type">double</span> l, w, h;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-built_in">Box</span>(<span class="hljs-type">double</span> s) : <span class="hljs-built_in">Box</span>(s, s, s) &#123;&#125; <span class="hljs-comment">// 委托给三参数构造函数</span><br>    <span class="hljs-built_in">Box</span>(<span class="hljs-type">double</span> lv, <span class="hljs-type">double</span> wv, <span class="hljs-type">double</span> hv) : <span class="hljs-built_in">l</span>(lv), <span class="hljs-built_in">w</span>(wv), <span class="hljs-built_in">h</span>(hv) &#123;&#125;<br>&#125;;<br></code></pre></td></tr></table></figure></li><li><strong><code>explicit</code> 关键字</strong>：用于修饰单参数构造函数（或所有参数都有默认值的多参数构造函数），防止隐式类型转换。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">String</span> &#123;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-function"><span class="hljs-keyword">explicit</span> <span class="hljs-title">String</span><span class="hljs-params">(<span class="hljs-type">int</span> size)</span></span>; <span class="hljs-comment">// 防止 int 隐式转换为 String</span><br>    <span class="hljs-built_in">String</span>(<span class="hljs-type">const</span> <span class="hljs-type">char</span> *s);<br>&#125;;<br><span class="hljs-comment">// String s1 = 10; // 错误，如果构造函数是 explicit</span><br><span class="hljs-function">String <span class="hljs-title">s2</span><span class="hljs-params">(<span class="hljs-number">10</span>)</span></span>; <span class="hljs-comment">// 正确</span><br></code></pre></td></tr></table></figure></li></ul><h4 id="3-析构函数-Destructor"><a href="#3-析构函数-Destructor" class="headerlink" title="3. 析构函数 (Destructor)"></a>3. 析构函数 (Destructor)</h4><ul><li>一种特殊的成员函数，在对象生命周期结束时（如对象离开作用域、<code>delete</code> 指向对象的指针时）自动被调用。</li><li>主要用于释放对象占用的资源（如动态分配的内存、打开的文件等）。</li><li><strong>特点</strong>：<ul><li>函数名是 <code>~</code> 后跟类名 (例如 <code>~MyClass()</code>)。</li><li>没有返回类型，没有参数。</li><li>一个类只能有一个析构函数，不能重载。</li><li>如果类中没有定义析构函数，编译器会自动生成一个不做任何事情的默认析构函数。</li><li>如果类中管理了动态资源（如构造函数中 <code>new</code> 了内存），通常需要自定义析构函数来 <code>delete</code> 这些资源。</li></ul>  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">MyArray</span> &#123;<br>    <span class="hljs-type">int</span> *data;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-built_in">MyArray</span>(<span class="hljs-type">int</span> size) &#123; data = <span class="hljs-keyword">new</span> <span class="hljs-type">int</span>[size]; &#125;<br>    ~<span class="hljs-built_in">MyArray</span>() &#123;<br>        <span class="hljs-keyword">delete</span> [] data; <span class="hljs-comment">// 释放动态分配的内存</span><br>        data = <span class="hljs-literal">nullptr</span>;<br>    &#125;<br>&#125;;<br></code></pre></td></tr></table></figure></li><li><strong>构造和析构顺序</strong>：<ul><li>局部对象：后构造的先析构（栈特性）。</li><li>数组成员&#x2F;基类成员：按声明&#x2F;继承顺序构造，按相反顺序析构。</li></ul></li></ul><h4 id="4-拷贝构造函数-Copy-Constructor"><a href="#4-拷贝构造函数-Copy-Constructor" class="headerlink" title="4. 拷贝构造函数 (Copy Constructor)"></a>4. 拷贝构造函数 (Copy Constructor)</h4><ul><li>一种特殊的构造函数，用于使用一个已存在的同类对象来初始化一个新创建的对象。</li><li><strong>调用时机</strong>：<ol><li>用一个对象初始化另一个对象：<code>MyClass obj2 = obj1;</code> 或 <code>MyClass obj2(obj1);</code></li><li>对象作为函数参数按值传递时。</li><li>函数返回值为对象时。</li></ol></li><li><strong>原型</strong>：<code>&lt;类名&gt;(const &lt;类名&gt; &amp;&lt;引用名&gt;);</code> (通常参数是 <code>const</code> 引用)</li><li><strong>默认拷贝构造函数</strong>：如果类没有显式定义拷贝构造函数，编译器会自动生成一个，它执行<strong>浅拷贝 (Shallow Copy)</strong>，即逐个复制非静态成员的值。</li><li><strong>深拷贝 (Deep Copy)</strong>：<ul><li>当类中包含指针成员，并且这些指针指向动态分配的资源时，浅拷贝会导致问题（多个对象指向同一块内存，一个对象析构时释放内存，其他对象指针悬挂；重复释放）。</li><li>此时需要自定义拷贝构造函数，为新对象重新分配独立的资源，并复制内容，这就是深拷贝。</li></ul>  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">MyString</span> &#123;<br>    <span class="hljs-type">char</span> *str;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-built_in">MyString</span>(<span class="hljs-type">const</span> <span class="hljs-type">char</span> *s = <span class="hljs-string">&quot;&quot;</span>) &#123;<br>        str = <span class="hljs-keyword">new</span> <span class="hljs-type">char</span>[<span class="hljs-built_in">strlen</span>(s) + <span class="hljs-number">1</span>];<br>        <span class="hljs-built_in">strcpy</span>(str, s);<br>    &#125;<br>    <span class="hljs-comment">// 深拷贝构造函数</span><br>    <span class="hljs-built_in">MyString</span>(<span class="hljs-type">const</span> MyString &amp;other) &#123;<br>        str = <span class="hljs-keyword">new</span> <span class="hljs-type">char</span>[<span class="hljs-built_in">strlen</span>(other.str) + <span class="hljs-number">1</span>];<br>        <span class="hljs-built_in">strcpy</span>(str, other.str);<br>    &#125;<br>    ~<span class="hljs-built_in">MyString</span>() &#123; <span class="hljs-keyword">delete</span> [] str; &#125;<br>    <span class="hljs-comment">// ... 可能还需要重载赋值运算符=</span><br>&#125;;<br></code></pre></td></tr></table></figure></li><li><strong>赋值运算符重载 (<code>operator=</code>)</strong>：与拷贝构造函数类似，当类管理动态资源时，通常也需要重载赋值运算符以实现深拷贝赋值，防止自我赋值，并正确处理原有资源。</li></ul><h3 id="四、常对象与常量成员"><a href="#四、常对象与常量成员" class="headerlink" title="四、常对象与常量成员"></a>四、常对象与常量成员</h3><h4 id="1-常对象-Constant-Object"><a href="#1-常对象-Constant-Object" class="headerlink" title="1. 常对象 (Constant Object)"></a>1. 常对象 (Constant Object)</h4><ul><li>用 <code>const</code> 修饰的对象。</li><li>声明格式：<code>const &lt;类名&gt; &lt;对象名&gt;(&lt;实参表&gt;);</code> 或 <code>&lt;类名&gt; const &lt;对象名&gt;(&lt;实参表&gt;);</code></li><li>常对象的数据成员在对象创建后不能被修改。</li><li><strong>常对象只能调用常量成员函数和访问公有常量数据成员。</strong></li></ul><h4 id="2-常量成员"><a href="#2-常量成员" class="headerlink" title="2. 常量成员"></a>2. 常量成员</h4><ul><li><strong>常量数据成员 (Constant Data Member)</strong>：<ul><li>用 <code>const</code> 修饰的数据成员。</li><li><strong>必须在构造函数的初始化列表中进行初始化。</strong></li><li>一旦初始化，其值不能再改变。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Test</span> &#123;<br>    <span class="hljs-type">const</span> <span class="hljs-type">int</span> MAX_SIZE;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-built_in">Test</span>(<span class="hljs-type">int</span> size) : <span class="hljs-built_in">MAX_SIZE</span>(size) &#123;&#125; <span class="hljs-comment">// 必须在初始化列表</span><br>&#125;;<br></code></pre></td></tr></table></figure></li></ul></li><li><strong>常量成员函数 (Constant Member Function)</strong>：<ul><li>在函数声明和定义的参数列表后加 <code>const</code> 关键字。</li><li>格式：<code>&lt;返回类型&gt; &lt;函数名&gt;(&lt;参数表&gt;) const;</code></li><li>常量成员函数<strong>不能修改对象的数据成员</strong> (除非数据成员被 <code>mutable</code> 修饰)。</li><li>常量成员函数可以被普通对象和常对象调用。</li><li>普通成员函数不能被常对象调用。</li><li>在常量成员函数中，<code>this</code> 指针是一个指向常对象的指针 (<code>const &lt;类名&gt;* const this</code>)。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Point</span> &#123;<br>    <span class="hljs-type">int</span> x, y;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-built_in">Point</span>(<span class="hljs-type">int</span> x, <span class="hljs-type">int</span> y) : <span class="hljs-built_in">x</span>(x), <span class="hljs-built_in">y</span>(y) &#123;&#125;<br>    <span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">getX</span><span class="hljs-params">()</span> <span class="hljs-type">const</span> </span>&#123; <span class="hljs-keyword">return</span> x; &#125; <span class="hljs-comment">// 常量成员函数</span><br>    <span class="hljs-comment">// void setX(int val) &#123; x = val; &#125; // 普通成员函数</span><br>    <span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">setX</span><span class="hljs-params">(<span class="hljs-type">int</span> val)</span> <span class="hljs-type">const</span> </span>&#123; <span class="hljs-comment">/* x = val; 错误 */</span> &#125; <span class="hljs-comment">// 尝试修改会报错</span><br>&#125;;<br><span class="hljs-comment">// const Point p1(1,2);</span><br><span class="hljs-comment">// p1.getX(); // 正确</span><br><span class="hljs-comment">// p1.setX(3); // 错误，如果setX不是const</span><br></code></pre></td></tr></table></figure></li></ul></li><li><code>mutable</code> 关键字：用于修饰数据成员，表示该成员即使在 <code>const</code> 成员函数或 <code>const</code> 对象中也可以被修改。</li></ul><h3 id="五、静态成员-Static-Members"><a href="#五、静态成员-Static-Members" class="headerlink" title="五、静态成员 (Static Members)"></a>五、静态成员 (Static Members)</h3><h4 id="1-静态数据成员-Static-Data-Members"><a href="#1-静态数据成员-Static-Data-Members" class="headerlink" title="1. 静态数据成员 (Static Data Members)"></a>1. 静态数据成员 (Static Data Members)</h4><ul><li>用 <code>static</code> 关键字修饰的数据成员。</li><li><strong>属于类本身，而不是类的某个特定对象。</strong> 该类的所有对象共享同一个静态数据成员的副本。</li><li><strong>必须在类定义之外进行初始化和定义</strong> (除非是 <code>const static</code> 整型或枚举型，可以在类内初始化 C++11&#x2F;17)。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Account</span> &#123;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-type">static</span> <span class="hljs-type">double</span> interestRate; <span class="hljs-comment">// 声明静态数据成员</span><br>    <span class="hljs-comment">// ...</span><br>&#125;;<br><span class="hljs-type">double</span> Account::interestRate = <span class="hljs-number">0.025</span>; <span class="hljs-comment">// 定义并初始化静态数据成员</span><br></code></pre></td></tr></table></figure></li><li><strong>访问方式</strong>：<ul><li>通过类名和作用域解析运算符：<code>&lt;类名&gt;::&lt;静态数据成员名&gt;</code></li><li>通过对象名：<code>&lt;对象名&gt;.&lt;静态数据成员名&gt;</code></li><li>通过对象指针：<code>&lt;指针名&gt;-&gt;&lt;静态数据成员名&gt;</code></li></ul></li><li>主要用途：统计对象个数、共享数据等。</li></ul><h4 id="2-静态成员函数-Static-Member-Functions"><a href="#2-静态成员函数-Static-Member-Functions" class="headerlink" title="2. 静态成员函数 (Static Member Functions)"></a>2. 静态成员函数 (Static Member Functions)</h4><ul><li>用 <code>static</code> 关键字修饰的成员函数。</li><li><strong>不依赖于任何特定对象，没有 <code>this</code> 指针。</strong></li><li><strong>只能直接访问类的静态数据成员和调用其他静态成员函数。</strong> 不能直接访问非静态成员（除非通过传递对象引用或指针）。</li><li><strong>访问方式</strong>：<ul><li>通过类名和作用域解析运算符：<code>&lt;类名&gt;::&lt;静态成员函数名&gt;()</code></li><li>通过对象名：<code>&lt;对象名&gt;.&lt;静态成员函数名&gt;()</code></li><li>通过对象指针：<code>&lt;指针名&gt;-&gt;&lt;静态成员函数名&gt;()</code></li></ul>  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Counter</span> &#123;<br>    <span class="hljs-type">static</span> <span class="hljs-type">int</span> count;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-built_in">Counter</span>() &#123; count++; &#125;<br>    <span class="hljs-function"><span class="hljs-type">static</span> <span class="hljs-type">int</span> <span class="hljs-title">getCount</span><span class="hljs-params">()</span> </span>&#123; <span class="hljs-keyword">return</span> count; &#125; <span class="hljs-comment">// 静态成员函数</span><br>&#125;;<br><span class="hljs-type">int</span> Counter::count = <span class="hljs-number">0</span>; <span class="hljs-comment">// 初始化</span><br><br><span class="hljs-comment">// Counter c1, c2;</span><br><span class="hljs-comment">// cout &lt;&lt; Counter::getCount(); // 通过类名调用</span><br></code></pre></td></tr></table></figure></li></ul><h3 id="六、友元-Friends"><a href="#六、友元-Friends" class="headerlink" title="六、友元 (Friends)"></a>六、友元 (Friends)</h3><h4 id="1-友元的基本概念"><a href="#1-友元的基本概念" class="headerlink" title="1. 友元的基本概念"></a>1. 友元的基本概念</h4><ul><li>友元是一种机制，允许一个类授予其他类或函数访问其 <code>private</code> 和 <code>protected</code> 成员的权限。</li><li>友元声明在类定义内部，使用 <code>friend</code> 关键字。</li><li>友元关系是单向的、不传递的。</li></ul><h4 id="2-友元函数-Friend-Functions"><a href="#2-友元函数-Friend-Functions" class="headerlink" title="2. 友元函数 (Friend Functions)"></a>2. 友元函数 (Friend Functions)</h4><ul><li>一个普通函数或另一个类的成员函数可以被声明为一个类的友元。</li><li>友元函数不是类的成员函数，但可以访问该类的所有成员。</li><li><strong>普通函数作为友元</strong>：  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">MyClass</span> &#123;<br>    <span class="hljs-type">int</span> data;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-built_in">MyClass</span>(<span class="hljs-type">int</span> d) : <span class="hljs-built_in">data</span>(d) &#123;&#125;<br>    <span class="hljs-function"><span class="hljs-keyword">friend</span> <span class="hljs-type">void</span> <span class="hljs-title">showData</span><span class="hljs-params">(<span class="hljs-type">const</span> MyClass &amp;obj)</span></span>; <span class="hljs-comment">// 友元函数声明</span><br>&#125;;<br><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">showData</span><span class="hljs-params">(<span class="hljs-type">const</span> MyClass &amp;obj)</span> </span>&#123;<br>    cout &lt;&lt; obj.data &lt;&lt; endl; <span class="hljs-comment">// 可以访问私有成员 data</span><br>&#125;<br></code></pre></td></tr></table></figure></li><li><strong>其他类的成员函数作为友元</strong>：  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">B</span>; <span class="hljs-comment">// 前向声明</span><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">A</span> &#123;<br>    <span class="hljs-type">int</span> val_a;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-built_in">A</span>(<span class="hljs-type">int</span> v) : <span class="hljs-built_in">val_a</span>(v) &#123;&#125;<br>    <span class="hljs-function"><span class="hljs-keyword">friend</span> <span class="hljs-type">void</span> <span class="hljs-title">B::displayA</span><span class="hljs-params">(<span class="hljs-type">const</span> A &amp;objA)</span></span>;<br>&#125;;<br><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">B</span> &#123;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">displayA</span><span class="hljs-params">(<span class="hljs-type">const</span> A &amp;objA)</span> </span>&#123;<br>        cout &lt;&lt; objA.val_a &lt;&lt; endl; <span class="hljs-comment">// B的成员函数访问A的私有成员</span><br>    &#125;<br>&#125;;<br></code></pre></td></tr></table></figure></li></ul><h4 id="3-友元类-Friend-Classes"><a href="#3-友元类-Friend-Classes" class="headerlink" title="3. 友元类 (Friend Classes)"></a>3. 友元类 (Friend Classes)</h4><ul><li>一个类B可以被声明为另一个类A的友元类。这意味着类B的所有成员函数都是类A的友元函数，可以访问类A的所有成员。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">A</span> &#123;<br>    <span class="hljs-type">int</span> secret;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-built_in">A</span>(<span class="hljs-type">int</span> s) : <span class="hljs-built_in">secret</span>(s) &#123;&#125;<br>    <span class="hljs-keyword">friend</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">B</span>; <span class="hljs-comment">// B是A的友元类</span><br>&#125;;<br><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">B</span> &#123;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">revealSecret</span><span class="hljs-params">(<span class="hljs-type">const</span> A &amp;objA)</span> </span>&#123;<br>        cout &lt;&lt; objA.secret &lt;&lt; endl; <span class="hljs-comment">// B可以访问A的私有成员</span><br>    &#125;<br>&#125;;<br></code></pre></td></tr></table></figure></li></ul><h3 id="七、类与类之间的关系"><a href="#七、类与类之间的关系" class="headerlink" title="七、类与类之间的关系"></a>七、类与类之间的关系</h3><h4 id="1-类的对象成员-Composition-Aggregation"><a href="#1-类的对象成员-Composition-Aggregation" class="headerlink" title="1. 类的对象成员 (Composition &#x2F; Aggregation)"></a>1. 类的对象成员 (Composition &#x2F; Aggregation)</h4><ul><li>一个类的数据成员可以是另一个类的对象。这表示 “has-a” 关系。</li><li><strong>构造顺序</strong>：当创建包含对象成员的类的对象时，会先调用对象成员的构造函数（按照在类中声明的顺序），然后再执行宿主类的构造函数体。</li><li><strong>初始化</strong>：对象成员的初始化通常在宿主类的构造函数的初始化列表中进行。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Engine</span> &#123; <span class="hljs-comment">/* ... */</span> &#125;;<br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Car</span> &#123;<br>    Engine carEngine; <span class="hljs-comment">// Engine对象是Car的成员</span><br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-built_in">Car</span>(...) : <span class="hljs-built_in">carEngine</span>(...) &#123; <span class="hljs-comment">/* ... */</span> &#125;<br>&#125;;<br></code></pre></td></tr></table></figure></li></ul><h4 id="2-类的嵌套-Nested-Classes"><a href="#2-类的嵌套-Nested-Classes" class="headerlink" title="2. 类的嵌套 (Nested Classes)"></a>2. 类的嵌套 (Nested Classes)</h4><ul><li>一个类可以定义在另一个类的内部。</li><li>嵌套类（内部类）的作用域在外围类之内。</li><li>访问权限规则依然适用。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Outer</span> &#123;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-keyword">class</span> <span class="hljs-title class_">Inner</span> &#123; <span class="hljs-comment">// 嵌套类</span><br>    <span class="hljs-keyword">public</span>:<br>        <span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">display</span><span class="hljs-params">()</span> </span>&#123; <span class="hljs-comment">/* ... */</span> &#125;<br>    &#125;;<br>    <span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">useInner</span><span class="hljs-params">()</span> </span>&#123;<br>        Inner in;<br>        in.<span class="hljs-built_in">display</span>();<br>    &#125;<br>&#125;;<br><span class="hljs-comment">// Outer::Inner obj; // 如果Inner是public的</span><br></code></pre></td></tr></table></figure></li></ul><h3 id="八、类中的运算符重载"><a href="#八、类中的运算符重载" class="headerlink" title="八、类中的运算符重载"></a>八、类中的运算符重载</h3><h4 id="1-运算符重载概述"><a href="#1-运算符重载概述" class="headerlink" title="1. 运算符重载概述"></a>1. 运算符重载概述</h4><ul><li>允许为自定义类类型的对象重新定义C++中已有运算符的含义。</li><li>使得对类对象的操作更自然、直观，类似基本数据类型。</li><li>通过定义特殊的成员函数或友元函数（称为运算符函数）来实现。</li><li>函数名格式：<code>operator&lt;运算符符号&gt;</code> (例如 <code>operator+</code>, <code>operator==</code>)。</li><li><strong>限制</strong>：<ul><li>不能创建新的运算符。</li><li>不能改变运算符的优先级、结合性或操作数个数。</li><li><code>.</code>, <code>::</code>, <code>?:</code>, <code>sizeof</code>, <code>.*</code> 这五个运算符不能被重载。</li><li><code>=</code>, <code>()</code>, <code>[]</code>, <code>-&gt;</code> 必须作为类的成员函数重载。</li></ul></li></ul><h4 id="2-友元方式重载运算符"><a href="#2-友元方式重载运算符" class="headerlink" title="2. 友元方式重载运算符"></a>2. 友元方式重载运算符</h4><ul><li>运算符函数是类的友元。</li><li>操作数都作为函数的显式参数。</li><li>对于二元运算符 <code>op</code>，<code>obj1 op obj2</code> 相当于 <code>operator op(obj1, obj2)</code>。</li><li>对于一元运算符 <code>op</code>，<code>op obj</code> 相当于 <code>operator op(obj)</code>。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Complex</span> &#123;<br>    <span class="hljs-type">double</span> real, imag;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-built_in">Complex</span>(<span class="hljs-type">double</span> r=<span class="hljs-number">0</span>, <span class="hljs-type">double</span> i=<span class="hljs-number">0</span>) : <span class="hljs-built_in">real</span>(r), <span class="hljs-built_in">imag</span>(i) &#123;&#125;<br>    <span class="hljs-keyword">friend</span> Complex <span class="hljs-keyword">operator</span>+(<span class="hljs-type">const</span> Complex &amp;c1, <span class="hljs-type">const</span> Complex &amp;c2);<br>    <span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">display</span><span class="hljs-params">()</span> <span class="hljs-type">const</span> </span>&#123; cout &lt;&lt; real &lt;&lt; <span class="hljs-string">&quot; + &quot;</span> &lt;&lt; imag &lt;&lt; <span class="hljs-string">&quot;i&quot;</span> &lt;&lt; endl; &#125;<br>&#125;;<br>Complex <span class="hljs-keyword">operator</span>+(<span class="hljs-type">const</span> Complex &amp;c1, <span class="hljs-type">const</span> Complex &amp;c2) &#123;<br>    <span class="hljs-keyword">return</span> <span class="hljs-built_in">Complex</span>(c<span class="hljs-number">1.</span>real + c<span class="hljs-number">2.</span>real, c<span class="hljs-number">1.</span>imag + c<span class="hljs-number">2.</span>imag);<br>&#125;<br><span class="hljs-comment">// Complex c1(1,2), c2(3,4); Complex c3 = c1 + c2;</span><br></code></pre></td></tr></table></figure></li></ul><h4 id="3-成员方式重载运算符"><a href="#3-成员方式重载运算符" class="headerlink" title="3. 成员方式重载运算符"></a>3. 成员方式重载运算符</h4><ul><li>运算符函数是类的成员函数。</li><li>左操作数是调用该函数的对象 (<code>this</code> 指针指向它)。</li><li>对于二元运算符 <code>op</code>，<code>obj1 op obj2</code> 相当于 <code>obj1.operator op(obj2)</code>。右操作数是参数。</li><li>对于一元运算符 <code>op</code>，<code>op obj</code> 相当于 <code>obj.operator op()</code>。无参数。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Complex</span> &#123;<br>    <span class="hljs-type">double</span> real, imag;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-built_in">Complex</span>(<span class="hljs-type">double</span> r=<span class="hljs-number">0</span>, <span class="hljs-type">double</span> i=<span class="hljs-number">0</span>) : <span class="hljs-built_in">real</span>(r), <span class="hljs-built_in">imag</span>(i) &#123;&#125;<br>    Complex <span class="hljs-keyword">operator</span>+(<span class="hljs-type">const</span> Complex &amp;other) <span class="hljs-type">const</span> &#123; <span class="hljs-comment">// 成员函数</span><br>        <span class="hljs-keyword">return</span> <span class="hljs-built_in">Complex</span>(<span class="hljs-keyword">this</span>-&gt;real + other.real, <span class="hljs-keyword">this</span>-&gt;imag + other.imag);<br>    &#125;<br>    <span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">display</span><span class="hljs-params">()</span> <span class="hljs-type">const</span> </span>&#123; cout &lt;&lt; real &lt;&lt; <span class="hljs-string">&quot; + &quot;</span> &lt;&lt; imag &lt;&lt; <span class="hljs-string">&quot;i&quot;</span> &lt;&lt; endl; &#125;<br>&#125;;<br><span class="hljs-comment">// Complex c1(1,2), c2(3,4); Complex c3 = c1 + c2; // c1.operator+(c2)</span><br></code></pre></td></tr></table></figure></li><li><strong>输入&#x2F;输出运算符 (<code>&lt;&lt;</code>, <code>&gt;&gt;</code>) 重载</strong>：通常作为友元函数重载，因为左操作数是流对象 (<code>ostream</code> 或 <code>istream</code>)。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Point</span> &#123;<br>    <span class="hljs-type">int</span> x, y;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-built_in">Point</span>(<span class="hljs-type">int</span> x=<span class="hljs-number">0</span>, <span class="hljs-type">int</span> y=<span class="hljs-number">0</span>) : <span class="hljs-built_in">x</span>(x), <span class="hljs-built_in">y</span>(y) &#123;&#125;<br>    <span class="hljs-keyword">friend</span> ostream&amp; <span class="hljs-keyword">operator</span>&lt;&lt;(ostream &amp;out, <span class="hljs-type">const</span> Point &amp;p);<br>    <span class="hljs-keyword">friend</span> istream&amp; <span class="hljs-keyword">operator</span>&gt;&gt;(istream &amp;in, Point &amp;p);<br>&#125;;<br>ostream&amp; <span class="hljs-keyword">operator</span>&lt;&lt;(ostream &amp;out, <span class="hljs-type">const</span> Point &amp;p) &#123;<br>    out &lt;&lt; <span class="hljs-string">&quot;(&quot;</span> &lt;&lt; p.x &lt;&lt; <span class="hljs-string">&quot;, &quot;</span> &lt;&lt; p.y &lt;&lt; <span class="hljs-string">&quot;)&quot;</span>;<br>    <span class="hljs-keyword">return</span> out;<br>&#125;<br>istream&amp; <span class="hljs-keyword">operator</span>&gt;&gt;(istream &amp;in, Point &amp;p) &#123;<br>    in &gt;&gt; p.x &gt;&gt; p.y;<br>    <span class="hljs-keyword">return</span> in;<br>&#125;<br></code></pre></td></tr></table></figure></li></ul><h3 id="九、简单的数据结构设计-使用类"><a href="#九、简单的数据结构设计-使用类" class="headerlink" title="九、简单的数据结构设计 (使用类)"></a>九、简单的数据结构设计 (使用类)</h3><ul><li><strong>链表 (Linked List)</strong>：<ul><li>节点类 (Node)：包含数据域和指向下一个节点的指针域。</li><li>链表类 (List)：包含头指针 (head)、尾指针 (tail)，以及插入、删除、查找、打印等操作。</li></ul></li><li><strong>栈 (Stack)</strong>：后进先出 (LIFO)。<ul><li>可以用数组或链表实现。</li><li>操作：<code>push</code> (入栈), <code>pop</code> (出栈), <code>top</code> (查看栈顶元素), <code>isEmpty</code>。</li></ul></li><li><strong>队列 (Queue)</strong>：先进先出 (FIFO)。<ul><li>可以用数组或链表实现。</li><li>操作：<code>enqueue</code> (入队), <code>dequeue</code> (出队), <code>front</code> (查看队头元素), <code>isEmpty</code>。</li></ul></li></ul><h2 id="第八章-类的继承与多态性"><a href="#第八章-类的继承与多态性" class="headerlink" title="第八章 类的继承与多态性"></a>第八章 类的继承与多态性</h2><h3 id="一、类的继承与派生"><a href="#一、类的继承与派生" class="headerlink" title="一、类的继承与派生"></a>一、类的继承与派生</h3><h4 id="1-继承与派生的基本概念"><a href="#1-继承与派生的基本概念" class="headerlink" title="1. 继承与派生的基本概念"></a>1. 继承与派生的基本概念</h4><ul><li><strong>继承 (Inheritance)</strong>：一种机制，允许一个类（派生类）获取另一个类（基类）的属性和方法。</li><li><strong>派生 (Derivation)</strong>：从基类创建派生类的过程。</li><li><strong>基类 (Base Class &#x2F; Parent Class &#x2F; Superclass)</strong>：被继承的类。</li><li><strong>派生类 (Derived Class &#x2F; Child Class &#x2F; Subclass)</strong>：通过继承创建的新类。</li><li><strong>目的</strong>：代码重用、建立类之间的层次关系 (“is-a” 关系)。</li></ul><h4 id="2-继承的种类"><a href="#2-继承的种类" class="headerlink" title="2. 继承的种类"></a>2. 继承的种类</h4><ul><li><strong>单继承 (Single Inheritance)</strong>：一个派生类只有一个直接基类。</li><li><strong>多级继承 (Multilevel Inheritance)</strong>：派生类本身又可以作为另一个类的基类，形成继承链 (e.g., A -&gt; B -&gt; C)。</li><li><strong>多重继承 (Multiple Inheritance)</strong>：一个派生类可以有多个直接基类。</li></ul><h3 id="二、派生类"><a href="#二、派生类" class="headerlink" title="二、派生类"></a>二、派生类</h3><h4 id="1-派生类的定义"><a href="#1-派生类的定义" class="headerlink" title="1. 派生类的定义"></a>1. 派生类的定义</h4><ul><li>语法：  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> &lt;派生类名&gt; : &lt;继承方式&gt; &lt;基类名<span class="hljs-number">1</span>&gt;, &lt;继承方式&gt; &lt;基类名<span class="hljs-number">2</span>&gt;, ... &#123;<br>    <span class="hljs-comment">// 派生类新增的成员</span><br>&#125;;<br></code></pre></td></tr></table></figure></li><li><strong>继承方式 (Access Specifier for Inheritance)</strong>：<code>public</code>, <code>protected</code>, <code>private</code>。<ul><li><strong><code>public</code> 继承</strong> (最常用)：<ul><li>基类的 <code>public</code> 成员在派生类中仍为 <code>public</code>。</li><li>基类的 <code>protected</code> 成员在派生类中仍为 <code>protected</code>。</li><li>基类的 <code>private</code> 成员在派生类中不可直接访问 (但被继承了)。</li></ul></li><li><strong><code>protected</code> 继承</strong>：<ul><li>基类的 <code>public</code> 和 <code>protected</code> 成员在派生类中都变为 <code>protected</code>。</li><li>基类的 <code>private</code> 成员在派生类中不可直接访问。</li></ul></li><li><strong><code>private</code> 继承</strong>：<ul><li>基类的 <code>public</code> 和 <code>protected</code> 成员在派生类中都变为 <code>private</code>。</li><li>基类的 <code>private</code> 成员在派生类中不可直接访问。</li></ul></li><li>如果省略继承方式，默认为 <code>private</code> (如果用 <code>class</code> 定义派生类) 或 <code>public</code> (如果用 <code>struct</code> 定义派生类)。</li></ul></li></ul><h4 id="2-派生类的构造函数与析构函数"><a href="#2-派生类的构造函数与析构函数" class="headerlink" title="2. 派生类的构造函数与析构函数"></a>2. 派生类的构造函数与析构函数</h4><ul><li><strong>构造函数</strong>：<ul><li>派生类不能继承基类的构造函数。</li><li>派生类构造函数必须负责初始化其基类部分的成员和派生类新增的成员。</li><li><strong>调用基类构造函数</strong>：在派生类构造函数的<strong>初始化列表</strong>中显式调用基类的构造函数。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Base</span> &#123;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-built_in">Base</span>(<span class="hljs-type">int</span> b_val) : <span class="hljs-built_in">b_data</span>(b_val) &#123; cout &lt;&lt; <span class="hljs-string">&quot;Base constructor&quot;</span> &lt;&lt; endl; &#125;<br><span class="hljs-keyword">private</span>:<br>    <span class="hljs-type">int</span> b_data;<br>&#125;;<br><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Derived</span> : <span class="hljs-keyword">public</span> Base &#123;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-built_in">Derived</span>(<span class="hljs-type">int</span> b_val, <span class="hljs-type">int</span> d_val) : <span class="hljs-built_in">Base</span>(b_val), <span class="hljs-built_in">d_data</span>(d_val) &#123; <span class="hljs-comment">// 调用基类构造函数</span><br>        cout &lt;&lt; <span class="hljs-string">&quot;Derived constructor&quot;</span> &lt;&lt; endl;<br>    &#125;<br><span class="hljs-keyword">private</span>:<br>    <span class="hljs-type">int</span> d_data;<br>&#125;;<br><span class="hljs-comment">// Derived d(10, 20);</span><br></code></pre></td></tr></table></figure></li><li><strong>构造顺序</strong>：<ol><li>(虚)基类构造函数 (按继承声明顺序，若有虚继承则特殊处理)。</li><li>直接基类构造函数 (按继承声明顺序)。</li><li>类成员对象构造函数 (按在类中声明顺序)。</li><li>派生类构造函数体。</li></ol></li><li>如果派生类构造函数初始化列表中没有显式调用基类构造函数，编译器会尝试调用基类的默认（无参）构造函数。如果基类没有默认构造函数，则编译错误。</li></ul></li><li><strong>析构函数</strong>：<ul><li>派生类不能继承基类的析构函数，但会自动调用。</li><li><strong>析构顺序</strong>与构造顺序相反：<ol><li>派生类析构函数体。</li><li>类成员对象析构函数 (按声明逆序)。</li><li>直接基类析构函数 (按继承声明逆序)。</li><li>(虚)基类析构函数。</li></ol></li><li><strong>重要</strong>：如果基类指针可能指向派生类对象，并且需要通过基类指针 <code>delete</code> 对象，则基类的析构函数<strong>必须声明为虚析构函数 (<code>virtual ~Base()</code>)</strong>，以确保正确调用派生类的析构函数，防止资源泄漏。</li></ul></li></ul><h4 id="3-友元与静态成员的继承"><a href="#3-友元与静态成员的继承" class="headerlink" title="3. 友元与静态成员的继承"></a>3. 友元与静态成员的继承</h4><ul><li><strong>友元关系不能被继承</strong>。基类的友元不是派生类的友元，派生类的友元也不是基类的友元。</li><li><strong>静态成员被继承</strong>。基类的静态成员（如果可访问）成为派生类的一部分，并且仍然是静态的，被所有基类和派生类对象共享。</li></ul><h4 id="4-派生类与基类的赋值兼容性-Type-Compatibility"><a href="#4-派生类与基类的赋值兼容性-Type-Compatibility" class="headerlink" title="4. 派生类与基类的赋值兼容性 (Type Compatibility)"></a>4. 派生类与基类的赋值兼容性 (Type Compatibility)</h4><ul><li><strong>公有继承 (<code>public</code>) 体现 “is-a” 关系</strong>，使得派生类对象可以被当作基类对象使用。</li><li><strong>赋值兼容规则</strong>：<ol><li><strong>派生类对象可以赋值给基类对象（对象切片 Object Slicing）</strong>：<figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs cpp">Base b_obj;<br>Derived d_obj;<br>b_obj = d_obj; <span class="hljs-comment">// 合法，但只复制基类部分，派生类特有成员丢失</span><br></code></pre></td></tr></table></figure></li><li><strong>派生类对象的地址可以赋值给基类指针</strong>：<figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs cpp">Base *b_ptr;<br>Derived d_obj;<br>b_ptr = &amp;d_obj; <span class="hljs-comment">// 合法，b_ptr 指向 d_obj 的基类部分</span><br><span class="hljs-comment">// b_ptr-&gt;derived_member; // 错误，只能访问基类成员</span><br></code></pre></td></tr></table></figure></li><li><strong>派生类对象可以初始化基类的引用</strong>：<figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs cpp">Derived d_obj;<br>Base &amp;b_ref = d_obj; <span class="hljs-comment">// 合法，b_ref 引用 d_obj 的基类部分</span><br></code></pre></td></tr></table></figure></li></ol></li><li>反向操作（基类对象赋值给派生类对象、基类指针指向派生类等）通常不被允许或不安全，除非进行显式类型转换且确保类型正确。</li></ul><h4 id="5-同名隐藏-Name-Hiding-Overriding-Data-Members"><a href="#5-同名隐藏-Name-Hiding-Overriding-Data-Members" class="headerlink" title="5. 同名隐藏 (Name Hiding &#x2F; Overriding Data Members)"></a>5. 同名隐藏 (Name Hiding &#x2F; Overriding Data Members)</h4><ul><li>如果派生类定义了一个与基类成员（数据成员或成员函数）同名的成员，则派生类中的成员会<strong>隐藏 (hide)</strong> 基类中的同名成员。</li><li>在派生类作用域内，直接使用该名称访问的是派生类的成员。</li><li>要访问被隐藏的基类成员，需要使用作用域解析运算符：<code>&lt;基类名&gt;::&lt;成员名&gt;</code>。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Base</span> &#123; <span class="hljs-keyword">public</span>: <span class="hljs-type">int</span> x = <span class="hljs-number">1</span>; <span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">print</span><span class="hljs-params">()</span> </span>&#123; cout &lt;&lt; <span class="hljs-string">&quot;Base x: &quot;</span> &lt;&lt; x &lt;&lt; endl; &#125; &#125;;<br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Derived</span> : <span class="hljs-keyword">public</span> Base &#123; <span class="hljs-keyword">public</span>: <span class="hljs-type">int</span> x = <span class="hljs-number">2</span>; <span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">print</span><span class="hljs-params">()</span> </span>&#123; cout &lt;&lt; <span class="hljs-string">&quot;Derived x: &quot;</span> &lt;&lt; x &lt;&lt; endl; &#125; &#125;;<br><span class="hljs-comment">// Derived d;</span><br><span class="hljs-comment">// d.x; // 访问 Derived::x (值为2)</span><br><span class="hljs-comment">// d.Base::x; // 访问 Base::x (值为1)</span><br><span class="hljs-comment">// d.print(); // 调用 Derived::print()</span><br><span class="hljs-comment">// d.Base::print(); // 调用 Base::print()</span><br></code></pre></td></tr></table></figure></li></ul><h3 id="三、虚基类与虚拟继承"><a href="#三、虚基类与虚拟继承" class="headerlink" title="三、虚基类与虚拟继承"></a>三、虚基类与虚拟继承</h3><h4 id="1-二义性问题-Ambiguity"><a href="#1-二义性问题-Ambiguity" class="headerlink" title="1. 二义性问题 (Ambiguity)"></a>1. 二义性问题 (Ambiguity)</h4><ul><li><strong>多重继承中的二义性</strong>：如果一个派生类从多个基类继承了同名成员，直接访问该成员会导致二义性。需要用类名限定。</li><li><strong>菱形继承 (Diamond Problem)</strong>：  <figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs css">  <span class="hljs-selector-tag">A</span><br> / \<br><span class="hljs-selector-tag">B</span>   C<br> \ /<br>  D<br></code></pre></td></tr></table></figure>  如果类 B 和类 C 都继承自类 A，然后类 D 同时继承自类 B 和类 C，那么类 D 的对象中会包含<strong>两份</strong>类 A 的成员副本（一份来自 B，一份来自 C）。这会导致访问 A 的成员时产生二义性，并且浪费内存。</li></ul><h4 id="2-虚基类与虚拟继承-Virtual-Base-Class-Virtual-Inheritance"><a href="#2-虚基类与虚拟继承-Virtual-Base-Class-Virtual-Inheritance" class="headerlink" title="2. 虚基类与虚拟继承 (Virtual Base Class &amp; Virtual Inheritance)"></a>2. 虚基类与虚拟继承 (Virtual Base Class &amp; Virtual Inheritance)</h4><ul><li>用于解决菱形继承问题。</li><li>当一个基类被声明为虚基类时，在后续的派生类中，无论该虚基类通过多少条路径被间接继承，派生类对象中都只包含该虚基类的一个共享副本。</li><li><strong>声明虚基类</strong>：在派生类继承基类时使用 <code>virtual</code> 关键字。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">A</span> &#123; <span class="hljs-comment">/* ... */</span> &#125;;<br><span class="hljs-keyword">class</span> <span class="hljs-title class_">B</span> : <span class="hljs-keyword">virtual</span> <span class="hljs-keyword">public</span> A &#123; <span class="hljs-comment">/* ... */</span> &#125;; <span class="hljs-comment">// B虚拟继承A</span><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">C</span> : <span class="hljs-keyword">virtual</span> <span class="hljs-keyword">public</span> A &#123; <span class="hljs-comment">/* ... */</span> &#125;; <span class="hljs-comment">// C虚拟继承A</span><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">D</span> : <span class="hljs-keyword">public</span> B, <span class="hljs-keyword">public</span> C &#123; <span class="hljs-comment">/* ... */</span> &#125;; <span class="hljs-comment">// D中只有一份A的副本</span><br></code></pre></td></tr></table></figure></li><li><strong>虚基类的构造</strong>：虚基类的构造函数由<strong>最终派生类 (Most Derived Class)</strong> 的构造函数负责调用。中间基类的构造函数初始化列表中对虚基类的调用会被忽略（除非该中间基类本身就是最终派生类）。</li></ul><h3 id="四、多态性与虚函数"><a href="#四、多态性与虚函数" class="headerlink" title="四、多态性与虚函数"></a>四、多态性与虚函数</h3><h4 id="1-多态性-Polymorphism"><a href="#1-多态性-Polymorphism" class="headerlink" title="1. 多态性 (Polymorphism)"></a>1. 多态性 (Polymorphism)</h4><ul><li>“多种形态”。指同样的消息（函数调用）被不同类型的对象接收时导致不同的行为。</li><li><strong>静态多态 (Compile-time Polymorphism &#x2F; Early Binding)</strong>：<ul><li>通过函数重载 (Function Overloading) 和运算符重载 (Operator Overloading) 实现。</li><li>在编译时确定调用哪个函数。</li></ul></li><li><strong>动态多态 (Run-time Polymorphism &#x2F; Late Binding)</strong>：<ul><li>通过虚函数 (Virtual Functions) 和继承实现。</li><li>在运行时根据对象的实际类型确定调用哪个函数。</li><li>是面向对象编程的核心特性之一。</li></ul></li></ul><h4 id="2-虚函数-Virtual-Function"><a href="#2-虚函数-Virtual-Function" class="headerlink" title="2. 虚函数 (Virtual Function)"></a>2. 虚函数 (Virtual Function)</h4><ul><li>在基类中使用 <code>virtual</code> 关键字声明的成员函数。</li><li>当派生类重写 (override) 基类的虚函数时（函数名、参数列表、返回类型、<code>const</code>属性都相同），通过基类指针或引用调用该虚函数，会根据指针或引用实际指向的对象的类型来调用相应的版本。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Shape</span> &#123;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-function"><span class="hljs-keyword">virtual</span> <span class="hljs-type">void</span> <span class="hljs-title">draw</span><span class="hljs-params">()</span> </span>&#123; cout &lt;&lt; <span class="hljs-string">&quot;Drawing a generic shape.&quot;</span> &lt;&lt; endl; &#125;<br>    <span class="hljs-keyword">virtual</span> ~<span class="hljs-built_in">Shape</span>() &#123;&#125; <span class="hljs-comment">// 虚析构函数</span><br>&#125;;<br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Circle</span> : <span class="hljs-keyword">public</span> Shape &#123;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">draw</span><span class="hljs-params">()</span> <span class="hljs-keyword">override</span> </span>&#123; cout &lt;&lt; <span class="hljs-string">&quot;Drawing a circle.&quot;</span> &lt;&lt; endl; &#125; <span class="hljs-comment">// C++11 override 关键字</span><br>&#125;;<br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Square</span> : <span class="hljs-keyword">public</span> Shape &#123;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">draw</span><span class="hljs-params">()</span> <span class="hljs-keyword">override</span> </span>&#123; cout &lt;&lt; <span class="hljs-string">&quot;Drawing a square.&quot;</span> &lt;&lt; endl; &#125;<br>&#125;;<br><br><span class="hljs-comment">// void showDrawing(Shape *s) &#123; s-&gt;draw(); &#125;</span><br><span class="hljs-comment">// Circle c; Square sq;</span><br><span class="hljs-comment">// showDrawing(&amp;c);  // 输出 &quot;Drawing a circle.&quot;</span><br><span class="hljs-comment">// showDrawing(&amp;sq); // 输出 &quot;Drawing a square.&quot;</span><br></code></pre></td></tr></table></figure></li><li><strong><code>override</code> 关键字 (C++11)</strong>：在派生类中重写虚函数时使用，编译器会检查基类是否存在对应的可重写的虚函数，有助于防止错误。</li><li><strong><code>final</code> 关键字 (C++11)</strong>：<ul><li>用于虚函数：表示该虚函数不能在更深层次的派生类中被重写。</li><li>用于类：表示该类不能被继承。</li></ul></li><li>构造函数不能是虚函数。</li><li>析构函数通常应声明为虚函数，特别是当基类指针可能用于删除派生类对象时。</li></ul><h4 id="3-纯虚函数与抽象基类-Pure-Virtual-Function-Abstract-Base-Class"><a href="#3-纯虚函数与抽象基类-Pure-Virtual-Function-Abstract-Base-Class" class="headerlink" title="3. 纯虚函数与抽象基类 (Pure Virtual Function &amp; Abstract Base Class)"></a>3. 纯虚函数与抽象基类 (Pure Virtual Function &amp; Abstract Base Class)</h4><ul><li><strong>纯虚函数</strong>：在基类中声明但没有定义的虚函数，其声明末尾加 <code>= 0;</code>。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">AbstractShape</span> &#123;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-function"><span class="hljs-keyword">virtual</span> <span class="hljs-type">void</span> <span class="hljs-title">calculateArea</span><span class="hljs-params">()</span> </span>= <span class="hljs-number">0</span>; <span class="hljs-comment">// 纯虚函数</span><br>    <span class="hljs-keyword">virtual</span> ~<span class="hljs-built_in">AbstractShape</span>() &#123;&#125;<br>&#125;;<br></code></pre></td></tr></table></figure></li><li><strong>抽象基类 (Abstract Base Class, ABC)</strong>：包含至少一个纯虚函数的类。<ul><li><strong>不能创建抽象基类的对象实例。</strong></li><li>主要用作接口，强制派生类实现纯虚函数。</li><li>如果派生类没有实现基类中的所有纯虚函数，那么该派生类仍然是抽象基类。</li><li>可以声明抽象基类的指针或引用，指向其具体派生类的对象。</li></ul></li></ul><h2 id="第九章-类模板与STL程序设计"><a href="#第九章-类模板与STL程序设计" class="headerlink" title="第九章 类模板与STL程序设计"></a>第九章 类模板与STL程序设计</h2><h3 id="一、函数模板"><a href="#一、函数模板" class="headerlink" title="一、函数模板"></a>一、函数模板</h3><h4 id="1-函数模板的基本概念"><a href="#1-函数模板的基本概念" class="headerlink" title="1. 函数模板的基本概念"></a>1. 函数模板的基本概念</h4><ul><li><strong>函数模板 (Function Template)</strong>：一个通用的函数描述，其中的数据类型使用类型参数（模板参数）表示。</li><li>编译器根据函数调用时提供的实参类型，自动推断模板参数的具体类型，并生成一个特定版本的函数实例（模板实例化）。</li><li>定义格式：  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">template</span> &lt;<span class="hljs-keyword">typename</span> &lt;类型参数名<span class="hljs-number">1</span>&gt;, <span class="hljs-keyword">typename</span> &lt;类型参数名<span class="hljs-number">2</span>&gt;, ...&gt; <span class="hljs-comment">// 或 class 代替 typename</span><br>&lt;返回类型&gt; &lt;函数名&gt;(&lt;参数列表&gt;) &#123;<br>    <span class="hljs-comment">// 函数体，可以使用类型参数名</span><br>&#125;<br>```cpp<br><span class="hljs-keyword">template</span> &lt;<span class="hljs-keyword">typename</span> T&gt;<br><span class="hljs-function">T <span class="hljs-title">maxVal</span><span class="hljs-params">(T a, T b)</span> </span>&#123;<br>    <span class="hljs-keyword">return</span> (a &gt; b) ? a : b;<br>&#125;<br><span class="hljs-comment">// int m = maxVal(10, 20);       // T 被推断为 int</span><br><span class="hljs-comment">// double d = maxVal(3.14, 2.71); // T 被推断为 double</span><br></code></pre></td></tr></table></figure></li><li>函数模板调用时不进行实参到形参类型的自动转换（除非显式指定模板参数）。</li></ul><h4 id="2-函数模板的特例-Specialization"><a href="#2-函数模板的特例-Specialization" class="headerlink" title="2. 函数模板的特例 (Specialization)"></a>2. 函数模板的特例 (Specialization)</h4><ul><li>为特定的数据类型提供一个不同于通用模板的特殊实现。</li><li>格式：<code>template &lt;&gt; &lt;返回类型&gt; &lt;函数名&gt;&lt;&lt;特化类型&gt;&gt;(&lt;参数列表&gt;) &#123; ... &#125;</code>  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-comment">// 通用模板</span><br><span class="hljs-keyword">template</span> &lt;<span class="hljs-keyword">typename</span> T&gt;<br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">compare</span><span class="hljs-params">(T a, T b)</span> </span>&#123; <span class="hljs-keyword">return</span> a &gt; b ? <span class="hljs-number">1</span> : (a &lt; b ? <span class="hljs-number">-1</span> : <span class="hljs-number">0</span>); &#125;<br><br><span class="hljs-comment">// 针对 const char* 的特化版本</span><br><span class="hljs-keyword">template</span> &lt;&gt;<br><span class="hljs-type">int</span> <span class="hljs-built_in">compare</span>&lt;<span class="hljs-type">const</span> <span class="hljs-type">char</span>*&gt;(<span class="hljs-type">const</span> <span class="hljs-type">char</span>* a, <span class="hljs-type">const</span> <span class="hljs-type">char</span>* b) &#123;<br>    <span class="hljs-keyword">return</span> <span class="hljs-built_in">strcmp</span>(a, b);<br>&#125;<br></code></pre></td></tr></table></figure></li></ul><h4 id="3-函数模板的重载-Overloading"><a href="#3-函数模板的重载-Overloading" class="headerlink" title="3. 函数模板的重载 (Overloading)"></a>3. 函数模板的重载 (Overloading)</h4><ul><li>可以定义多个同名函数模板，只要它们的模板参数列表或函数参数列表不同。</li><li>也可以定义同名的普通函数。调用时，编译器优先匹配普通函数，其次是更特化的模板，最后是通用模板。</li></ul><h3 id="二、类模板的基本概念"><a href="#二、类模板的基本概念" class="headerlink" title="二、类模板的基本概念"></a>二、类模板的基本概念</h3><h4 id="1-类模板的定义"><a href="#1-类模板的定义" class="headerlink" title="1. 类模板的定义"></a>1. 类模板的定义</h4><ul><li><strong>类模板 (Class Template)</strong>：一个通用的类描述，允许类的数据成员、成员函数的参数或返回类型使用类型参数。</li><li>定义格式：  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">template</span> &lt;<span class="hljs-keyword">typename</span> &lt;类型参数名<span class="hljs-number">1</span>&gt;, <span class="hljs-type">int</span> &lt;非类型参数名<span class="hljs-number">1</span>&gt;, ...&gt; <span class="hljs-comment">// 可以有类型参数和非类型参数</span><br><span class="hljs-keyword">class</span> &lt;类模板名&gt; &#123;<br>    <span class="hljs-comment">// 类成员，可以使用模板参数</span><br>&#125;;<br></code></pre></td></tr></table></figure><ul><li><strong>类型参数 (Type Parameter)</strong>：用 <code>typename</code> 或 <code>class</code> 声明，代表一种数据类型。</li><li><strong>非类型参数 (Non-type Parameter)</strong>：如 <code>int N</code>，代表一个常量值，其实参必须是常量表达式。</li></ul></li></ul><h4 id="2-类模板的实例化"><a href="#2-类模板的实例化" class="headerlink" title="2. 类模板的实例化"></a>2. 类模板的实例化</h4><ul><li>使用类模板创建对象时，必须显式指定所有模板参数的具体类型或值。</li><li>格式：<code>&lt;类模板名&gt;&lt;&lt;实参列表&gt;&gt; &lt;对象名&gt;;</code>  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">template</span> &lt;<span class="hljs-keyword">typename</span> T, <span class="hljs-type">int</span> SIZE&gt;<br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Array</span> &#123;<br>    T data[SIZE];<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-comment">// ...</span><br>&#125;;<br><span class="hljs-comment">// Array&lt;int, 10&gt; intArray;   // T 为 int, SIZE 为 10</span><br><span class="hljs-comment">// Array&lt;double, 5&gt; doubleArray; // T 为 double, SIZE 为 5</span><br></code></pre></td></tr></table></figure></li></ul><h4 id="3-类模板的成员函数"><a href="#3-类模板的成员函数" class="headerlink" title="3. 类模板的成员函数"></a>3. 类模板的成员函数</h4><ul><li>可以在类模板定义内定义，也可以在类模板定义外定义。</li><li><strong>类外定义格式</strong>：  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">template</span> &lt;<span class="hljs-keyword">typename</span> T, <span class="hljs-type">int</span> SIZE&gt;<br><span class="hljs-type">void</span> Array&lt;T, SIZE&gt;::<span class="hljs-built_in">someFunction</span>() &#123;<br>    <span class="hljs-comment">// ...</span><br>&#125;<br></code></pre></td></tr></table></figure>  每一处使用类模板名的地方都需要带上模板参数列表 <code>&lt;T, SIZE&gt;</code>。</li></ul><h4 id="4-类模板的静态成员与友元"><a href="#4-类模板的静态成员与友元" class="headerlink" title="4. 类模板的静态成员与友元"></a>4. 类模板的静态成员与友元</h4><ul><li><strong>静态成员</strong>：每个实例化后的类模板都有其自己的一份静态成员。<ul><li>静态数据成员的定义和初始化仍在类外，但需要 <code>template &lt;...&gt;</code> 前缀和类名后的 <code>&lt;...&gt;</code>。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">template</span> &lt;<span class="hljs-keyword">typename</span> T&gt;<br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Counter</span> &#123;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-type">static</span> <span class="hljs-type">int</span> count;<br>    <span class="hljs-built_in">Counter</span>() &#123; count++; &#125;<br>&#125;;<br><span class="hljs-keyword">template</span> &lt;<span class="hljs-keyword">typename</span> T&gt; <span class="hljs-comment">// 不能少</span><br><span class="hljs-type">int</span> Counter&lt;T&gt;::count = <span class="hljs-number">0</span>; <span class="hljs-comment">// 初始化</span><br><span class="hljs-comment">// Counter&lt;int&gt; c1; Counter&lt;double&gt; c2; // Counter&lt;int&gt;::count 和 Counter&lt;double&gt;::count 是不同的</span><br></code></pre></td></tr></table></figure></li></ul></li><li><strong>友元</strong>：<ul><li>普通函数&#x2F;类作友元：是所有实例化类的友元。</li><li>函数模板&#x2F;类模板作友元：情况较复杂，可以指定特定实例化为友元。</li></ul></li></ul><h4 id="5-类模板的特例版本-Specialization"><a href="#5-类模板的特例版本-Specialization" class="headerlink" title="5. 类模板的特例版本 (Specialization)"></a>5. 类模板的特例版本 (Specialization)</h4><ul><li>为特定的模板参数组合提供一个完全不同的类定义。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-comment">// 通用类模板</span><br><span class="hljs-keyword">template</span> &lt;<span class="hljs-keyword">typename</span> T&gt; <span class="hljs-keyword">class</span> <span class="hljs-title class_">Storage</span> &#123; T data; <span class="hljs-comment">/* ... */</span> &#125;;<br><br><span class="hljs-comment">// 针对 bool 的特化版本</span><br><span class="hljs-keyword">template</span> &lt;&gt; <span class="hljs-keyword">class</span> <span class="hljs-title class_">Storage</span>&lt;<span class="hljs-type">bool</span>&gt; &#123; <span class="hljs-type">unsigned</span> <span class="hljs-type">char</span> bits; <span class="hljs-comment">/* ... 特殊实现 ... */</span> &#125;;<br></code></pre></td></tr></table></figure></li><li>也可以只特化部分模板参数（偏特化，Partial Specialization）。</li></ul><h3 id="三、类模板的继承和派生"><a href="#三、类模板的继承和派生" class="headerlink" title="三、类模板的继承和派生"></a>三、类模板的继承和派生</h3><ul><li>类模板可以参与继承关系。<ol><li><strong>普通类派生类模板</strong>：基类是普通类，派生类是类模板。</li><li><strong>类模板派生普通类</strong>：基类是类模板的特定实例化，派生类是普通类。<figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">template</span> &lt;<span class="hljs-keyword">typename</span> T&gt; <span class="hljs-keyword">class</span> <span class="hljs-title class_">BaseT</span> &#123; T data; &#125;;<br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Derived</span> : <span class="hljs-keyword">public</span> BaseT&lt;<span class="hljs-type">int</span>&gt; &#123; <span class="hljs-comment">/* ... */</span> &#125;; <span class="hljs-comment">// 派生自 BaseT&lt;int&gt;</span><br></code></pre></td></tr></table></figure></li><li><strong>类模板派生类模板</strong>：<figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">template</span> &lt;<span class="hljs-keyword">typename</span> T&gt; <span class="hljs-keyword">class</span> <span class="hljs-title class_">BaseT</span> &#123; <span class="hljs-comment">/* ... */</span> &#125;;<br><span class="hljs-keyword">template</span> &lt;<span class="hljs-keyword">typename</span> U&gt; <span class="hljs-keyword">class</span> <span class="hljs-title class_">DerivedT</span> : <span class="hljs-keyword">public</span> BaseT&lt;U&gt; &#123; <span class="hljs-comment">/* ... */</span> &#125;;<br><span class="hljs-keyword">template</span> &lt;<span class="hljs-keyword">typename</span> T1, <span class="hljs-keyword">typename</span> T2&gt; <span class="hljs-keyword">class</span> <span class="hljs-title class_">DerivedMultiT</span> : <span class="hljs-keyword">public</span> BaseT&lt;T1&gt; &#123; T2 other_data; &#125;;<br></code></pre></td></tr></table></figure></li></ol></li></ul><h3 id="四、标准模板库-STL-程序设计"><a href="#四、标准模板库-STL-程序设计" class="headerlink" title="四、标准模板库 (STL) 程序设计"></a>四、标准模板库 (STL) 程序设计</h3><h4 id="1-STL基本概念"><a href="#1-STL基本概念" class="headerlink" title="1. STL基本概念"></a>1. STL基本概念</h4><ul><li><strong>STL (Standard Template Library)</strong>：C++标准库的一部分，提供了一套通用的模板类和函数，用于实现常用的数据结构和算法。</li><li><strong>核心组件</strong>：<ul><li><strong>容器 (Containers)</strong>：存储数据的对象。</li><li><strong>迭代器 (Iterators)</strong>：访问容器中元素的通用机制，类似指针。</li><li><strong>算法 (Algorithms)</strong>：处理容器中数据的通用函数（如排序、查找）。</li><li><strong>仿函数&#x2F;函数对象 (Functors&#x2F;Function Objects)</strong>：行为类似函数的对象。</li><li><strong>适配器 (Adapters)</strong>：修改容器、迭代器或仿函数的接口。</li><li><strong>分配器 (Allocators)</strong>：管理内存。</li></ul></li></ul><h4 id="2-容器-Containers"><a href="#2-容器-Containers" class="headerlink" title="2. 容器 (Containers)"></a>2. 容器 (Containers)</h4><ul><li><strong>顺序容器 (Sequence Containers)</strong>：元素按线性顺序存储。<ul><li><code>std::vector</code>：动态数组。尾部插入&#x2F;删除快，随机访问快，中间插入&#x2F;删除慢。头文件 <code>&lt;vector&gt;</code>。</li><li><code>std::list</code>：双向链表。任意位置插入&#x2F;删除快，随机访问慢。头文件 <code>&lt;list&gt;</code>。</li><li><code>std::deque</code> (Double-ended Queue)：双端队列。头尾插入&#x2F;删除快，随机访问较快。头文件 <code>&lt;deque&gt;</code>。</li><li><code>std::array</code> (C++11)：固定大小数组。性能与内置数组相当。头文件 <code>&lt;array&gt;</code>。</li><li><code>std::forward_list</code> (C++11)：单向链表。头文件 <code>&lt;forward_list&gt;</code>。</li></ul></li><li><strong>关联容器 (Associative Containers)</strong>：元素按键值有序存储（通常基于红黑树）。<ul><li><code>std::set</code>：存储唯一的、有序的键。头文件 <code>&lt;set&gt;</code>。</li><li><code>std::multiset</code>：存储可重复的、有序的键。头文件 <code>&lt;set&gt;</code>。</li><li><code>std::map</code>：存储唯一的键值对 (key-value pair)，键有序。头文件 <code>&lt;map&gt;</code>。</li><li><code>std::multimap</code>：存储可重复的键值对，键有序。头文件 <code>&lt;map&gt;</code>。</li></ul></li><li><strong>无序关联容器 (Unordered Associative Containers) (C++11)</strong>：元素无序存储（基于哈希表）。查找、插入、删除平均时间复杂度O(1)。<ul><li><code>std::unordered_set</code>, <code>std::unordered_multiset</code>：头文件 <code>&lt;unordered_set&gt;</code>。</li><li><code>std::unordered_map</code>, <code>std::unordered_multimap</code>：头文件 <code>&lt;unordered_map&gt;</code>。</li></ul></li><li><strong>容器适配器 (Container Adapters)</strong>：提供特定接口的容器，基于底层顺序容器实现。<ul><li><code>std::stack</code>：栈 (LIFO)。默认基于 <code>std::deque</code>。头文件 <code>&lt;stack&gt;</code>。</li><li><code>std::queue</code>：队列 (FIFO)。默认基于 <code>std::deque</code>。头文件 <code>&lt;queue&gt;</code>。</li><li><code>std::priority_queue</code>：优先队列。默认基于 <code>std::vector</code> 和 <code>std::less</code> (大顶堆)。头文件 <code>&lt;queue&gt;</code>。</li></ul></li></ul><h4 id="3-迭代器-Iterators"><a href="#3-迭代器-Iterators" class="headerlink" title="3. 迭代器 (Iterators)"></a>3. 迭代器 (Iterators)</h4><ul><li>提供统一访问容器元素的方式，行为类似指针。</li><li><strong>主要操作</strong>：<ul><li><code>*it</code>：解引用，获取迭代器指向的元素。</li><li><code>it-&gt;member</code>：访问元素成员 (如果元素是对象)。</li><li><code>++it</code>, <code>it++</code>：移动到下一个元素。</li><li><code>--it</code>, <code>it--</code>：移动到上一个元素 (双向和随机访问迭代器)。</li><li><code>it1 == it2</code>, <code>it1 != it2</code>：比较迭代器。</li><li><code>it + n</code>, <code>it - n</code>, <code>it1 - it2</code>：(仅随机访问迭代器)。</li></ul></li><li><strong>容器成员函数获取迭代器</strong>：<ul><li><code>container.begin()</code>：指向第一个元素的迭代器。</li><li><code>container.end()</code>：指向末尾元素之后位置的迭代器（哨兵）。</li><li><code>container.rbegin()</code>, <code>container.rend()</code>：反向迭代器。</li><li><code>container.cbegin()</code>, <code>container.cend()</code>：常量迭代器 (C++11)。</li></ul></li><li><strong>迭代器类别</strong>：<ul><li>输入迭代器 (Input Iterator)：只读，单向，只能前进。</li><li>输出迭代器 (Output Iterator)：只写，单向，只能前进。</li><li>前向迭代器 (Forward Iterator)：可读写，单向，只能前进。</li><li>双向迭代器 (Bidirectional Iterator)：可读写，双向，可前进后退。</li><li>随机访问迭代器 (Random Access Iterator)：可读写，可随机访问 (如 <code>it + n</code>)。</li></ul></li></ul><h4 id="4-算法-Algorithms"><a href="#4-算法-Algorithms" class="headerlink" title="4. 算法 (Algorithms)"></a>4. 算法 (Algorithms)</h4><ul><li>定义在 <code>&lt;algorithm&gt;</code> 和 <code>&lt;numeric&gt;</code> 等头文件中。</li><li>通常以迭代器范围 <code>[first, last)</code> 作为参数。</li><li><strong>常用算法示例</strong>：<ul><li>非修改序列操作：<code>for_each</code>, <code>find</code>, <code>find_if</code>, <code>count</code>, <code>count_if</code>, <code>equal</code>, <code>search</code>。</li><li>修改序列操作：<code>copy</code>, <code>move</code>, <code>transform</code>, <code>replace</code>, <code>fill</code>, <code>remove</code>, <code>unique</code>, <code>reverse</code>, <code>rotate</code>, <code>random_shuffle</code> (C++17废弃,用<code>shuffle</code>)。</li><li>排序和相关操作：<code>sort</code>, <code>stable_sort</code>, <code>partial_sort</code>, <code>nth_element</code>, <code>binary_search</code>, <code>lower_bound</code>, <code>upper_bound</code>, <code>merge</code>。</li><li>数值操作 (<code>&lt;numeric&gt;</code>)：<code>accumulate</code>, <code>inner_product</code>, <code>partial_sum</code>, <code>adjacent_difference</code>。</li></ul></li></ul><h2 id="第十章-输入输出流"><a href="#第十章-输入输出流" class="headerlink" title="第十章 输入输出流"></a>第十章 输入输出流</h2><h3 id="一、C-流类库简介"><a href="#一、C-流类库简介" class="headerlink" title="一、C++流类库简介"></a>一、C++流类库简介</h3><h4 id="1-文件与流的概念"><a href="#1-文件与流的概念" class="headerlink" title="1. 文件与流的概念"></a>1. 文件与流的概念</h4><ul><li><strong>文件 (File)</strong>：物理概念，外部存储介质上信息的集合（如磁盘文件、键盘、显示器）。</li><li><strong>流 (Stream)</strong>：逻辑概念，C++对I&#x2F;O设备的抽象。代表程序与设备之间数据流动的通道。<ul><li>输入流：数据从设备流向程序。</li><li>输出流：数据从程序流向设备。</li></ul></li></ul><h4 id="2-C-流类库的特点"><a href="#2-C-流类库的特点" class="headerlink" title="2. C++流类库的特点"></a>2. C++流类库的特点</h4><ul><li><strong>类型安全 (Type Safe)</strong>：通过重载，编译器可以检查数据类型。</li><li><strong>易于扩充</strong>：可以为自定义类型重载 <code>&lt;&lt;</code> 和 <code>&gt;&gt;</code>。</li><li><strong>统一接口</strong>：对不同设备的I&#x2F;O操作使用相似的接口。</li></ul><h4 id="3-基本流类-头文件"><a href="#3-基本流类-头文件" class="headerlink" title="3. 基本流类 (头文件 &lt;iostream&gt;)"></a>3. 基本流类 (头文件 <code>&lt;iostream&gt;</code>)</h4><ul><li><code>ios_base</code>：流类的基类，提供格式化、状态等基础功能（C++标准中 <code>ios</code> 的一部分功能移至此）。</li><li><code>ios</code>：继承自 <code>ios_base</code>，是输入输出流的基类。</li><li><code>istream</code>：输入流类，支持提取运算符 <code>&gt;&gt;</code>。</li><li><code>ostream</code>：输出流类，支持插入运算符 <code>&lt;&lt;</code>。</li><li><code>iostream</code>：同时继承 <code>istream</code> 和 <code>ostream</code>，支持双向I&#x2F;O。</li></ul><h4 id="4-预定义的流类对象"><a href="#4-预定义的流类对象" class="headerlink" title="4. 预定义的流类对象"></a>4. 预定义的流类对象</h4><ul><li><code>cin</code> (类型 <code>istream</code>)：标准输入流，通常连接到键盘。</li><li><code>cout</code> (类型 <code>ostream</code>)：标准输出流，通常连接到显示器。</li><li><code>cerr</code> (类型 <code>ostream</code>)：标准错误流，不带缓冲，通常连接到显示器。</li><li><code>clog</code> (类型 <code>ostream</code>)：标准日志流，带缓冲，通常连接到显示器。</li></ul><h4 id="5-文件流类-头文件"><a href="#5-文件流类-头文件" class="headerlink" title="5. 文件流类 (头文件 &lt;fstream&gt;)"></a>5. 文件流类 (头文件 <code>&lt;fstream&gt;</code>)</h4><ul><li><code>ifstream</code>：输入文件流，从 <code>istream</code> 派生，用于从磁盘文件读取数据。</li><li><code>ofstream</code>：输出文件流，从 <code>ostream</code> 派生，用于向磁盘文件写入数据。</li><li><code>fstream</code>：双向文件流，从 <code>iostream</code> 派生，支持对磁盘文件进行读写操作。</li></ul><h3 id="二、插入与提取运算符重载"><a href="#二、插入与提取运算符重载" class="headerlink" title="二、插入与提取运算符重载"></a>二、插入与提取运算符重载</h3><ul><li><code>&lt;&lt;</code> (插入运算符) 和 <code>&gt;&gt;</code> (提取运算符) 已为标准数据类型预定义。</li><li>为自定义类重载这两个运算符，通常作为友元函数，以便访问类的私有成员，并且使流对象 (<code>cin</code>&#x2F;<code>cout</code> 或文件流对象) 作为第一个参数。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">class</span> <span class="hljs-title class_">Complex</span> &#123; <span class="hljs-comment">/* ... */</span><br>    <span class="hljs-keyword">friend</span> ostream&amp; <span class="hljs-keyword">operator</span>&lt;&lt;(ostream&amp; os, <span class="hljs-type">const</span> Complex&amp; c);<br>    <span class="hljs-keyword">friend</span> istream&amp; <span class="hljs-keyword">operator</span>&gt;&gt;(istream&amp; is, Complex&amp; c);<br>&#125;;<br>ostream&amp; <span class="hljs-keyword">operator</span>&lt;&lt;(ostream&amp; os, <span class="hljs-type">const</span> Complex&amp; c) &#123;<br>    os &lt;&lt; c.real &lt;&lt; <span class="hljs-string">&quot; + &quot;</span> &lt;&lt; c.imag &lt;&lt; <span class="hljs-string">&quot;i&quot;</span>;<br>    <span class="hljs-keyword">return</span> os; <span class="hljs-comment">// 返回流对象的引用以支持链式操作</span><br>&#125;<br>istream&amp; <span class="hljs-keyword">operator</span>&gt;&gt;(istream&amp; is, Complex&amp; c) &#123;<br>    is &gt;&gt; c.real &gt;&gt; c.imag; <span class="hljs-comment">// 假设输入格式为 &quot;real imag&quot;</span><br>    <span class="hljs-keyword">return</span> is;<br>&#125;<br></code></pre></td></tr></table></figure></li></ul><h3 id="三、输入-输出格式控制"><a href="#三、输入-输出格式控制" class="headerlink" title="三、输入&#x2F;输出格式控制"></a>三、输入&#x2F;输出格式控制</h3><h4 id="1-格式控制函数-ios类的成员函数"><a href="#1-格式控制函数-ios类的成员函数" class="headerlink" title="1. 格式控制函数 (ios类的成员函数)"></a>1. 格式控制函数 (ios类的成员函数)</h4><ul><li><strong>格式控制标志字 (Format Flags)</strong>：<code>ios</code> 类中定义了一系列枚举常量（如 <code>ios::left</code>, <code>ios::right</code>, <code>ios::dec</code>, <code>ios::hex</code>, <code>ios::scientific</code>, <code>ios::fixed</code>, <code>ios::showpoint</code> 等）来控制输出格式。</li><li><strong><code>setf(long flags)</code></strong>: 设置指定的格式标志位。</li><li><strong><code>setf(long flags, long mask)</code></strong>: 先清除 <code>mask</code> 指定的位，再设置 <code>flags</code>。</li><li><strong><code>unsetf(long flags)</code></strong>: 清除指定的格式标志位。</li><li><strong><code>flags()</code></strong>: 返回当前的格式标志字。</li><li><strong><code>flags(long newflags)</code></strong>: 设置新的格式标志字，覆盖旧的。</li><li><strong><code>width(int w)</code></strong>: 设置下一次输出的最小字段宽度 (只对下一次输出有效)。</li><li><strong><code>precision(int p)</code></strong>: 设置浮点数的精度。<ul><li>默认：总有效数字位数。</li><li><code>ios::fixed</code> 或 <code>ios::scientific</code>：小数点后数字位数。</li></ul></li><li><strong><code>fill(char c)</code></strong>: 设置填充字符 (当输出宽度大于数据实际宽度时使用)。</li></ul><h4 id="2-格式控制符-Manipulators"><a href="#2-格式控制符-Manipulators" class="headerlink" title="2. 格式控制符 (Manipulators)"></a>2. 格式控制符 (Manipulators)</h4><ul><li>可以直接在 <code>&lt;&lt;</code> 或 <code>&gt;&gt;</code> 链式操作中使用。</li><li><strong>无参控制符 (定义在 <code>&lt;iostream&gt;</code>)</strong>:<ul><li><code>endl</code>: 输出换行符并刷新缓冲区。</li><li><code>ends</code>: 输出空字符 <code>\0</code>。</li><li><code>flush</code>: 刷新输出缓冲区。</li><li><code>ws</code>: 读取并丢弃前导空白字符 (用于输入流)。</li><li><code>dec</code>, <code>hex</code>, <code>oct</code>: 设置整数的基数为十进制、十六进制、八进制。</li></ul></li><li><strong>有参控制符 (定义在 <code>&lt;iomanip&gt;</code>)</strong>:<ul><li><code>setw(int n)</code>: 设置字段宽度。</li><li><code>setprecision(int n)</code>: 设置浮点数精度。</li><li><code>setfill(char c)</code>: 设置填充字符。</li><li><code>setiosflags(long flags)</code>: 设置格式标志。</li><li><code>resetiosflags(long flags)</code>: 清除格式标志。</li><li><code>setbase(int base)</code>: 设置整数基数 (8, 10, 16)。</li></ul></li></ul><h3 id="四、磁盘文件的输入与输出"><a href="#四、磁盘文件的输入与输出" class="headerlink" title="四、磁盘文件的输入与输出"></a>四、磁盘文件的输入与输出</h3><h4 id="1-文件的打开与关闭"><a href="#1-文件的打开与关闭" class="headerlink" title="1. 文件的打开与关闭"></a>1. 文件的打开与关闭</h4><ul><li><strong>打开文件 (Opening a File)</strong>：<ul><li>通过文件流对象的构造函数：  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function">ofstream <span class="hljs-title">outfile</span><span class="hljs-params">(<span class="hljs-string">&quot;output.txt&quot;</span>)</span></span>; <span class="hljs-comment">// 默认以 ios::out 打开</span><br><span class="hljs-function">ifstream <span class="hljs-title">infile</span><span class="hljs-params">(<span class="hljs-string">&quot;input.txt&quot;</span>)</span></span>;   <span class="hljs-comment">// 默认以 ios::in 打开</span><br></code></pre></td></tr></table></figure></li><li>通过 <code>open()</code> 成员函数：  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs cpp">ofstream outfile;<br>outfile.<span class="hljs-built_in">open</span>(<span class="hljs-string">&quot;output.txt&quot;</span>, ios::out | ios::app); <span class="hljs-comment">// 追加模式</span><br></code></pre></td></tr></table></figure></li><li><strong>文件打开模式 (File Modes - <code>ios</code> 枚举常量)</strong>：<ul><li><code>ios::in</code>: 以读方式打开。</li><li><code>ios::out</code>: 以写方式打开 (如果文件存在则清空，不存在则创建)。</li><li><code>ios::app</code>: 以追加方式打开 (在文件末尾写入)。</li><li><code>ios::ate</code>: 打开文件并立即定位到文件末尾。</li><li><code>ios::trunc</code>: 如果文件存在，则清空其内容。</li><li><code>ios::binary</code>: 以二进制模式打开文件。</li><li>可以用 <code>|</code> (位或) 组合多个模式。</li></ul></li></ul></li><li><strong>检查文件是否成功打开</strong>：  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function">ifstream <span class="hljs-title">infile</span><span class="hljs-params">(<span class="hljs-string">&quot;data.txt&quot;</span>)</span></span>;<br><span class="hljs-keyword">if</span> (!infile) &#123; <span class="hljs-comment">// 或者 if (infile.fail())</span><br>    cerr &lt;&lt; <span class="hljs-string">&quot;Error opening file!&quot;</span> &lt;&lt; endl;<br>&#125;<br></code></pre></td></tr></table></figure></li><li><strong>关闭文件 (Closing a File)</strong>：<ul><li>调用 <code>close()</code> 成员函数：<code>outfile.close();</code></li><li>文件流对象在析构时会自动关闭文件。显式关闭是个好习惯。</li></ul></li></ul><h4 id="2-使用插入与提取运算符进行文件读写"><a href="#2-使用插入与提取运算符进行文件读写" class="headerlink" title="2. 使用插入与提取运算符进行文件读写"></a>2. 使用插入与提取运算符进行文件读写</h4><ul><li>与 <code>cin</code>&#x2F;<code>cout</code> 类似，但操作对象是文件流对象。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function">ofstream <span class="hljs-title">ofs</span><span class="hljs-params">(<span class="hljs-string">&quot;numbers.txt&quot;</span>)</span></span>;<br><span class="hljs-type">int</span> x = <span class="hljs-number">10</span>; <span class="hljs-type">double</span> y = <span class="hljs-number">3.14</span>;<br>ofs &lt;&lt; x &lt;&lt; <span class="hljs-string">&quot; &quot;</span> &lt;&lt; y &lt;&lt; endl; <span class="hljs-comment">// 写入文件</span><br>ofs.<span class="hljs-built_in">close</span>();<br><br><span class="hljs-function">ifstream <span class="hljs-title">ifs</span><span class="hljs-params">(<span class="hljs-string">&quot;numbers.txt&quot;</span>)</span></span>;<br><span class="hljs-type">int</span> a; <span class="hljs-type">double</span> b;<br>ifs &gt;&gt; a &gt;&gt; b; <span class="hljs-comment">// 从文件读取</span><br>cout &lt;&lt; a &lt;&lt; <span class="hljs-string">&quot; &quot;</span> &lt;&lt; b &lt;&lt; endl;<br>ifs.<span class="hljs-built_in">close</span>();<br></code></pre></td></tr></table></figure></li><li>写入时，数据间需要分隔符 (如空格、换行) 才能被 <code>&gt;&gt;</code> 正确读取。</li></ul><h4 id="3-使用成员函数对文件流类对象进行操作"><a href="#3-使用成员函数对文件流类对象进行操作" class="headerlink" title="3. 使用成员函数对文件流类对象进行操作"></a>3. 使用成员函数对文件流类对象进行操作</h4><ul><li><strong>字符I&#x2F;O</strong>:<ul><li><code>istream&amp; get(char&amp; ch)</code>: 读取单个字符。</li><li><code>ostream&amp; put(char ch)</code>: 写入单个字符。</li><li><code>int get()</code>: 读取单个字符并返回其ASCII码，或EOF。</li></ul></li><li><strong>行I&#x2F;O</strong>:<ul><li><code>istream&amp; getline(char* buffer, streamsize n, char delim = &#39;\n&#39;)</code>: 读取一行到 <code>buffer</code>，最多 <code>n-1</code> 个字符，或遇到 <code>delim</code>。</li><li><code>istream&amp; getline(istream&amp; is, string&amp; str, char delim = &#39;\n&#39;)</code> (全局函数，常用于 <code>std::string</code>)</li></ul></li><li><strong>二进制I&#x2F;O (用于非文本数据，如结构体、对象)</strong>:<ul><li><code>istream&amp; read(char* buffer, streamsize n)</code>: 从文件读取 <code>n</code> 字节到 <code>buffer</code>。</li><li><code>ostream&amp; write(const char* buffer, streamsize n)</code>: 将 <code>buffer</code> 中的 <code>n</code> 字节写入文件。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">struct</span> <span class="hljs-title class_">Record</span> &#123; <span class="hljs-type">int</span> id; <span class="hljs-type">char</span> name[<span class="hljs-number">20</span>]; &#125;;<br>Record r = &#123;<span class="hljs-number">1</span>, <span class="hljs-string">&quot;Test&quot;</span>&#125;;<br><span class="hljs-function">ofstream <span class="hljs-title">obfs</span><span class="hljs-params">(<span class="hljs-string">&quot;record.dat&quot;</span>, ios::binary)</span></span>;<br>obfs.<span class="hljs-built_in">write</span>(<span class="hljs-built_in">reinterpret_cast</span>&lt;<span class="hljs-type">const</span> <span class="hljs-type">char</span>*&gt;(&amp;r), <span class="hljs-built_in">sizeof</span>(Record));<br>obfs.<span class="hljs-built_in">close</span>();<br><br>Record r_in;<br><span class="hljs-function">ifstream <span class="hljs-title">ibfs</span><span class="hljs-params">(<span class="hljs-string">&quot;record.dat&quot;</span>, ios::binary)</span></span>;<br>ibfs.<span class="hljs-built_in">read</span>(<span class="hljs-built_in">reinterpret_cast</span>&lt;<span class="hljs-type">char</span>*&gt;(&amp;r_in), <span class="hljs-built_in">sizeof</span>(Record));<br>ibfs.<span class="hljs-built_in">close</span>();<br></code></pre></td></tr></table></figure></li></ul></li></ul><h3 id="五、文本文件与二进制文件"><a href="#五、文本文件与二进制文件" class="headerlink" title="五、文本文件与二进制文件"></a>五、文本文件与二进制文件</h3><ul><li><strong>文本文件 (.txt)</strong>：<ul><li>内容是字符序列，人可读。</li><li>数值数据存储为其字符表示 (如整数 <code>123</code> 存为字符 <code>&#39;1&#39;</code>, <code>&#39;2&#39;</code>, <code>&#39;3&#39;</code>)。</li><li>在某些系统上，行尾符 (<code>\n</code>) 可能会被转换 (如 Windows 上的 <code>\r\n</code>)。</li><li>优点：兼容性好，易于查看和编辑。</li><li>缺点：数值数据存储效率低，读写可能涉及格式转换。</li></ul></li><li><strong>二进制文件 (.bin, .dat等)</strong>：<ul><li>内容是字节序列，直接存储数据的内存表示。</li><li>优点：数值数据存储效率高，读写速度快，不进行格式转换。</li><li>缺点：兼容性差，不易直接阅读。</li></ul></li><li>打开文件时用 <code>ios::binary</code> 模式指定二进制文件。</li></ul><h3 id="六、对数据文件的随机访问"><a href="#六、对数据文件的随机访问" class="headerlink" title="六、对数据文件的随机访问"></a>六、对数据文件的随机访问</h3><ul><li>允许直接跳转到文件中的任意位置进行读写。</li><li><strong>文件指针 (File Position Pointer)</strong>：每个文件流对象内部维护一个指向文件中当前读&#x2F;写位置的指针。</li><li><strong><code>seekg(offset, direction)</code> (seek get)</strong>：移动输入文件指针。</li><li><strong><code>seekp(offset, direction)</code> (seek put)</strong>：移动输出文件指针。<ul><li><code>offset</code>: 偏移量 (字节数)。</li><li><code>direction</code>: 起始位置：<ul><li><code>ios::beg</code>: 文件开头。</li><li><code>ios::cur</code>: 当前位置。</li><li><code>ios::end</code>: 文件末尾。</li></ul></li></ul></li><li><strong><code>tellg()</code> (tell get)</strong>：返回当前输入文件指针的位置。</li><li><strong><code>tellp()</code> (tell put)</strong>：返回当前输出文件指针的位置。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs cpp"><span class="hljs-function">fstream <span class="hljs-title">file</span><span class="hljs-params">(<span class="hljs-string">&quot;data.bin&quot;</span>, ios::in | ios::out | ios::binary)</span></span>;<br>file.<span class="hljs-built_in">seekg</span>(<span class="hljs-number">2</span> * <span class="hljs-built_in">sizeof</span>(<span class="hljs-type">int</span>), ios::beg); <span class="hljs-comment">// 定位到第3个int数据处</span><br><span class="hljs-type">int</span> value;<br>file.<span class="hljs-built_in">read</span>(<span class="hljs-built_in">reinterpret_cast</span>&lt;<span class="hljs-type">char</span>*&gt;(&amp;value), <span class="hljs-built_in">sizeof</span>(<span class="hljs-type">int</span>));<br></code></pre></td></tr></table></figure></li></ul><h3 id="七、字符串流-String-Streams"><a href="#七、字符串流-String-Streams" class="headerlink" title="七、字符串流 (String Streams)"></a>七、字符串流 (String Streams)</h3><ul><li>头文件 <code>&lt;sstream&gt;</code>。</li><li>在内存中的 <code>std::string</code> 对象上进行格式化的输入输出，行为类似文件流。</li><li><strong><code>istringstream</code></strong>: 从字符串读取数据。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs cpp">string data = <span class="hljs-string">&quot;10 20.5 Hello&quot;</span>;<br><span class="hljs-function">istringstream <span class="hljs-title">iss</span><span class="hljs-params">(data)</span></span>;<br><span class="hljs-type">int</span> i; <span class="hljs-type">double</span> d; string s;<br>iss &gt;&gt; i &gt;&gt; d &gt;&gt; s; <span class="hljs-comment">// i=10, d=20.5, s=&quot;Hello&quot;</span><br></code></pre></td></tr></table></figure></li><li><strong><code>ostringstream</code></strong>: 将数据格式化写入字符串。  <figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs cpp">ostringstream oss;<br><span class="hljs-type">int</span> age = <span class="hljs-number">30</span>; string name = <span class="hljs-string">&quot;Alice&quot;</span>;<br>oss &lt;&lt; <span class="hljs-string">&quot;Name: &quot;</span> &lt;&lt; name &lt;&lt; <span class="hljs-string">&quot;, Age: &quot;</span> &lt;&lt; age;<br>string result = oss.<span class="hljs-built_in">str</span>(); <span class="hljs-comment">// result = &quot;Name: Alice, Age: 30&quot;</span><br></code></pre></td></tr></table></figure></li><li><strong><code>stringstream</code></strong>: 支持从字符串读和向字符串写。</li></ul><h3 id="八、其它输入输出函数-流状态"><a href="#八、其它输入输出函数-流状态" class="headerlink" title="八、其它输入输出函数 (流状态)"></a>八、其它输入输出函数 (流状态)</h3><ul><li><code>ios</code> 类维护流的状态标志位，指示I&#x2F;O操作的结果。</li><li><strong>状态标志位</strong>:<ul><li><code>ios::goodbit</code>: 无错误。</li><li><code>ios::eofbit</code>: 到达文件末尾 (End Of File)。</li><li><code>ios::failbit</code>: 非致命I&#x2F;O错误 (如格式错误)，流仍可用但后续操作可能失败。</li><li><code>ios::badbit</code>: 致命I&#x2F;O错误 (如磁盘错误)，流损坏。</li></ul></li><li><strong>成员函数检查状态</strong>:<ul><li><code>good()</code>: 如果 <code>goodbit</code> 置位 (即无错误)，返回 <code>true</code>。</li><li><code>eof()</code>: 如果 <code>eofbit</code> 置位，返回 <code>true</code>。</li><li><code>fail()</code>: 如果 <code>failbit</code> 或 <code>badbit</code> 置位，返回 <code>true</code>。</li><li><code>bad()</code>: 如果 <code>badbit</code> 置位，返回 <code>true</code>。</li><li><code>operator bool()</code>: 流对象在布尔上下文中隐式转换为 <code>!fail()</code>。</li><li><code>operator!()</code>: 流对象在布尔上下文中隐式转换为 <code>fail()</code>。</li></ul></li><li><strong><code>clear(iostate state = ios::goodbit)</code></strong>: 清除（或设置）流的状态标志。</li><li><strong><code>rdstate()</code></strong>: 返回当前流状态字。</li></ul>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>OpenGL学习笔记</title>
    <link href="/2025/04/10/OpenGL%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/"/>
    <url>/2025/04/10/OpenGL%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/</url>
    
    <content type="html"><![CDATA[<!--  # OpenGL学习笔记 --><h2 id="一、简介"><a href="#一、简介" class="headerlink" title="一、简介"></a>一、简介</h2><p>OpenGL（Open Graphics Library）是一个跨编程语言、跨平台的编程图形程序接口，它将计算机的资源抽象称为一个个OpenGL的对象，对这些资源的操作抽象为一个个的OpenGL指令。<br>    OpenGL有着以下主要特性:</p><h3 id="1-低层次的渲染-API"><a href="#1-低层次的渲染-API" class="headerlink" title="1. 低层次的渲染 API"></a>1. 低层次的渲染 API</h3><p>OpenGL 提供了直接与图形硬件进行交互的能力。这使得它非常强大，因为它可以充分利用图形处理器（GPU）的性能。然而，这也意味着使用 OpenGL 需要对计算机图形学有深入的理解。</p><h3 id="2-状态机"><a href="#2-状态机" class="headerlink" title="2. 状态机"></a>2. 状态机</h3><p>OpenGL 是一个状态机，这意味着你可以通过一系列的函数调用来改变它的内部状态，然后这些状态会影响后续的渲染操作。</p><h3 id="3-可扩展性"><a href="#3-可扩展性" class="headerlink" title="3. 可扩展性"></a>3. 可扩展性</h3><p>OpenGL 的一个重要特性是它的可扩展性。硬件制造商可以为他们的硬件特性定义新的 OpenGL 函数和参数。这些扩展可以通过 OpenGL 的扩展机制被查询和使用。</p><h3 id="4-跨平台和跨语言"><a href="#4-跨平台和跨语言" class="headerlink" title="4. 跨平台和跨语言"></a>4. 跨平台和跨语言</h3><p>OpenGL 可以在所有主要的操作系统（如 Windows、macOS 和 Linux）上运行，并且有各种编程语言的绑定，包括 C、C++、Python、Java 和许多其他语言。</p><h3 id="5-显示列表和着色器"><a href="#5-显示列表和着色器" class="headerlink" title="5. 显示列表和着色器"></a>5. 显示列表和着色器</h3><p>OpenGL 提供了两种主要的方式来指定如何渲染图形。一种是通过使用显示列表来指定一系列的顶点和参数。另一种是通过使用着色器，这是一种在 GPU 上运行的小程序，可以控制图形的最终颜色和位置。</p><h2 id="二、前置知识"><a href="#二、前置知识" class="headerlink" title="二、前置知识"></a>二、前置知识</h2><p>在学习OpenGL之前，以下是一些应该需要了解的技术和知识：</p><h3 id="1-编程语言"><a href="#1-编程语言" class="headerlink" title="1.编程语言"></a>1.编程语言</h3><p>OpenGL 是一个 C 语言库，因此理解 C 语言（或 C++）的基本知识是非常有用的。然而，许多其他语言（如 Python、Java、JavaScript 等）也有 OpenGL 的绑定，所以你可以选择你最熟悉的语言来学习。</p><h3 id="2-计算机图形学基础"><a href="#2-计算机图形学基础" class="headerlink" title="2.计算机图形学基础"></a>2.计算机图形学基础</h3><p>理解基本的计算机图形学概念，如向量、矩阵、变换、光照、纹理等，可以帮助你更好地理解和使用 OpenGL。这些主题通常在计算机图形学的入门课程中介绍。</p><h3 id="3-线性代数"><a href="#3-线性代数" class="headerlink" title="3.线性代数"></a>3.线性代数</h3><p>在计算机图形学中，我们经常需要处理 2D 或 3D 空间中的点和向量，这需要一些线性代数的知识。至少要熟悉向量的加法、减法和点积，以及如何使用矩阵进行变换。</p><h3 id="4-计算机硬件"><a href="#4-计算机硬件" class="headerlink" title="4.计算机硬件"></a>4.计算机硬件</h3><p>理解计算机硬件，特别是图形处理器 (GPU) 的基本工作原理，可以帮助你理解 OpenGL 的许多设计决策，以及为什么某些操作比其他操作更快。</p><h3 id="5-IDE-和调试工具"><a href="#5-IDE-和调试工具" class="headerlink" title="5.IDE 和调试工具"></a>5.IDE 和调试工具</h3><p>熟悉一个集成开发环境（如 Visual Studio、Eclipse 或者 PyCharm）将会对你编写和调试代码非常有帮助。此外，学习使用像 gDEBugger 或者 RenderDoc 这样的 OpenGL 调试工具也是很有用的。</p><h3 id="6-API-文档和教程"><a href="#6-API-文档和教程" class="headerlink" title="6.API 文档和教程"></a>6.API 文档和教程</h3><p>熟悉 OpenGL 的官方文档（或你正在使用的语言绑定的文档）。这将是你在学习和使用 OpenGL 时的重要资源。同时，还有许多优秀的在线教程，如 learnopengl.com。</p><h2 id="三、OpenGL的基本概念"><a href="#三、OpenGL的基本概念" class="headerlink" title="三、OpenGL的基本概念"></a>三、OpenGL的基本概念</h2><h3 id="1-工作流程"><a href="#1-工作流程" class="headerlink" title="1.工作流程"></a>1.工作流程</h3><h4 id="定义图形数据"><a href="#定义图形数据" class="headerlink" title="定义图形数据"></a>定义图形数据</h4><p>首先，你需要定义你要渲染的图形数据，这些数据主要包括顶点数据。例如，如果你要渲染一个三角形，你就需要定义三个顶点的坐标。这些数据通常存储在顶点缓冲对象（Vertex Buffer Object，VBO）中。</p><h4 id="定义着色器程序"><a href="#定义着色器程序" class="headerlink" title="定义着色器程序"></a>定义着色器程序</h4><p>着色器是运行在图形处理器（GPU）上的小程序，用于控制图形的渲染过程。最常见的着色器类型有顶点着色器（Vertex Shader）和片元着色器（Fragment Shader）。顶点着色器用于处理顶点数据，片元着色器用于计算像素的颜色。</p><h4 id="渲染图形"><a href="#渲染图形" class="headerlink" title="渲染图形"></a>渲染图形</h4><p>在图形数据和着色器程序都准备好之后，你可以调用 OpenGL 的函数来渲染图形。在渲染过程中，OpenGL 会根据你设置的状态和着色器程序来处理顶点数据，生成图形，并将图形绘制到一个叫做帧缓冲（Frame Buffer）的内存区域。</p><h4 id="显示图形"><a href="#显示图形" class="headerlink" title="显示图形"></a>显示图形</h4><p>最后，你可以将帧缓冲的内容显示到屏幕上。这通常是通过交换前后帧缓冲（在双缓冲模式下）或者直接复制帧缓冲的内容到屏幕上完成的。</p><h3 id="2-基本组件"><a href="#2-基本组件" class="headerlink" title="2.基本组件"></a>2.基本组件</h3><h4 id="顶点（Vertex）"><a href="#顶点（Vertex）" class="headerlink" title="顶点（Vertex）"></a>顶点（Vertex）</h4><p>顶点是构成图形的基本元素。一个顶点主要包含位置信息，但也可能包含颜色、法线、纹理坐标等其他信息。在 OpenGL 中，顶点数据通常存储在顶点缓冲对象（VBO）中，并通过顶点数组对象（VAO）来管理。</p><h4 id="坐标（Coordinates）"><a href="#坐标（Coordinates）" class="headerlink" title="坐标（Coordinates）"></a>坐标（Coordinates）</h4><p>OpenGL 使用坐标来描述顶点的位置。在 3D 空间中，每个顶点都有一个三维坐标（x, y, z）。在 OpenGL 中，所有的坐标都被归一化到一个叫做裁剪空间（Clip Space）的立方体中，这个立方体的中心位于原点，长度、宽度和高度都是 2。</p><h4 id="颜色（Color）"><a href="#颜色（Color）" class="headerlink" title="颜色（Color）"></a>颜色（Color）</h4><p>颜色用于描述顶点或者片元的颜色。在 OpenGL 中，颜色通常由红、绿、蓝三个分量来表示，每个分量的值在 0 到 1 之间。有时候还会有一个额外的分量 alpha，用于表示颜色的透明度。</p><h4 id="纹理（Texture）"><a href="#纹理（Texture）" class="headerlink" title="纹理（Texture）"></a>纹理（Texture）</h4><p>纹理是一个图像，可以被映射到图形的表面上，增加图形的细节。每个顶点都可以有一个或多个纹理坐标，用于指定纹理的哪一部分应该被映射到顶点上。在 OpenGL 中，纹理数据通常存储在纹理对象中。</p><h4 id="着色器（Shader）"><a href="#着色器（Shader）" class="headerlink" title="着色器（Shader）"></a>着色器（Shader）</h4><p>着色器是运行在 GPU 上的小程序，用于控制图形的渲染过程。顶点着色器（Vertex Shader）用于处理顶点数据，片元着色器（Fragment Shader）用于计算像素的颜色。</p><h4 id="帧缓冲（Frame-Buffer）"><a href="#帧缓冲（Frame-Buffer）" class="headerlink" title="帧缓冲（Frame Buffer）"></a>帧缓冲（Frame Buffer）</h4><p>帧缓冲是一个内存区域，用于存储渲染的结果。在渲染过程中，OpenGL 会将图形绘制到帧缓冲中。帧缓冲中的数据最终可以被复制到屏幕上，或者被保存为图像文件。</p><h3 id="3-状态机模型"><a href="#3-状态机模型" class="headerlink" title="3.状态机模型"></a>3.状态机模型</h3><p>OpenGL 使用一种称为 “状态机” 的模型来管理它的操作。这意味着在任何给定的时间点，OpenGL 都有一组相关的 “状态” ，这些状态会影响 OpenGL 的操作和行为。</p><p>状态机模型的工作方式是，你设置一些状态，然后执行一些操作，这些操作会根据当前的状态来执行。例如，你可以设置颜色状态为红色，然后绘制一个形状，这个形状就会被渲染成红色。如果你改变颜色状态为蓝色，然后再绘制一个形状，那么新的形状就会被渲染成蓝色。</p><p>OpenGL 的状态包括诸如当前的颜色、光照参数、纹理参数、视口大小、裁剪平面等等。这些状态可以通过特定的 OpenGL 函数进行设置。例如，使用 glColor3f 函数可以设置当前的颜色，使用 glViewport 函数可以设置视口大小。</p><p>一旦设置了 OpenGL 的某个状态，该状态就会保持不变，直到你明确地改变它。这就是为什么 OpenGL 被称为状态机：它保存了一系列的状态，这些状态在调用绘图命令时决定了行为。</p><p>一个重要的概念是，OpenGL 的状态是全局的。这意味着如果你在一部分代码中改变了状态，那么这个改变会影响到所有后续的 OpenGL 调用，直到你再次改变那个状态。这就是为什么良好的编程实践是在更改了 OpenGL 状态后尽快恢复它，这样你就不会意外地影响到其他部分的代码。</p><p>总的来说，理解 OpenGL 是一种状态机，并且知道如何设置和管理这些状态，是有效使用 OpenGL 的关键。</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>Graham-Pollak定理的证明</title>
    <link href="/2024/12/12/Graham-Pollak%E5%AE%9A%E7%90%86%E7%9A%84%E8%AF%81%E6%98%8E/"/>
    <url>/2024/12/12/Graham-Pollak%E5%AE%9A%E7%90%86%E7%9A%84%E8%AF%81%E6%98%8E/</url>
    
    <content type="html"><![CDATA[<!--  # Graham-Pollak定理 --><h2 id="一、定理陈述"><a href="#一、定理陈述" class="headerlink" title="一、定理陈述"></a>一、定理陈述</h2><p>Graham - Pollak定理：任何$K_n$（完全图，有$n$个顶点）的分解中至少需要$n - 1$个完全二部图。</p><h2 id="二、符号定义"><a href="#二、符号定义" class="headerlink" title="二、符号定义"></a>二、符号定义</h2><ol><li>对于图中的每个顶点$v_i \in V$，定义一个实变量$x_i$，其中$V$是图中所有顶点的集合。</li><li>对于第$k$个二部图，将其左侧顶点集记为$L_k$，右侧顶点集记为$R_k$。</li><li>对于任意顶点集$S$，定义$X(S)&#x3D;\sum_{v_i \in S} x_i$，即$S$中顶点对应的变量之和。</li></ol><h2 id="三、用变量表示图的边"><a href="#三、用变量表示图的边" class="headerlink" title="三、用变量表示图的边"></a>三、用变量表示图的边</h2><ol><li>完全图$K_n$的边可以表示为$\sum_{1\leq i &lt; j\leq n}x_ix_j$。</li><li>因为二部图$k$覆盖了完全图的边，所以有$\sum_{1\leq i &lt; j\leq n}x_ix_j&#x3D;\sum_{k}X(L_k)X(R_k)$。</li></ol><h2 id="四、构建线性方程组"><a href="#四、构建线性方程组" class="headerlink" title="四、构建线性方程组"></a>四、构建线性方程组</h2><ol><li>考虑线性方程组$X(V) &#x3D; 0$和$X(L_k)&#x3D;0$，$X(R_k)&#x3D;0$对每个$k$成立。</li><li>对于任意满足上述线性方程组的解，有：<ul><li>$0 &#x3D; X(V)^2&#x3D;\left(\sum_{i}x_i\right)^2&#x3D;\sum_{i}x_i^2 + 2\sum_{1\leq i &lt; j\leq n}x_ix_j$。</li><li>由$\sum_{1\leq i &lt; j\leq n}x_ix_j&#x3D;\sum_{k}X(L_k)X(R_k)$，可得$0&#x3D;\sum_{i}x_i^2+ 2\sum_{k}X(L_k)X(R_k)$。</li></ul></li></ol><h2 id="五、矛盾推导"><a href="#五、矛盾推导" class="headerlink" title="五、矛盾推导"></a>五、矛盾推导</h2><ol><li>注意到$\sum_{i}x_i^2$是实变量的平方和。</li><li>对于实变量的平方和，只有当所有变量都为零时，其和才为零。</li><li>如果使用的二部图数量少于$n - 1$，则线性方程组中的方程数量少于变量数量（变量数量为$n$，方程数量少于$n$）。</li><li>根据线性代数知识，这样的齐次线性方程组会有非零解。</li><li>但如果存在非零解，就会导致$\sum_{i}x_i^2 &#x3D; 0$的矛盾（因为非零解意味着至少有一个$x_i$不为零，平方和就不可能为零）。</li></ol><h2 id="六、结论"><a href="#六、结论" class="headerlink" title="六、结论"></a>六、结论</h2><p>因此，为了避免这种矛盾，完全图$K_n$的分解中至少需要$n - 1$个完全二部图，这就证明了Graham - Pollak定理。</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>Mantel定理的证明</title>
    <link href="/2024/12/12/Mantel%E5%AE%9A%E7%90%86%E7%9A%84%E8%AF%81%E6%98%8E/"/>
    <url>/2024/12/12/Mantel%E5%AE%9A%E7%90%86%E7%9A%84%E8%AF%81%E6%98%8E/</url>
    
    <content type="html"><![CDATA[<!--  # Mantel定理 --><h2 id="一、定理陈述"><a href="#一、定理陈述" class="headerlink" title="一、定理陈述"></a>一、定理陈述</h2><p>Mantel定理：对于一个具有 $n$ 个顶点的简单无向图 $G$，其边数 $m$ 满足 $m\leq\frac{n^2}{4}$，当且仅当图 $G$ 是一个完全二部图 $K_{\lfloor\frac{n}{2}\rfloor,\lceil\frac{n}{2}\rceil}$ 时等号成立。</p><h2 id="二、证明过程"><a href="#二、证明过程" class="headerlink" title="二、证明过程"></a>二、证明过程</h2><h3 id="（一）基本设定和握手引理的应用"><a href="#（一）基本设定和握手引理的应用" class="headerlink" title="（一）基本设定和握手引理的应用"></a>（一）基本设定和握手引理的应用</h3><ol><li>设 $xy$ 是图 $G$ 的一条边，其中 $d(x)$ 和 $d(y)$ 分别表示顶点 $x$ 和 $y$ 的度数，$n$ 是图 $G$ 的顶点数。对于任意边 $xy$，有 $d(x)+d(y)\leq n$。</li><li>根据握手引理，对于图 $G$ 的顶点集 $V$ 和边集 $E$，有 $\sum_{x\in V}d(x)&#x3D;2m$，其中 $m$ 是图 $G$ 的边数。</li></ol><h3 id="（二）对度数平方和的分析"><a href="#（二）对度数平方和的分析" class="headerlink" title="（二）对度数平方和的分析"></a>（二）对度数平方和的分析</h3><ol><li>我们得到 $\sum_{x\in V}d(x)^2&#x3D;\sum_{xy\in E}(d(x)+d(y))\leq mn$。</li><li>这里等号成立的原因是：在计算 $\sum_{xy\in E}(d(x)+d(y))$ 时，因为每条边 $xy$ 对 $d(x)$ 和 $d(y)$ 都有贡献，所以每条边被算了两次。具体来说，对于每条边 $xy\in E$，在求和过程中，$d(x)$ 由于对应顶点 $x$ 出现一次，$d(y)$ 由于对应顶点 $y$ 出现一次，所以 $\sum_{xy\in E}(d(x)+d(y))$ 实际上就是对每条边算了两次。</li></ol><h3 id="（三）应用柯西-施瓦茨不等式"><a href="#（三）应用柯西-施瓦茨不等式" class="headerlink" title="（三）应用柯西 - 施瓦茨不等式"></a>（三）应用柯西 - 施瓦茨不等式</h3><ol><li>根据柯西 - 施瓦茨不等式：$(\sum_{x\in V}a_xb_x)^2\leq(\sum_{x\in V}a_x^2)(\sum_{x\in V}b_x^2)$。</li><li>在当前情况下，设 $a_x &#x3D; 1$，$b_x&#x3D;d(x)$，则有：<ul><li>$(\sum_{x\in V}d(x))^2\leq n\sum_{x\in V}d(x)^2$。</li><li>已知 $\sum_{x\in V}d(x)&#x3D;2m$，将其代入上式得到 $(2m)^2\leq n\sum_{x\in V}d(x)^2$。</li><li>又因为 $\sum_{x\in V}d(x)^2\leq mn$，所以可得 $4m^2\leq n\cdot mn$，即 $4m^2\leq mn^2$。</li></ul></li></ol><h3 id="（四）得出结论"><a href="#（四）得出结论" class="headerlink" title="（四）得出结论"></a>（四）得出结论</h3><ol><li>由 $4m^2\leq mn^2$，两边同时除以 $4n$，得到 $m\leq\frac{n^2}{4}$。</li><li>当 $m &#x3D; \frac{n^2}{4}$ 时，上述不等式都变成等式。这意味着所有顶点的度数相差不超过 $1$，此时图 $G$ 是一个完全二部图 $K_{\lfloor\frac{n}{2}\rfloor,\lceil\frac{n}{2}\rceil}$。</li></ol><h2 id="三、总结"><a href="#三、总结" class="headerlink" title="三、总结"></a>三、总结</h2><p>通过上述步骤，我们利用边和顶点度数的关系，结合握手引理和柯西 - 施瓦茨不等式，成功地证明了图 $G$ 的边数上界为 $\frac{n^2}{4}$，并且明确了在边数达到此上界时图的结构特征。这种证明方法体现了图论中不同定理和不等式在推导图的性质方面的强大作用。</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>柯西交错定理的证明</title>
    <link href="/2024/12/11/%E6%9F%AF%E8%A5%BF%E4%BA%A4%E9%94%99%E5%AE%9A%E7%90%86%E7%9A%84%E8%AF%81%E6%98%8E/"/>
    <url>/2024/12/11/%E6%9F%AF%E8%A5%BF%E4%BA%A4%E9%94%99%E5%AE%9A%E7%90%86%E7%9A%84%E8%AF%81%E6%98%8E/</url>
    
    <content type="html"><![CDATA[<!--  #柯西交错定理 --><h2 id="一、定理陈述"><a href="#一、定理陈述" class="headerlink" title="一、定理陈述"></a>一、定理陈述</h2><p>柯西交错定理： 设 $A&#x3D;\begin{bmatrix}a &amp; y^{*} \ y &amp; B\end{bmatrix}$ 是 $n$ 阶 Hermitian 矩阵，$B$ 是 $A$ 的 $n - 1$ 阶主子矩阵，$\mu_{2} \leq \mu_{3} \leq \cdots \leq \mu_{n}$ 是 $B$ 的特征值，$\lambda_{1} \leq \lambda_{2} \leq \cdots \leq \lambda_{n}$ 是 $A$ 的特征值，则 $\lambda_{n} \leq \mu_{n} \leq \lambda_{n - 1} \leq \mu_{n - 1} \leq \cdots \leq \lambda_{2} \leq \mu_{2} \leq \lambda_{1}$。</p><h2 id="二、证明过程"><a href="#二、证明过程" class="headerlink" title="二、证明过程"></a>二、证明过程</h2><h3 id="（一）特殊情况证明（-mu-n"><a href="#（一）特殊情况证明（-mu-n" class="headerlink" title="（一）特殊情况证明（$\mu_{n}&lt;\mu_{n - 1}&lt;\cdots&lt;\mu_{3}&lt;\mu_{2}$ 且 $z_{i} \neq 0$，$i &#x3D; 2,3,\cdots,n$）"></a>（一）特殊情况证明（$\mu_{n}&lt;\mu_{n - 1}&lt;\cdots&lt;\mu_{3}&lt;\mu_{2}$ 且 $z_{i} \neq 0$，$i &#x3D; 2,3,\cdots,n$）</h3><ol><li><strong>酉矩阵的构造</strong><ul><li>通过同时置换 $A$ 的行和列（若必要），假设子矩阵 $B$ 占据 $A$ 的第 $2,3,\cdots,n$ 行和列，即 $A&#x3D;\begin{bmatrix}a &amp; y^{*} \ y &amp; B\end{bmatrix}$。</li><li>因为 $B$ 是 Hermitian 矩阵，存在 $n - 1$ 阶酉矩阵 $U$ 使得 $U^{<em>} B U &#x3D; D$，其中 $D&#x3D;\text{diag}(\mu_{2},\mu_{3},\cdots,\mu_{n})$。设 $U^{</em>} y &#x3D; z&#x3D;(z_{2},z_{3},\cdots,z_{n})^{T}$。</li><li>构造酉矩阵 $V&#x3D;\begin{bmatrix}1 &amp; 0^{T} \ 0 &amp; U\end{bmatrix}$，则 $V^{<em>} A V&#x3D;\begin{bmatrix}a &amp; z^{</em>} \ z &amp; D\end{bmatrix}$。</li></ul></li><li><strong>行列式变换</strong><ul><li>首先，对 $V^{*} A V$ 的行列式变换进行说明：<ul><li>酉矩阵的性质：<ul><li>$V$ 是酉矩阵，满足 $V^{<em>}V &#x3D; VV^{</em>}&#x3D;I$，其中 $V^{*}$ 是 $V$ 的共轭转置，$I$ 是单位矩阵。</li></ul></li><li>相似矩阵的行列式性质：<ul><li>对于任意方阵 $A$ 和可逆矩阵 $P$，有 $\text{det}(P^{-1}AP)&#x3D;\text{det}(A)$。</li><li>在此处，$V$ 是酉矩阵，$V^{*}&#x3D;V^{-1}$。</li><li>所以 $V^{<em>}AV$ 和 $A$ 是相似矩阵，根据相似矩阵的行列式性质，$\text{det}(xI - A)&#x3D;\text{det}(xI - V^{</em>}AV)$。具体而言，设 $y &#x3D; xI - A$，$z &#x3D; xI - V^{<em>}AV$，则 $z &#x3D; V^{</em>}(xI - A)V &#x3D; V^{*}yV$。</li><li>由于 $\text{det}(V^{<em>}yV)&#x3D;\text{det}(V^{</em>})\text{det}(y)\text{det}(V)$，且 $\text{det}(V^{<em>})&#x3D;\overline{\text{det}(V)}$ 以及 $\text{det}(V)\overline{\text{det}(V)} &#x3D; 1$（酉矩阵性质），所以 $\text{det}(V^{</em>}yV)&#x3D;\text{det}(y)$，即 $\text{det}(xI - A)&#x3D;\text{det}(xI - V^{*}AV)$。</li></ul></li></ul></li><li>然后将 $\text{det}(xI - V^{*} A V)$ 沿第一行展开得到：<ul><li>$f(x)&#x3D;(x - a)(x-\mu_{2}) \cdots(x-\mu_{n})-\sum_{i &#x3D; 2}^{n} f_{i}(x)$</li><li>其中 $f_{i}(x)&#x3D;\vert z_{i}\vert^{2}(x-\mu_{2}) \cdots(x\widehat{-}\mu_{i}) \cdots(x-\mu_{n})$（$i &#x3D; 2,3,\cdots,n$），这里 $(x\widehat{-}\mu_{i})$ 表示去掉 $(x - \mu_{i})$ 这一项。</li></ul></li></ul></li><li><strong>$f_{i}(x)$ 正负性质的判断</strong><ul><li>分析 $f_{i}(x)$ 的值可知：<ul><li>当 $j \neq i$ 时，$f_{i}(\mu_{j}) &#x3D; 0$。</li><li>当 $i$ 为偶数时，$f_{i}(\mu_{i})&gt;0$；当 $i$ 为奇数时，$f_{i}(\mu_{i})&lt;0$。其原因如下：<ul><li>当 $i$ 为奇数时，考虑 $f_{i}(\mu_{i})&#x3D;\vert z_{i}\vert^{2}(\mu_{i} - \mu_{2})\cdots(\mu_{i} - \mu_{i - 1})(\mu_{i} - \mu_{i + 1})\cdots(\mu_{i} - \mu_{n})$ 这一乘积：<ul><li>对于这一乘积：<ul><li>当 $i$ 为奇数时，$\mu_{i}$ 小于它前面的偶数位置的 $\mu_{j}$（$j &lt; i$ 且 $j$ 为偶数），所以 $(\mu_{i} - \mu_{j})&lt;0$（$j &lt; i$ 且 $j$ 为偶数）。</li><li>同时，$\mu_{i}$ 大于它后面的偶数位置的 $\mu_{k}$（$k &gt; i$ 且 $k$ 为偶数），所以 $(\mu_{i} - \mu_{k})&lt;0$（$k &gt; i$ 且 $k$ 为偶数）。</li><li>而对于奇数位置的 $\mu_{l}$（$l\neq i$），$(\mu_{i} - \mu_{l})$ 的正负情况交替出现，但总体乘积的符号由偶数位置的 $\mu$ 决定。</li></ul></li><li>因为有偶数个 $(\mu_{i} - \mu_{j})&lt;0$（$j$ 为偶数且 $j\neq i$），所以 $(\mu_{i} - \mu_{2})\cdots(\mu_{i} - \mu_{i - 1})(\mu_{i} - \mu_{i + 1})\cdots(\mu_{i} - \mu_{n})&lt;0$。</li><li>又因为 $\vert z_{i}\vert^{2}&gt;0$，所以 $f_{i}(\mu_{i})&#x3D;\vert z_{i}\vert^{2}(\mu_{i} - \mu_{2})\cdots(\mu_{i} - \mu_{i - 1})(\mu_{i} - \mu_{i + 1})\cdots(\mu_{i} - \mu_{n})&lt;0$。</li></ul></li></ul></li></ul></li><li>进而得到 $f(\mu_{i})$ 的正负性：<ul><li>当 $i$ 为偶数时，$f(\mu_{i})&lt;0$；当 $i$ 为奇数时，$f(\mu_{i})&gt;0$（$i &#x3D; 2,3,\cdots,n$）。</li></ul></li></ul></li><li><strong>根的讨论</strong><ul><li>由于 $f(x)$ 是首项系数为正的 $n$ 次多项式，根据中间值定理可知方程 $f(x)&#x3D;0$ 存在 $n$ 个根 $\lambda_{1},\lambda_{2},\cdots,\lambda_{n}$，使得 $\lambda_{n}&lt;\mu_{n}&lt;\lambda_{n - 1}&lt;\mu_{n - 1}&lt;\cdots&lt;\lambda_{2}&lt;\mu_{2}&lt;\lambda_{1}$。</li></ul></li></ol><h3 id="（二）一般情况证明"><a href="#（二）一般情况证明" class="headerlink" title="（二）一般情况证明"></a>（二）一般情况证明</h3><ol><li><strong>构造趋近矩阵</strong><ul><li>设 $\epsilon_{1},\epsilon_{2},\cdots$ 是一列正实数，满足 $\epsilon_{k} \downarrow 0$，$z_{i}+\epsilon_{k} \neq 0$（$i &#x3D; 2,3,\cdots,n$；$k &#x3D; 1,2,\cdots$）且 $D+\epsilon_{k}\text{diag}(2,3,\cdots,n)$ 的对角元对固定的 $k$ 是不同的。</li><li>对 $k &#x3D; 1,2,\cdots$，定义 $C_{k}&#x3D;\begin{bmatrix}a &amp; z(\epsilon_{k})^{<em>} \ z(\epsilon_{k}) &amp; D(\epsilon_{k})\end{bmatrix}$，其中 $z(\epsilon_{k})&#x3D;z+\epsilon_{k}(1,1,\cdots,1)^{T}$，$D(\epsilon_{k})&#x3D;D+\epsilon_{k}\text{diag}(2,3,\cdots,n)$，且 $A_{k}&#x3D;V C_{k} V^{</em>}$，则 $A_{k}$ 是 Hermitian 矩阵且 $A_{k} \to A$。</li></ul></li><li><strong>特征值关系</strong><ul><li>设 $\lambda_{n}^{(k)} \leq\lambda_{n - 1}^{(k)} \leq\cdots \leq\lambda_{2}^{(k)} \leq\lambda_{1}^{(k)}$ 是 $A_{k}$ 的特征值，则 $\lambda_{n}^{(k)}&lt;\mu_{n}+n\epsilon_{k}&lt;\lambda_{n - 1}^{(k)}&lt;\mu_{n - 1}+(n - 1)\epsilon_{k}&lt;\cdots&lt;\lambda_{2}^{(k)}&lt;\mu_{2}+2\epsilon_{k}&lt;\lambda_{1}^{(k)}$。</li></ul></li><li><strong>极限情况</strong><ul><li>因为 $\lambda_{n}^{(k)},\lambda_{n - 1}^{(k)},\cdots,\lambda_{1}^{(k)}$ 是 $\text{det}(xI - A_{k}) &#x3D; 0$ 的 $n$ 个不同根且 $y&#x3D;\text{det}(xI - A_{k})$ 的图像与 $y&#x3D;\text{det}(xI - A)$ 的图像充分接近，所以 $(\lambda_{n}^{(k)},\lambda_{n - 1}^{(k)},\cdots,\lambda_{1}^{(k)}) \to (\lambda_{n},\lambda_{n - 1},\cdots,\lambda_{1})$，证明完成。</li></ul></li></ol><p>通过以上特殊情况和一般情况的证明，柯西交错定理得证。</p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>浅谈A*算法</title>
    <link href="/2024/11/17/%E6%B5%85%E8%B0%88A-%E7%AE%97%E6%B3%95/"/>
    <url>/2024/11/17/%E6%B5%85%E8%B0%88A-%E7%AE%97%E6%B3%95/</url>
    
    <content type="html"><![CDATA[<!-- # A* 算法 --><h2 id="1-算法思想"><a href="#1-算法思想" class="headerlink" title="1. 算法思想"></a>1. 算法思想</h2><pre><code class="hljs">A* 算法是一种启发式搜索算法，常用于在图中寻找从起始节点到目标节点的最短路径。它结合了 Dijkstra 算法（保证能找到最优解的广度优先搜索拓展）的特点以及启发式函数来引导搜索方向，以减少搜索空间。算法维护两个集合，一个是已探索的节点集合，另一个是待探索的节点集合（通常用优先队列实现，按照节点的评估函数值排序）。每个节点都有一个评估函数 `f(n)`，它由两部分组成：从起始节点到当前节点 `n` 的实际代价 `g(n)`（类似 Dijkstra 算法中的距离度量），以及从当前节点 `n` 到目标节点的预估代价 `h(n)`（启发式函数），即 `f(n) = g(n) + h(n)`。在搜索过程中，每次从待探索集合中取出 `f(n)` 值最小的节点进行扩展，直到找到目标节点或者待探索集合为空。启发式函数 `h(n)` 需要满足一定的条件（可采纳性，即 `h(n)` 始终小于等于节点 `n` 到目标节点的实际最短距离），这样才能保证 A* 算法最终找到的是最优解。</code></pre><h2 id="2-代码示例"><a href="#2-代码示例" class="headerlink" title="2. 代码示例"></a>2. 代码示例</h2><p>（Python 代码，以简单的二维网格地图为例，寻找起点到终点的最短路径，地图中 0 表示可通行，1 表示障碍物）</p><pre><code class="hljs"><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br></pre></td><td class="code"><pre><code class="hljs python"><br><span class="hljs-keyword">import</span> heapq<br><br><span class="hljs-comment"># 定义四个方向的移动向量（上下左右）</span><br>dx = [-<span class="hljs-number">1</span>, <span class="hljs-number">1</span>, <span class="hljs-number">0</span>, <span class="hljs-number">0</span>]<br>dy = [<span class="hljs-number">0</span>, <span class="hljs-number">0</span>, -<span class="hljs-number">1</span>, <span class="hljs-number">1</span>]<br><br><span class="hljs-keyword">def</span> <span class="hljs-title function_">heuristic</span>(<span class="hljs-params">a, b</span>):<br>    <span class="hljs-string">&quot;&quot;&quot;</span><br><span class="hljs-string">    启发式函数，这里使用曼哈顿距离（在二维网格中常用），计算两点之间的预估距离</span><br><span class="hljs-string">    &quot;&quot;&quot;</span><br>    <span class="hljs-keyword">return</span> <span class="hljs-built_in">abs</span>(a[<span class="hljs-number">0</span>] - b[<span class="hljs-number">0</span>]) + <span class="hljs-built_in">abs</span>(a[<span class="hljs-number">1</span>] - b[<span class="hljs-number">1</span>])<br><br><span class="hljs-keyword">def</span> <span class="hljs-title function_">a_star</span>(<span class="hljs-params">grid, start, goal</span>):<br>    <span class="hljs-string">&quot;&quot;&quot;</span><br><span class="hljs-string">    A* 算法实现</span><br><span class="hljs-string">    &quot;&quot;&quot;</span><br>    rows = <span class="hljs-built_in">len</span>(grid)<br>    cols = <span class="hljs-built_in">len</span>(grid[<span class="hljs-number">0</span>])<br>    <span class="hljs-comment"># 初始化 g 值，初始设为无穷大</span><br>    g_scores = [[<span class="hljs-built_in">float</span>(<span class="hljs-string">&#x27;inf&#x27;</span>)] * cols <span class="hljs-keyword">for</span> _ <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(rows)]<br>    g_scores[start[<span class="hljs-number">0</span>]][start[<span class="hljs-number">1</span>]] = <span class="hljs-number">0</span><br>    <span class="hljs-comment"># 初始化 f 值，初始设为无穷大</span><br>    f_scores = [[<span class="hljs-built_in">float</span>(<span class="hljs-string">&#x27;inf&#x27;</span>)] * cols <span class="hljs-keyword">for</span> _ <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(rows)]<br>    f_scores[start[<span class="hljs-number">0</span>]][start[<span class="hljs-number">1</span>]] = heuristic(start, goal)<br>    <span class="hljs-comment"># 优先队列，存储待探索的节点，元素为 (f值, (x坐标, y坐标))</span><br>    open_set = [(f_scores[start[<span class="hljs-number">0</span>]][start[<span class="hljs-number">1</span>]], start)]<br>    came_from = &#123;&#125;<br>    <span class="hljs-keyword">while</span> open_set:<br>        _, current = heapq.heappop(open_set)<br>        <span class="hljs-keyword">if</span> current == goal:<br>            path = []<br>            <span class="hljs-keyword">while</span> current <span class="hljs-keyword">in</span> came_from:<br>                path.append(current)<br>                current = came_from[current]<br>            path.append(start)<br>            <span class="hljs-keyword">return</span> path[::-<span class="hljs-number">1</span>]<br>        <span class="hljs-keyword">for</span> i <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(<span class="hljs-number">4</span>):<br>            new_x = current[<span class="hljs-number">0</span>] + dx[i]<br>            new_y = current[<span class="hljs-number">1</span>] + dy[i]<br>            <span class="hljs-keyword">if</span> <span class="hljs-number">0</span> &lt;= new_x &lt; rows <span class="hljs-keyword">and</span> <span class="hljs-number">0</span> &lt;= new_y &lt; cols <span class="hljs-keyword">and</span> grid[new_x][new_y] == <span class="hljs-number">0</span>:<br>                tentative_g_score = g_scores[current[<span class="hljs-number">0</span>]][current[<span class="hljs-number">1</span>]] + <span class="hljs-number">1</span><br>                <span class="hljs-keyword">if</span> tentative_g_score &lt; g_scores[new_x][new_y]:<br>                    came_from[(new_x, new_y)] = current<br>                    g_scores[new_x][new_y] = tentative_g_score<br>                    f_scores[new_x][new_y] = tentative_g_score + heuristic((new_x, new_y), goal)<br>                    heapq.heappush(open_set, (f_scores[new_x][new_y], (new_x, new_y)))<br>    <span class="hljs-keyword">return</span> <span class="hljs-literal">None</span><br></code></pre></td></tr></table></figure></code></pre><p>你可以使用以下方式调用这个函数：</p><pre><code class="hljs"><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs python"><br><span class="hljs-comment"># 示例地图</span><br>grid = [<br>    [<span class="hljs-number">0</span>, <span class="hljs-number">0</span>, <span class="hljs-number">0</span>, <span class="hljs-number">0</span>],<br>    [<span class="hljs-number">0</span>, <span class="hljs-number">1</span>, <span class="hljs-number">0</span>, <span class="hljs-number">0</span>],<br>    [<span class="hljs-number">0</span>, <span class="hljs-number">0</span>, <span class="hljs-number">0</span>, <span class="hljs-number">0</span>],<br>    [<span class="hljs-number">0</span>, <span class="hljs-number">0</span>, <span class="hljs-number">0</span>, <span class="hljs-number">0</span>]<br>]<br>start = (<span class="hljs-number">0</span>, <span class="hljs-number">0</span>)<br>goal = (<span class="hljs-number">3</span>, <span class="hljs-number">3</span>)<br>path = a_star(grid, start, goal)<br><span class="hljs-built_in">print</span>(path)<br><br></code></pre></td></tr></table></figure></code></pre><h2 id="3-复杂度分析"><a href="#3-复杂度分析" class="headerlink" title="3. 复杂度分析"></a>3. 复杂度分析</h2><ul><li><strong>时间复杂度</strong>：在最坏情况下（比如启发式函数不够好，接近均匀搜索整个图空间时），A* 算法的时间复杂度与图中节点数 <code>n</code> 和边数 <code>m</code> 相关，类似于 Dijkstra 算法，时间复杂度为 $O((n + m) \log n)$，其中 $\log n$ 来自于优先队列操作的复杂度。但如果启发式函数非常好，能有效引导搜索方向，实际运行时间可以大大优于这个复杂度，接近最优解所在路径长度的线性复杂度。</li><li><strong>空间复杂度</strong>：需要存储每个节点的 <code>g</code> 值、<code>f</code> 值以及记录节点的前驱等信息，在最坏情况下，空间复杂度为 $O(n)$，<code>n</code> 为图中节点的数量，主要取决于节点数以及需要记录的辅助信息占用的空间。</li></ul><h2 id="4-正确性证明（基于可采纳性）"><a href="#4-正确性证明（基于可采纳性）" class="headerlink" title="4. 正确性证明（基于可采纳性）"></a>4. 正确性证明（基于可采纳性）</h2><ul><li>假设存在一条从起始节点 <code>s</code> 到目标节点 <code>t</code> 的最优路径 <code>P</code>，其长度为 <code>L</code>。我们要证明 A* 算法一定能找到这条最优路径。</li><li>由于启发式函数 <code>h(n)</code> 满足可采纳性，即对于任意节点 <code>n</code>，<code>h(n)</code> 始终小于等于节点 <code>n</code> 到目标节点的实际最短距离。</li><li>当 A* 算法扩展节点时，它总是选择 <code>f(n)</code> 值最小的节点进行扩展，其中 <code>f(n) = g(n) + h(n)</code>。因为 <code>h(n)</code> 不会高估到目标的距离，所以如果某个节点 <code>n</code> 在最优路径 <code>P</code> 上，那么最终 <code>f(n)</code> 值会小于等于 <code>L</code>（最优路径长度），且沿着最优路径上的节点依次被扩展，直到到达目标节点 <code>t</code>。</li><li>而且，不会出现因为优先选择了其他非最优路径上的节点而错过最优路径的情况，因为那些非最优路径上的节点最终计算出的 <code>f</code> 值必然大于最优路径对应的 <code>f</code> 值（由于 <code>h</code> 函数的可采纳性以及实际代价 <code>g</code> 的累积），所以 A* 算法最终一定会沿着最优路径扩展到目标节点，从而找到最优解。</li></ul>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>浅谈 Johnson 算法</title>
    <link href="/2024/11/17/%E6%B5%85%E8%B0%88Johnson%E7%AE%97%E6%B3%95/"/>
    <url>/2024/11/17/%E6%B5%85%E8%B0%88Johnson%E7%AE%97%E6%B3%95/</url>
    
    <content type="html"><![CDATA[<!-- # Johnson 算法 --><h2 id="1-算法思想"><a href="#1-算法思想" class="headerlink" title="1. 算法思想"></a>1. 算法思想</h2><p>Johnson 算法用于解决带权有向图中所有节点对之间的最短路径问题，它结合了 Bellman-Ford 算法和 Dijkstra 算法的优点。<br>首先，通过添加一个虚拟节点 <code>s</code> 到图中，将所有边的权重进行重新赋值（重赋权技巧），使得图中不存在负权环的同时，利用 Bellman-Ford 算法计算出从虚拟节点 <code>s</code> 到图中每个节点的最短距离 <code>h(v)</code>。<br>然后，对于图中的每一对节点 <code>u</code> 和 <code>v</code>，将原边权重 <code>w(u, v)</code> 替换为新的权重 <code>w&#39;(u, v) = w(u, v) + h(u) - h(v)</code>，这样处理后可以保证新的权重是非负的（通过利用最短距离的性质）。<br>最后，针对每一个节点作为源点，使用 Dijkstra 算法来计算经过重赋权后的图中到其他节点的最短路径，再根据重赋权的关系将计算出的距离转换回原图中的最短路径距离。</p><h2 id="2-代码示例"><a href="#2-代码示例" class="headerlink" title="2. 代码示例"></a>2. 代码示例</h2><p>（Python 代码，以下是简化实现，假设输入的图用邻接矩阵表示，图中不存在负权环，节点编号从 0 开始）</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br></pre></td><td class="code"><pre><code class="hljs python"><br><span class="hljs-keyword">import</span> math<br><br><span class="hljs-keyword">def</span> <span class="hljs-title function_">bellman_ford</span>(<span class="hljs-params">graph, num_vertices</span>):<br>    <span class="hljs-string">&quot;&quot;&quot;</span><br><span class="hljs-string">    Bellman-Ford 算法，用于计算从虚拟源点到各节点的最短距离</span><br><span class="hljs-string">    &quot;&quot;&quot;</span><br>    <span class="hljs-comment"># 初始化距离数组，初始设为无穷大</span><br>    dist = [math.inf] * num_vertices<br>    dist[<span class="hljs-number">0</span>] = <span class="hljs-number">0</span><br>    <span class="hljs-keyword">for</span> _ <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(num_vertices - <span class="hljs-number">1</span>):<br>        <span class="hljs-keyword">for</span> u <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(num_vertices):<br>            <span class="hljs-keyword">for</span> v <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(num_vertices):<br>                <span class="hljs-keyword">if</span> graph[u][v]!= math.inf:<br>                    <span class="hljs-keyword">if</span> dist[u] + graph[u][v] &lt; dist[v]:<br>                        dist[v] = dist[u] + graph[u][v]<br>    <span class="hljs-keyword">return</span> dist<br><br><span class="hljs-keyword">def</span> <span class="hljs-title function_">dijkstra</span>(<span class="hljs-params">graph, source, num_vertices</span>):<br>    <span class="hljs-string">&quot;&quot;&quot;</span><br><span class="hljs-string">    Dijkstra 算法，用于计算从给定源点到其他节点的最短路径</span><br><span class="hljs-string">    &quot;&quot;&quot;</span><br>    <span class="hljs-comment"># 初始化距离数组，初始设为无穷大</span><br>    dist = [math.inf] * num_vertices<br>    dist[source] = <span class="hljs-number">0</span><br>    visited = [false] * num_vertices<br>    <span class="hljs-keyword">for</span> _ <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(num_vertices):<br>        min_dist = math.inf<br>        min_index = -<span class="hljs-number">1</span><br>        <span class="hljs-keyword">for</span> v <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(num_vertices):<br>            <span class="hljs-keyword">if</span> <span class="hljs-keyword">not</span> visited[v] <span class="hljs-keyword">and</span> dist[v] &lt; min_dist:<br>                min_dist = dist[v]<br>                min_index = v<br>        visited[min_index] = true<br>        <span class="hljs-keyword">for</span> v <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(num_vertices):<br>            <span class="hljs-keyword">if</span> graph[min_index][v]!= math.inf <span class="hljs-keyword">and</span> dist[min_index] + graph[min_index][v] &lt; dist[v]:<br>                dist[v] = dist[min_index] + graph[min_index][v]<br>    <span class="hljs-keyword">return</span> dist<br><br><span class="hljs-keyword">def</span> <span class="hljs-title function_">johnson</span>(<span class="hljs-params">graph</span>):<br>    <span class="hljs-string">&quot;&quot;&quot;</span><br><span class="hljs-string">    Johnson 算法实现</span><br><span class="hljs-string">    &quot;&quot;&quot;</span><br>    num_vertices = <span class="hljs-built_in">len</span>(graph)<br>    <span class="hljs-comment"># 添加虚拟源点，构建新的图</span><br>    new_graph = [[math.inf] * (num_vertices + <span class="hljs-number">1</span>) <span class="hljs-keyword">for</span> _ <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(num_vertices + <span class="hljs-number">1</span>)]<br>    <span class="hljs-keyword">for</span> i <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(num_vertices):<br>        <span class="hljs-keyword">for</span> j <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(num_vertices):<br>            new_graph[i][j] = graph[i][j]<br>    <span class="hljs-comment"># 运行 Bellman-Ford 算法</span><br>    h = bellman_ford(new_graph, num_vertices + <span class="hljs-number">1</span>)[<span class="hljs-number">0</span>:num_vertices]<br>    <span class="hljs-comment"># 重赋权边</span><br>    new_weight_graph = [[math.inf] * num_vertices <span class="hljs-keyword">for</span> _ <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(num_vertices)]<br>    <span class="hljs-keyword">for</span> u <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(num_vertices):<br>    <span class="hljs-keyword">for</span> v <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(num_vertices):<br>        <span class="hljs-keyword">if</span> graph[u][v]!= math.inf:<br>            new_weight_graph[u][v] = graph[u][v] + h[u] - h[v]<br>    <span class="hljs-comment"># 对每个节点作为源点运行 Dijkstra 算法，存储所有节点对最短路径结果</span><br>    all_pairs_shortest_paths = []<br>    <span class="hljs-keyword">for</span> source <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(num_vertices):<br>        dist = dijkstra(new_weight_graph, source, num_vertices)<br>        <span class="hljs-comment"># 还原原权重下的最短路径距离</span><br>        <span class="hljs-keyword">for</span> v <span class="hljs-keyword">in</span> <span class="hljs-built_in">range</span>(num_vertices):<br>            <span class="hljs-keyword">if</span> dist[v]!= math.inf:<br>                dist[v] = dist[v] - h[source] + h[v]<br>        all_pairs_shortest_paths.append(dist)<br>    <span class="hljs-keyword">return</span> all_pairs_shortest_paths<br></code></pre></td></tr></table></figure><p>你可以使用以下方式调用这个函数：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs python"><br><span class="hljs-comment"># 示例图，用邻接矩阵表示，无穷大表示无边相连</span><br>graph = [<br>    [<span class="hljs-number">0</span>, <span class="hljs-number">3</span>, math.inf, <span class="hljs-number">7</span>],<br>    [<span class="hljs-number">8</span>, <span class="hljs-number">0</span>, <span class="hljs-number">2</span>, math.inf],<br>    [<span class="hljs-number">5</span>, math.inf, <span class="hljs-number">0</span>, <span class="hljs-number">1</span>],<br>    [<span class="hljs-number">2</span>, math.inf, math.inf, <span class="hljs-number">0</span>]<br>]<br>shortest_paths = johnson(graph)<br><span class="hljs-built_in">print</span>(shortest_paths)<br><br></code></pre></td></tr></table></figure><h2 id="3-复杂度分析"><a href="#3-复杂度分析" class="headerlink" title="3. 复杂度分析"></a>3. 复杂度分析</h2><ul><li><p><strong>时间复杂度</strong>：</p><ul><li><p>第一步 Bellman-Ford 算法的时间复杂度为 $O(V^3)$，其中 <code>V</code> 是图中节点的数量，因为它要对每条边进行 <code>V - 1</code> 次松弛操作，每次松弛操作遍历所有边（在邻接矩阵表示下），时间复杂度为 $O(V^2)$，整体就是 $O(V^3)$。</p></li><li><p>第二步对每个节点运行 Dijkstra 算法，若使用二叉堆实现优先队列的 Dijkstra 算法，每次时间复杂度为 $O((V + E) \log V)$，<code>E</code> 是边数，总共运行 <code>V</code> 次，这部分时间复杂度就是 $O(V (V + E) \log V)$。在稀疏图（<code>E</code> 接近 <code>V</code>）情况下，时间复杂度约为 $O(V^2 \log V)$，在稠密图（<code>E</code> 接近 $V^2$）情况下，时间复杂度约为 $O(V^3 \log V)$。所以 Johnson 算法总的时间复杂度在最坏情况下为 $O(V^3 \log V)$。</p></li></ul></li><li><p><strong>空间复杂度</strong>：需要存储图的相关信息（如邻接矩阵等）、距离数组等，在 Bellman-Ford 和 Dijkstra 算法执行过程中还需要一些临时空间，总体空间复杂度主要取决于图的表示方式以及节点数量，一般来说在 $O(V^2)$ 级别（使用邻接矩阵表示图时），<code>V</code> 为节点数量。</p></li></ul><h2 id="4-正确性证明"><a href="#4-正确性证明" class="headerlink" title="4. 正确性证明"></a>4. 正确性证明</h2><ul><li><p><strong>重赋权后不存在负权边</strong>：<br>根据 Bellman-Ford 算法计算出的 <code>h(v)</code> 是从虚拟节点 <code>s</code> 到节点 <code>v</code> 的最短距离。对于图中任意边 <code>(u, v)</code>，有 <code>w&#39;(u, v) = w(u, v) + h(u) - h(v)</code>，根据最短路径的三角不等式（从 <code>s</code> 到 <code>v</code> 的最短路径长度不会大于从 <code>s</code> 经过 <code>u</code> 再到 <code>v</code> 的路径长度），可得 <code>h(v) ≤ h(u) + w(u, v)</code>，移项后就是 <code>w&#39;(u, v) = w(u, v) + h(u) - h(v) ≥ 0</code>，所以重赋权后的边权重都是非负的。</p></li><li><p><strong>计算的最短路径等价于原图最短路径</strong>：<br>设 <code>d(u, v)</code> 是原图中从 <code>u</code> 到 <code>v</code> 的最短路径距离，<code>d&#39;(u, v)</code> 是重赋权后图中从 <code>u</code> 到 <code>v</code> 的最短路径距离。根据重赋权的定义以及最短路径的性质，对于任意节点对 <code>u</code> 和 <code>v</code>，有 <code>d&#39;(u, v) = d(u, v) + h(u) - h(v)</code>，在计算出重赋权后图中的最短路径 <code>d&#39;(u, v)</code> 后，通过还原操作 <code>d(u, v) = d&#39;(u, v) - h(u) + h(v)</code> 就可以得到原图中的最短路径距离，所以 Johnson 算法最终计算出的所有节点对之间的最短路径是正确的。</p></li></ul>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>Hello World</title>
    <link href="/2024/11/17/hello-world/"/>
    <url>/2024/11/17/hello-world/</url>
    
    <content type="html"><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p><h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo new <span class="hljs-string">&quot;My New Post&quot;</span><br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p><h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo server<br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p><h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo generate<br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p><h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">$ hexo deploy<br></code></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>]]></content>
    
    
    
  </entry>
  
  
  
  
</search>
